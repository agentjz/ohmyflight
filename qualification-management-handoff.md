# 资质管理接手地图

这份文档用于帮助接手南货航资质管理工作的人，先建立总体业务地图，再逐步把人工查询和 Excel 维护变成可核对、可复用的工具流程。

它不是手册的替代品，也不是某一张 Excel 的操作说明。手册原文是规则依据，Excel 是人员当前状态和办理进度的台账，菜鸟教程是把规则讲清楚并提供检索入口的知识层，培训皇帝是目前已经落地的培训排班和有效期复核工具。后续接口化工具是在这几层之上减少人工查询，不改变手册规则。

## 1. 先记住这条主线

资质管理是一条需要持续核对和闭环维护的业务链：

```text
确认人员和当前状态
  -> 判断哪些培训/检查即将到期
  -> 排培训或安排检查
  -> 核对实际完成情况和结果
  -> 把结果更新到台账
  -> 按规则计算新的有效期或状态
  -> 按要求完成签注、聘任、审批、发布、入库等生效动作
  -> 继续监控下一轮到期风险
```

这里有几个必须分开的事件：

- **培训安排**：回答计划什么时候做，但不能证明已经完成。
- **培训完成或记录录入**：回答这次培训是否实际完成、是否已经登记，但不能自动证明所有运行权利已经生效。
- **检查或考试合格**：回答能否通过该项检查，但不能自动替代执照签注、聘任或资质发布。
- **有效期更新**：回答下一次需要关注到什么时候，但不能替代检查失败后的恢复路径。
- **签注、聘任、审批、发布和系统入库**：回答权利是否正式生效，不能只凭培训日期推断。

## 2. 本次接手使用的输入

路径由每次任务的用户输入决定，不要把某一台电脑上的绝对路径当成永久规则。进入工作时先确定下面这些输入：

- `$MANUAL_DIR`：用户指定的手册原文目录。
- `$TRAINING_PROGRAM`：在 `$MANUAL_DIR` 中确认版本后的《飞行人员训练大纲》文件。
- `$TECHNICAL_MANUAL`：在 `$MANUAL_DIR` 中确认版本后的《飞行技术管理手册》文件。
- `$QUALIFICATION_XLSX`：用户指定的飞行实力或资质维护 Excel 文件。
- `$TUTORIAL_DIR`：菜鸟教程知识源和页面目录。
- `$API_PROJECT_DIR`：接口项目目录。本次已经确认的项目是 `C:\Users\Administrator\Desktop\repository\csn2api`，项目名称按实际目录写作 `csn2api`。

每次开始前先记录文件名、版本号、修订号、修改日期、工作表名称和输入文件大小。目录里有旧版、副本或运行手册时，不能按文件名相似度直接选用。

## 3. 四层资料各自负责什么

### 3.1 两本手册：回答“规则是什么”

- 《飞行人员训练大纲》主要回答训练如何实施：训练对象、课程、课时、训练器/模拟机、本场或航线训练、检查项目和训练结果。
- 《飞行技术管理手册》主要回答等级、检查、运行权利和管理如何生效：技术等级、经历门槛、熟练检查、聘任、执照、资质审批发布、档案和恢复。
- 同一件事通常要两本手册一起读。训练合格、检查合格、签注、聘任、审批发布和系统入库不是同一个动作。
- 手册没有写出的昼间/夜间、PF/PM、航段/起落组合，不能按常识补出来。

### 3.2 Excel：回答“这个人现在是什么状态”

Excel 是维护台账，不是规则的最终来源。它把手册规则落成当前人员的日期、进度、经历、检查和办理状态，通常需要反复核对：

- `人员信息`：人员基本信息、技术信息、熟练检查、机长年度、120-100、型别更新、体检和多项运行资质有效期。
- `副驾驶进度`：副驾驶 A1、A2、B、C、D 等训练或等级进度。
- `一二检进度`：机长转机型后的左座经历、起落以及一级、二级检查进度。
- `机长进度`：机长技术等级、本机型/左座/机长经历和起落等进度。
- `执照划转`：执照或相关划转办理台账。
- `检查员管理`：委任代表、熟练检查员、通信检查员等权利及有效期。
- `EBT教员管理`、`教员分级`：EBT、航线教员和型别教员分级及有效期。
- `重要数据统计`、`划转联系人`、`实力简介`、`阿拉木图偏离`：管理统计、联系人或专项业务信息，使用前先确认当前版本的字段含义。

不要把表名、列名或字母等级直接当成规则解释。先做“字段 -> 手册概念”的映射，再决定这个字段能否参与自动判断。

### 3.3 菜鸟教程：回答“规则怎么理解、去哪找”

菜鸟教程把两本手册里会改变人员成长、权限和资格恢复的核心事件链整理成可搜索的知识入口。它适合先建立概念、定位条款和查恢复路径，但办理实际签注、聘任、资质发布或排班入库时仍要回到现行手册和个人记录。

教程的价值不是把每个表格字段都抄一遍，而是把这些高风险问题讲清楚：

- 谁可以进入，必须完成什么训练和经历；
- 检查的对象、位置、昼间/夜间、PF/PM、航段、起落、人工飞行时间分别是什么；
- 资格怎样取得、保持、失效和恢复；
- 检查不合格后是否补训、增加经历、降级、停飞或退出；
- 训练合格后还需要哪些签注、聘任、审批、发布和入库动作。

教程中的恢复规则只保留权威正文一份，其他条目只在确实能回答“当前对象失效后怎么办”时链接过去，避免把简单规则做成关系图。

### 3.4 API 工具：回答“能不能自动拿到原来人工查的数据”

`csn2api` 项目（路径由 `$API_PROJECT_DIR` 提供）应先作为数据查询和证据获取工具理解，不要一开始就把它当成新的业务规则引擎。它的第一价值是把逐人、逐日期、逐页面点击的查询变成可重复的接口调用，例如飞行经历、左座经历、起落数等。

接口返回的是事实数据；“这些数据是否满足手册条件”仍由经过核对的规则层判断。接口项目和资质 Excel 之间要保留来源、查询时间、参数和原始结果，便于复核。

## 4. 培训与有效期在整体流程中的位置

危险品、应急训练、CRM、航空安保、TSA、英语考试等培训，属于资质管理流程中的培训和有效期维护环节。培训皇帝目前覆盖这一环节的以下工作：

1. 导入整本培训总表，先做 Excel 健康检查。
2. 按人员当前有效期和培训安排判断已过期、必须排、推荐排、已排未覆盖和异常。
3. 查询个人资质有效期，查看月度培训日历和实际培训负载。
4. 预测现有安排执行后未来的资质到期压力。
5. 按已确认的规则更新有效期，并导出 Excel 副本和更新报告。

这个环节需要重点核对的口径：

- `航空安保`和`TSA`是两个独立项目；同一天同时完成时，两个项目表都要有记录。
- `CRM`是年度参加情况核对和培训负载统计，不直接产生资质有效期或排班覆盖状态。
- `机器看 = Y`只用于有效期更新；`培训信息是否录入`是人工登记状态，不能把它当成“是否完成”的唯一依据。
- 备注含“取消”的记录不参与有效覆盖；表里有一条记录，不等于它一定覆盖本轮到期。
- 工具的自动排班检查只覆盖当前已有规则的项目；未纳入覆盖规则的资质仍要回 Excel 和手册人工核对。
- 工具导出的是新副本，不覆盖原始总表。导出后要抽查人员、项目、日期、备注和更新报告。

这个环节的闭环是：确认当前有效期，安排培训，核对实际完成情况，按规则更新台账，再检查新的有效期和下一轮风险。它需要和技术等级、飞行经历、检查、签注、聘任、审批、发布及系统入库等环节衔接，不能单独代表完整资质流程。

## 5. 其他业务环节的学习与核对

以下内容不能从 Excel 的字母或现有人员状态反推，必须逐条回到手册核对：

- 副驾驶升级、机长升级和转机型；
- 一检、二检、熟练检查、报务检查；
- 技术能力恢复检查、资质能力恢复检查及不合格后的分支；
- 航段、起落、飞行次数、起飞/着陆、人工飞行时间；
- 昼间/夜间、PF/PM、操作驾驶员/辅助操作驾驶员；
- 教员、考试员、检查员聘任和权利生效；
- PC-CP 等签注时点；
- Z、B、C、D 等机长状态的关系。

特别是 Z/B/C/D 的关系，本交接文档不预设 Z→B→C→D 的自动晋级链，必须以手册原文和业务确认结果为准。类似地，PC-CP 也要以原文确认“哪一次不合格、需要什么补充训练和检查、何时可以签注”，不能只凭记忆操作。

## 6. 手册语义核对方法

每次新增规则或解释 Excel 字段时，按下面顺序做：

1. **确认版本**：扫描 `$MANUAL_DIR`，确认当前适用文件，不混用旧版、对比稿和运行手册。
2. **结构化读取**：Word 优先用 `python-docx` 或仓库中的结构化提取脚本读取段落、表格和样式；PDF 先确认是否为可搜索文字。
3. **先定位后通读**：先用章节、标题和关键词定位，再从适用对象和进入条件读到检查、合格/不合格、记录和审批动作结束。
4. **保留证据**：每条结论记录手册名称、版本、章节/小节、原文位置和关键上下文。
5. **做语义拆分**：分别记录数量、单位、位置、角色、时段、起算事件、窗口和失败分支；不要把“航段”改写成“起落”，不要把 PF 自动改成 PM，也不要把昼间条件扩成夜间条件。
6. **双向检查**：教程每一条核心记录都能回到手册；手册中所有会改变人员状态或权限的核心事件，要么进入教程，要么明确列为特殊例外或范围外。
7. **最后再映射 Excel**：只有明确了规则含义，才把 Excel 的列映射为当前状态、经历、检查结果或办理状态。

高价值搜索词可以分组使用：

- 进入与保持：`进入条件`、`资格获取`、`资格保持`、`过期后的办理`；
- 经历与位置：`航线运行经历`、`左座`、`右座`、`PF`、`PM`、`昼间`、`夜航`、`航段`、`起落`、`人工飞行`；
- 检查与失败：`熟练检查`、`航线技术检查`、`报务检查`、`补充训练`、`增加经历`、`不合格`、`再次检查`、`降为`、`停飞`；
- 生效动作：`签注`、`聘任`、`批准`、`发布`、`入库`、`不得参加运行`、`恢复相应运行资格`；
- 关键例外：`PC-CP`、`复飞`、`操作驾驶员`、`辅助操作驾驶员`、`有意留空`。

搜索结果只能当定位线索。命中一个数字或一句话，不代表已经理解了整条规则。

## 7. Excel 与手册的学习顺序

建议按“先会用、再理解、再自动化”的顺序推进：

### 第一步：把培训环节做成可复盘流程

每天或每轮培训结束后，能回答：哪些人要排、为什么要排、排了什么、实际完成了什么、哪些日期更新了、哪些结果仍待人工确认。

### 第二步：建立字段字典

对每个关键 Excel 列写清楚：字段名、人员对象、数据类型、来源、更新时间、是否能参与有效期计算、异常时回哪里核对。字段字典应链接到手册章节或教程记录，不能只写“看起来像”。

### 第三步：按事件链学习技术等级和检查

先读训练大纲中转机型、等级训练、教员考试员和重新获得资格的章节，再读技术管理手册中等级、检查、聘任、执照、资质发布和档案章节。每读完一类，就用一个具体人员状态反推：进入条件是什么、当前缺什么、完成后谁批准、系统何时生效。

### 第四步：用菜鸟教程做快速检索

教程用于理解概念和跳到唯一的恢复详情；遇到有争议的等级关系、检查结果或签注时点，立即回手册，不在教程页面上继续猜。

### 第五步：再接 API

先选一个只读查询闭环（例如某人某日期范围的飞行经历和起落数），把输入、接口响应、人工核对结果和 Excel 写入结果全部留痕，再推广到批量查询。

## 8. 接口化和整合路线

目标不是“做一个更大的 Excel”，而是建立一条可审计的数据链：

```text
接口/页面原始数据
  -> 标准化人员和日期
  -> 与 Excel 人员、训练、检查记录匹配
  -> 按已确认手册规则计算状态
  -> 输出待处理清单、更新预览和证据
  -> 人工复核后导出 Excel 或提交既有流程
```

推荐分阶段实施：

1. **只读查询层**：复用 `$API_PROJECT_DIR` 已有登录、查询和分页能力，先实现稳定的单人查询和批量查询，不写生产数据。
2. **标准化层**：统一员工号、姓名、日期、机型、座位、PF/PM、昼/夜、航段、起落等字段；空值、重复、接口失败都要显式标记。
3. **规则层**：把手册核对后的规则单独维护，输入是事实数据，输出是“有效、即将到期、已失效、需补训、需检查、待审批”等状态和证据。
4. **整合层**：把接口事实、Excel 当前状态、培训安排和教程/手册来源合并展示；任何无法唯一匹配的人员或记录进入人工复核队列。
5. **输出层**：生成培训排班清单、有效期更新预览、飞行经历核对表和 Excel 副本；不覆盖原始文件，不绕过现有审批和发布流程。

第一版数据对象至少要保留：人员、培训记录、检查/考试结果、资质、飞行经历、来源证据、查询时间、规则版本和人工复核状态。

## 9. 每轮工作的最小检查清单

### 排培训前

- 当前有效期和最晚完成日期是否来自正确的人员记录；
- 这项培训是否确实对应这个资质；
- 人员是否属于不飞、教员或其他特判名单；
- 是否已有能覆盖本轮到期的安排；
- 安保和 TSA 是否分别留有记录。

### 培训完成后

- 实际完成日期、人员名单和培训结果是否核对；
- 取消、未录入、机器看等字段是否按当前口径处理；
- 是否更新正确的项目 sheet 和人员信息有效期；
- 是否生成更新预览并抽查；
- 是否还有检查、签注、审批、发布或入库动作未完成。

### 遇到技术等级或检查问题时

- 不用字母等级或同事口述直接推导结论；
- 把航段、起落、飞行次数、PF/PM、昼/夜等字段逐项对齐原文；
- 读完不合格后的补训、增加经历、复查、降级和停飞分支；
- 对 PC-CP、Z/B/C/D 等高风险事项保留原文证据并向业务同事确认；
- 明确区分“事实已确认”和“当前仍待确认”。

## 10. 本文的维护规则

每次完成一轮手册探索、Excel 复核或接口试验后，只把能跨任务复用的经验沉淀到本文：稳定的资料入口、有效的读取方法、高风险关键词、容易混淆的字段和可重复的核对步骤。具体版本数字、人员名单、临时结论和完整手册原文仍放在对应的知识源、spec 或审计记录中，不把本文变成第二本手册。

当本文和现行手册、培训工作台 spec、菜鸟教程或 API 实际行为出现冲突时，以最新核实的原始手册和可复现数据为准，并同步修正文档中冲突部分。

## 参考入口

- 培训皇帝开发事实：`.agents/skills/training-workbench/SKILL.md`、`spec/dev/training-workbench/`
- 培训皇帝操作方法：`spec/user/training-workbench/manual.md`
- 菜鸟教程维护规范：`.agents/skills/beginner-tutorial-dev/SKILL.md`
- 菜鸟教程操作方法：`spec/user/beginner-tutorial/manual.md`
- 菜鸟教程知识源：`$TUTORIAL_DIR`
- 原始手册：`$MANUAL_DIR`
- 资质 Excel：`$QUALIFICATION_XLSX`
- 接口项目：`$API_PROJECT_DIR`，本次路径为 `C:\Users\Administrator\Desktop\repository\csn2api`
