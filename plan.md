# 换季学习动态均衡规则重建 Plan

## 1. 需求文档

用户要解决什么实际问题：换季学习当前只按带队机长、机长、副驾驶和美线带队几个固定维度生成初版，A1、A2、B、C、D、划转副驾驶以及不同机长、教员等级没有分别均衡；继续向固定分类算法追加条件会使规则难以扩展和解释。

谁会使用：负责导入换季名单、生成初版、在线移动人员并导出`换季实际`的业务人员。

用户完成任务时应该看到什么体验：导入空的`换季实际`后，页面列出按优先级排列的均衡 Hook，默认全部勾选；用户点击一次“均衡负载”，工具按启用 Hook 和完整技术等级生成稳定、可解释的初版。严格均衡不是排班成功的前提，算法返回当前规则下差异尽量小的结果；后续“均衡检查”只读。

当前范围包含：

- 将完整`技术信息`作为动态基础均衡组，不维护技术等级白名单。
- 建立默认启用的`美线带队`和`带队机长` Hook，并按优先级首个命中。
- 公司领导继续由独立过滤字典识别，只参与每期总人数均衡。
- 页面提供 Hook 复选框，默认全部勾选，并显示动态均衡组检查结果。
- 重建首次分配、均衡检查、状态类型、页面接线、规格和回归测试。
- 保留现有导入恢复、空日期待分配、移动、调整记录、健康检查和 Excel 导出行为。

当前范围不包含：

- 不新增代理机长等尚无明确来源字段和匹配口径的 Hook。
- 不恢复交换、撤销或自动重新洗牌。
- 不因重新导入、修改日期或点击均衡检查而自动调整人员。
- 不启动本地开发服务；前端视觉由 owner 人工验收。

业务上怎样算完成：每名运行人员按启用规则只进入一个有效均衡组；美线带队优先于带队机长，未命中 Hook 的人员按完整技术等级分组；取消 Hook 后人员向下一规则回落。生成结果不漏人、不重复、总人数和各有效组尽量平均；无法严格均衡时不报错阻断。公司领导只补入人数较少的期次。代码、测试、开发规格和用户手册描述一致。

## 2. 当前事实

已确认代码事实：

- `data.ts`读取完整`技术信息`，同时把人员压成`leader`、`captain`、`firstOfficer`三个固定`category`。
- `allocation.ts`通过固定`CATEGORY_ORDER`和美线带队流量配额求解三类人数，不能动态发现技术等级或 Hook。
- `logic.ts`按固定三类和美线标记生成初版并检查五个固定维度。
- `view.ts`的摘要、图表和期次卡片按三类岗位展示；三类展示仍适合名单阅读，但不应继续决定排班配额。
- `balance-filter.ts`已经把`身份 = 公司领导`集中为字段值过滤字典，没有公司领导专属人员类型或页面分组。
- `app-context.ts`尚未保存启用 Hook；`main.ts`的均衡按钮尚未传递规则选择。
- `换季实际`只有日期和期数同时有效时才恢复期次，日期为空时进入待分配。

已确认测试事实：

- 现有`logic.test.ts`只保护固定三类、美线标记和公司领导过滤，不覆盖同一岗位多个技术等级的分别均衡、Hook 优先级或取消后的回落。
- 上一轮完整验证为 61 个 Vitest 文件、245 项测试和 7 项 Python 测试通过。
- 当前测试通过不代表新规则已实现。

已确认文档/spec 事实：

- 现有 spec 仍规定首次生成和后续检查使用总人数、带队机长、机长、副驾驶、美线带队五个固定维度。
- 现有用户手册仍说明全部副驾驶作为一个整体检查。
- 代码、测试和文档都需要切换为动态有效均衡组口径。

已确认配置或入口事实：

- 工具页面脚本按`data -> health -> balance-filter -> allocation -> logic -> export -> app-context -> view -> main`加载。
- 新的 Hook 注册表必须在`allocation`和`logic`之前加载，并由页面与算法共用。

已确认命令输出：

- 最新本地样表`9.15-9.30换季学习_换季实际.xlsx`有 243 人和 14 种完整技术信息。
- 按当前公司领导过滤口径，样表有 13 种运行技术等级、22 种“技术等级/带队/美线/过滤”组合。
- 对最新样表做只读整数约束验证时，总人数、运行技术等级、带队机长和美线带队严格均衡可行；这只证明当前样本可行，不把严格可行作为生产排班前提。
- 当前工作区仅有 owner 删除的`public/tool/assets/翼起健康.webp`，与本任务无关，不能纳入提交。

当前已有能力：按员工号恢复和合并、待分配、日期解析、健康检查、身份标记、单人/批量移动、调整记录和副本导出均已存在。

当前缺口：固定分类同时承担展示分类和配额分类；规则无法从配置自动渲染；均衡检查无法列出技术等级；Hook 未进入页面状态；旧求解器无法复用到后续新增 Hook。

仍不明确：未来代理机长等 Hook 的字段和值尚未定义。本次只提供可扩展注册结构，不预设业务事实。

## 3. 失败测试

自动测试：

- 同时包含 A1、A2、B、C、D、划转副驾驶及不同机长、教员等级时，现有算法不能证明每种完整技术等级分别均衡。
- 同一人员同时命中美线带队和带队机长时，现有代码没有“首个启用 Hook 决定有效均衡组”的可观察结果。
- 取消美线带队或带队机长 Hook 后，现有代码不能让人员依次回落到下一 Hook 或完整技术等级。
- 现有均衡报告只有固定五维，不能报告动态有效均衡组的各期人数和差值。
- 现有页面状态不能把默认启用 Hook 传入首次生成和后续只读检查。

类型检查：动态规则、Hook 标识、有效均衡组和动态报告必须有明确参数与返回类型，不能通过新增隐式`any`掩盖旧模型残留。

构建命令：新增规则脚本必须被构建到`dist/tool/app/seasonal-learning/`并按正确顺序加载。

静态检查：本次相关页面不得出现单边强调色；规则控件和动态结果不得挤压或覆盖顶部操作、日期、图表和期次名单。

真实用户路径演练：使用最新 243 人样表读取真实技术等级，执行默认 Hook 的 6 期初版回放；再分别关闭美线带队和全部 Hook，确认有效组回落、人员不漏不重、结果稳定；不把真实 Excel 写入仓库。

失败判定：出现漏排、重复、无效期次、Hook 优先级错误、取消 Hook 不回落、公司领导占用运行配额、均衡检查改动人员、严格均衡不可达时抛错中止，或代码/测试/spec/手册任一仍使用旧固定配额事实，均视为失败。

## 4. 目标

用户路径完成到：导入名单后看到默认全选的 Hook，按需要取消后点击“均衡负载”；生成后 Hook 配置锁定，后续点击只读检查；移动和导出继续可用。

代码主链路接到：`规则注册表 -> 首个命中解析有效均衡组 -> 动态分组配额 -> 确定性人员分配 -> 同规则动态检查 -> 页面结果`。

状态和记录落到：启用 Hook ID 保存在当前页面状态；初版不产生人工调整记录；人工移动仍只记录移动事件。

输出呈现到：页面保留三类名单阅读结构和现有概览图，同时新增紧凑的 Hook 选择及动态均衡结果；人员继续显示完整技术信息、身份和美线标记。

测试和文档同步到：核心测试覆盖动态技术等级、Hook 优先级、取消回落、公司领导填充、只读检查、随机期数和真实样表回放；spec 和手册只描述新口径。

完成判定：专项测试、构建、类型检查、全量 Vitest、Python、BOM 和差异检查通过；真实样表回放结果闭合；只提交本任务文件并推送`master`。

## 5. 不做范围

不做：不新增未确认 Hook，不修改身份随机浅色逻辑，不修改健康检查业务口径，不新增服务端或持久化数据库。

不保留：不保留固定三类配额求解器和固定五维均衡报告作为兼容入口。

不兼容：不为旧的内部`SeasonalLearningAllocationQuotas.category/usLineLeader`结构提供适配层；页面展示`category`可以保留，但它不再参与初版配额。

不处理：不自动修正恢复自`换季实际`的已有安排，不把 Hook 选择写入来源 Excel，不提交真实样表或无关图片删除。

## 6. 设计

主链路：

Excel 总名单 -> 读取稳定人员字段 -> 公司领导过滤 -> 按优先级匹配启用 Hook -> 未命中者按完整技术信息形成基础组 -> 动态配额协调每组余数和期次总人数 -> 按原始顺序落人 -> 动态均衡报告 -> 页面摘要、检查结果和期次名单 -> 移动后只重新检查 -> 导出当前安排。

输入 -> 判断 -> 状态 -> 执行 -> 输出 -> 记录：

- 输入：人员、期数、页面勾选的 Hook ID。
- 判断：公司领导先退出运行分组；其余人员按`美线带队 -> 带队机长 -> 完整技术信息`首个命中形成唯一有效组。
- 状态：`enabledBalanceHookIds`由页面持有，首次生成后锁定；导入实际安排时使用默认全选规则进行只读检查。
- 执行：各有效组先取得`floor(人数/期数)`基础配额，余数依次放入当前总人数较少的期次；公司领导最后逐人放入总人数较少的期次。该过程不因某个维度无法达到理想差值而抛出业务错误。
- 输出：每人唯一期次、总人数报告、动态有效组报告、每期问题标签。
- 记录：初版不标人工调整；移动沿用既有逐人记录。

模块边界：

- Excel 解析和展示分类不决定均衡组。
- 规则注册只负责 Hook 元数据、优先级和匹配，不分配人员。
- 配额模块只处理已经解析出的互斥有效组和中性填充人员。
- 逻辑模块组织分组、调用配额、落人和检查，不渲染 DOM。
- 页面从同一规则注册表生成复选框，不复制 Hook 名称和优先级。

文件职责：

- `balance-filter.ts`：保留字段值过滤字典及公司领导运行过滤。
- 新增`balance-rules.ts`：维护 Hook 注册表、默认启用项和首个命中解析。
- `allocation.ts`：删除固定三类/美线流量模型，改为动态互斥组的确定性最佳努力分配。
- `logic.ts`：使用动态组生成初版和报告，保留移动边界。
- `runtime.d.ts`：定义 Hook、有效组、动态报告、状态和 API。
- `app-context.ts`、`main.ts`：保存、读取和锁定 Hook 选择。
- `view.ts`、`index.html`、`styles.css`：渲染紧凑规则选择和动态均衡结果；三类名单与图表只作阅读概览。
- `tests/tool/seasonal-learning/logic.test.ts`：保护动态核心规则；导出和健康检查测试只在公共类型变化影响时调整。
- `spec/dev/seasonal-learning/spec.md`、`spec/user/seasonal-learning/manual.md`：替换旧五维首次均衡事实和操作说明。

状态归属：Hook 定义属于静态规则注册表；启用 Hook ID 属于当前页面状态；人员有效均衡组按当前规则即时派生，不写回 Excel，也不永久塞入人员模型。

数据或事件流：导入后 View 读取规则表显示默认选择；复选框变化更新状态；首次按钮把状态传给 Logic；Logic 调用 Rules 和 Allocation；生成后 View 锁定复选框并显示报告；移动后仍使用同一状态检查。

错误、恢复、中断、重试边界：缺失员工号、姓名、技术信息或无效期数仍属于输入错误；均衡差异不是错误。生成过程是纯内存确定性计算，不需要重试。导入实际安排和重导入继续遵循现有权威来源规则。

与现有架构的关系：保留静态浏览器工具和独立脚本模式，不引入框架或外部求解器。互斥有效组使动态配额可以用小型确定性算法完成，避免为当前规模引入整数规划运行时。固定`category`只保留给三栏名单和概览图。

测试和文档影响：删除依赖固定配额结构的断言，新增业务可观察回归；不写“某段旧代码不得出现”类负向测试。文档只保留最终动态规则。

## 7. 实施任务

- [x] T001 动作：新增动态 Hook 注册与有效组解析失败测试；文件/模块：`tests/tool/seasonal-learning/logic.test.ts`；依赖：已确认优先级；验收：旧实现下测试因缺少 API 或行为不符而失败。
- [x] T002 动作：定义规则、动态报告和页面状态契约；文件/模块：`runtime.d.ts`、`balance-rules.ts`、`index.html`；依赖：T001；验收：Hook 默认值、优先级和首个命中只有一个事实源。
- [x] T003 动作：用动态互斥组配额替换固定三类求解器；文件/模块：`allocation.ts`、`logic.ts`；依赖：T002；验收：人员不漏不重，总人数和各有效组尽量平均，公司领导只作总人数填充，任何可分配名单不因差异抛错。
- [x] T004 动作：接通 Hook 选择、锁定和动态检查结果；文件/模块：`app-context.ts`、`main.ts`、`view.ts`、`styles.css`；依赖：T002、T003；验收：默认全选、可取消、生成后锁定、检查只读，页面无单边强调色。
- [x] T005 动作：更新旧测试并补随机回放；文件/模块：`tests/tool/seasonal-learning/logic.test.ts`及受影响测试；依赖：T003、T004；验收：动态等级、优先级、回落、过滤、只读和 1 至 30 期闭合。
- [x] T006 动作：同步最终业务事实和操作说明；文件/模块：`spec/dev/seasonal-learning/spec.md`、`spec/user/seasonal-learning/manual.md`；依赖：T003、T004；验收：不再描述固定三类配额和固定五维检查。
- [x] T007 动作：真实样表回放和完整验证；文件/模块：构建产物及测试；依赖：T001-T006；验收：专项、真实样表、构建、类型、全量测试、BOM、diff 检查通过。
- [~] T008 动作：收口、限定暂存并推送；文件/模块：本计划和本任务相关文件；依赖：T007；验收：不包含真实 Excel和`翼起健康.webp`删除，`HEAD`与`origin/master`一致。

## 8. 验证计划

相关局部测试：

- `npm.cmd run build`
- `npx.cmd vitest run tests/tool/seasonal-learning/logic.test.ts`
- `npx.cmd vitest run tests/tool/seasonal-learning`

完整验证命令：

- `npm.cmd run verify`
- `git diff --check`
- 仓库既有 BOM 检查随`verify`或构建验证；若未覆盖则单独扫描文本文件。

构建或安装检查：确认`balance-rules.js`和改造后的脚本共计正常构建到`dist`，页面脚本顺序满足运行依赖，不新增网络运行依赖。

页面/工具入口检查：静态核对 Hook 控件 ID、默认勾选、禁用状态、动态结果容器、移动弹窗和导出按钮接线；按 owner 要求不启动本地服务，视觉留给 owner 验收。

恢复或中断演练：导入空实际表生成初版；导入带实际安排恢复而不重排；日期为空进入待分配；重导入保留已有期次；移动后只读检查。

文档同步检查：搜索`五个维度`、固定三类首次均衡等旧事实，只保留作为页面阅读概览的三类统计，不保留为排班规则。

未验证内容：最终视觉观感和真实浏览器点击手感由 owner 人工验收；未来代理机长字段未定义，不做虚构验证。

剩余风险：自定义关闭 Hook 的选择不写入 Excel，重新打开导出工作簿时会恢复为默认全选；这是本次明确不扩展 Excel 结构的结果，需要在手册中说明。

## 9. 收口

目标是否完成：代码、页面接线、测试、规格和真实表回放目标已完成；提交推送尚在执行。

失败测试是否变绿：已变绿。动态 Hook、技术等级分组、公司领导填充、只读检查和 1 至 30 期组合回放均通过。

改了哪些文件：新增`balance-rules.ts`和本计划；重建`allocation.ts`、`logic.ts`及运行类型；接通页面状态、Hook 控件和动态结果；同步换季学习测试、开发规格和用户手册。

跑了哪些验证：专项换季测试 22 项通过；`npm.cmd run verify`通过，其中构建 178 个脚本、61 个 Vitest 文件 249 项、Python 7 项和 TypeScript 全部通过；BOM、DOM ID、旧模型残留、单边强调色、构建产物和`git diff --check`检查通过。最新 243 人真实表在默认 Hook、仅带队 Hook和仅技术等级三种模式下均为 243 个唯一员工号、0 人待分配、总人数`41/41/41/40/40/40`且结果确定。

哪些内容没有验证：未启动本地服务；最终视觉观感和真实点击手感按 owner 要求留给人工验收。未来代理机长字段尚未定义。

剩余风险：Hook 选择不写入 Excel，重新打开工作簿会恢复默认全选；页面已在用户手册说明。

是否 commit / push：尚未提交或推送，T008 正在执行。
