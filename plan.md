# 未来资质压力监控重构 Plan

## 1. 需求文档

资质管理员需要直观看见：按照人员当前有效期和培训表中现有安排，未来连续月份会有多少资质到期，峰值由哪些培训项目构成，以及具体涉及哪些人员。系统负责呈现事实和压力，不替用户生成缺少执行约束的逐人智能排班。

页面删除“智能排班方案”整块。保留并强化现有资质压力模块：主图固定显示连续 12 个月，通过图内滑块和滚轮向后查看完整 24、36 或 48 个月；主折线展示所选口径下每月总人项，并提供观察范围月均参考。点击月份后，用饼图展示该月各资质项目构成，并继续展示人员、安排日期、预测日期和覆盖状态明细。

业务完成标准：导入培训表后无需理解优化参数即可看见未来压力；图表初始不一次挤入全部月份；当前有效期和排班后预测继续使用统一规则事实；点击峰值月份能够从总体下钻到项目构成和人员明细。

## 2. 当前事实

已确认用户需求：

- 用户要解决的是提前发现未来到期峰值并自行安排，不需要系统给出逐人推荐月份。
- 用户明确同意删除智能排班模块，回到简洁的未来资质压力监控。
- 压力主图必须是固定可视窗口并能够滚动，不能一次展示全部观察月份。
- 总体趋势使用折线图；所选月份的具体资质构成使用饼图。

已确认代码和版本事实：

- `19fdf76` 是当前 `HEAD` 的祖先，该版本已具备“排班后资质压力”和“实际培训负载”，没有智能排班模块。
- `19fdf76..HEAD` 的 8 个提交主要增加智能排班；当前差异包含 `smart-schedule.ts`、`smart-schedule-optimizer.ts`、相关测试和 `yalps` 依赖，合计约新增 2550 行。
- `qualification-pressure.ts` 已复用排班总览、项目 sheet、模拟排班、有效记录、取消备注、培训规则和特判忽略清单，输出每月当前总量、预测总量及按项目构成。
- 当前资质压力图使用总量折线、观察范围月均参考和 ECharts `dataZoom`；默认可见窗口锁定连续 12 个月，点击月份后有项目构成饼图。
- 当前月份明细已经展示当前最晚完成、安排日期、预测最晚完成、覆盖状态、提前天数和判断说明。
- 当前工作区包含本轮删除智能排班、收敛压力视图、文档同步和测试补充的未提交改动。

已确认实现边界：

- 已推送的历史不使用 `reset --hard` 或强制推送改写；通过新的向前提交移除智能排班。
- 页面视觉效果按仓库约定由 owner 人工检查。

## 3. 失败测试

自动和静态检查覆盖：

- 删除前的页面、源码和依赖曾包含智能排班；本轮改动需要确认它们已全部移除。
- 压力模块已经接入资质月份构成饼图和固定滚动明细；需要由类型检查和构建验证接线完整。
- `qualification-pressure.test.ts` 必须继续确认月份总量等于各项目构成之和，保证主折线、饼图和人员明细使用同一份事实。
- 真实表回放必须继续识别 2026 年 10 月当前有效期压力 71 人项、其中 TSA 46 人项，并确认排班后预测由已覆盖安排正确移动压力。

页面检查：

- 主图固定高度，默认只显示连续 12 个月；观察范围仍可选择 24、36 或 48 个月，并通过滑块或图内滚动查看后续月份。
- 点击月份时显示该月项目构成饼图和人员明细；未选择月份时不展示空饼图。
- 页面不再出现智能方案、人工方案、均衡方案、推荐月份、安全提前量或避让月份。

## 4. 目标

用户路径完成到：

- 导入培训表后，直接查看未来 12 个月压力趋势。
- 需要更远时间时滚动主图，而不是把 24 至 48 个月压缩在同一视口。
- 切换当前有效期或排班后预测，查看同一批人员在现有安排执行前后的到期压力。
- 点击某个月，查看该月各资质占比和具体人员。

代码主链路收敛为：

- 有效期与现有安排 -> 统一覆盖判断 -> 月度当前/预测压力 -> 总体折线 -> 选中月项目饼图与人员明细。

## 5. 不做范围

- 不生成逐人推荐月份，不求解负载均衡方案。
- 不保留“我的方案”“均衡方案”或智能排班旧入口。
- 不估算未来具体培训日、期次、教员、场地、合班人数和人员冲突。
- 不把实际培训负载与资质到期人项叠加在同一坐标轴。
- 不修改有效期、窗口、忽略人员、覆盖状态或模拟排班业务规则。
- 不改写已推送 Git 历史。

## 6. 设计

压力主图：

- 当前有效期模式使用 `currentTotal`；排班后预测模式使用 `forecastTotal`。
- 资质项目筛选为空时显示全部项目总人项；选择单项目时显示该项目人项。
- 主系列使用带数据点的折线图，月份是离散统计周期。
- 月均参考使用当前观察范围内主系列总量除以月份数，仅作为视觉参考，不命名为推荐容量。
- `dataZoom` 固定初始显示 12 个月；观察范围超过 12 个月时使用底部滑块和图内滚动查看后续月份。

月份构成：

- 点击主图月份后，从同一 `QualificationPressureMonthRow` 的 `currentByProject` 或 `forecastByProject` 生成饼图。
- 饼图只表达选中月份的项目构成，不汇总整个观察范围。
- 选中单项目时饼图只显示该项目，人员明细继续只显示该项目。
- 选中月份无数据时显示明确空状态，不伪造切片。

人员明细：

- 继续复用当前/预测/两者模式的月份筛选逻辑和现有字段。
- 构成饼图与明细放在同一月份下钻区域；表格保留独立滚动边界。

移除范围：

- 删除智能任务、优化器和智能排班测试。
- 删除页面控件、图表、状态、元素、事件和渲染接线。
- 删除 `yalps` 依赖及锁文件记录。
- 删除 spec、用户手册和 skill 中智能排班事实，只保留未来资质压力口径。

模块职责：

- `qualification-pressure.ts`：保持统一当前/预测压力业务模型。
- `app-charts.ts`：渲染固定窗口趋势图和选中月份构成饼图。
- `app-training-capacity.ts`：选择月份、渲染人员明细并协调两个图表。
- `index.html`、`dashboard.css`、`responsive.css`：压力区结构和稳定滚动布局。

## 7. 实施任务

- [x] T001 重写计划，固化删除智能排班、固定 12 月窗口、总体折线和月份构成饼图方案。
- [x] T002 删除智能排班模块、页面接线、测试和 `yalps` 依赖。
- [x] T003 将资质压力主图改为总量折线、月均参考和固定 12 月滚动窗口。
- [x] T004 新增选中月份资质构成饼图并与现有人员明细联动。
- [x] T005 同步开发 spec、模拟排班 spec、用户手册和 training-workbench skill。
- [x] T006 补充压力总量与项目构成一致性测试，并使用真实培训表回放。
- [x] T007 运行专项测试、构建、类型检查、全量测试、确定性验证、依赖审计和静态检查。
- [x] T008 确认暂存范围，使用简体中文提交并推送。

## 8. 验证计划

局部验证：

- `npx.cmd vitest run tests/tool/training-workbench/qualification-pressure.test.ts tests/tool/training-workbench/training-load.test.ts`
- 培训工作台构建和相关状态测试。

完整验证：

- `npm.cmd run build`
- `npm.cmd run typecheck`
- `npm.cmd test`
- `npm.cmd run verify`
- `npm.cmd audit --omit=dev`

静态检查：

- `git diff --check`
- 检查修改文件 UTF-8 无 BOM。
- 搜索智能排班页面、源码、测试、文档和 `yalps` 残留。
- 确认真实 Excel、临时回放脚本和构建临时文件未进入提交。

页面检查：

- 自动验证元素、事件、构建资源和类型接线完整；最终视觉效果由 owner 人工检查。
- 主图固定高度和 12 月初始窗口；滑块、月份标签、饼图、表格在常见宽度下不重叠。

## 9. 收口

目标是否完成：已完成。

失败测试是否变绿：已变绿；专项 4/4、全量单元 263/263、Python 9/9 通过。

改了哪些文件：删除智能排班模块及依赖，收敛资质压力为总量折线与固定 12 个月窗口，新增月份构成饼图和固定滚动明细，同步代码、测试、spec、用户手册和 skill。

跑了哪些验证：`npm.cmd run build`、`npm.cmd run typecheck`、`npm.cmd test`、`npm.cmd run verify:deterministic`、`npm.cmd audit --omit=dev`、`git diff --check`；真实培训表回放确认 411 人、2026-10 当前压力 71 人项、TSA 46 人项、总量构成无差异。

哪些内容没有验证：页面最终视觉效果仍需 owner 在浏览器中人工确认，尤其是主图滑块/滚轮和明细表纵向滚动体验。

剩余风险：页面最终视觉由 owner 人工确认；ECharts 在不同浏览器上的滚轮灵敏度未做自动化覆盖。

是否 commit / push：已完成；功能提交 `497daaf` 已推送到 `master`，本次计划收口记录随后一并推送。
