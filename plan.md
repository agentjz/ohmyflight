# watchdog 全量更名 Plan

## 1. 需求文档

将当前项目断裂式全量更名为纯小写 `watchdog`。所有当前页面、源码、配置、内部 ID、下载文件、文档、spec、测试、skill、GitHub/Pages 链接和 Git 远端统一使用新名称，不保留旧名称兼容，不添加中文副标题或额外品牌文案。Git 历史提交说明保持原样，本地工作区目录名不处理。

## 2. 当前事实

已确认本地 `master` 与 `origin/master` 同步，HEAD 为 `7238818`，工作区在实施前干净。旧品牌内容命中 42 个已跟踪文件，旧品牌路径命中 1 个通用开发 skill 目录。另有 24 个项目自有文件使用旧项目缩写作为全局 CSS token 或内部样式引用前缀；第三方压缩库中的相同字母组合不属于项目命名。主题存储键和事件、全局对象、项目归档格式、构建变量、下载文件名、包名、页面链接、维护 skill 和现有契约测试均使用当前品牌，属于跨模块断裂式更名。仓库不维护真实浏览器测试。GitHub 仓库是否已由 owner 改名为新名称仍需只读核对。

## 3. 失败测试

当前文件内容扫描、已跟踪路径扫描和项目自有旧缩写前缀扫描必须由有命中变为零命中。主题、归档和构建契约若只修改一侧，类型检查、核心测试或确定性构建应失败。`dist/version.json` 原样导出的 Git 历史提交说明不参与当前品牌残留判定，不过滤或改写历史。第三方压缩库不做机械替换。仓库级验证不得启动 Playwright 或真实浏览器。

## 4. 目标

页面文案和链接、包名、内部业务 ID、浏览器主题契约、全局 CSS token、归档格式、构建变量、临时目录前缀、下载文件名、源码、测试、文档、spec 与维护 skill 全部统一为 `watchdog`。GitHub 仓库和 Git 远端使用新地址。构建、类型检查、全量测试、确定性构建、diff 检查、BOM 检查及残留扫描通过，最终提交推送且工作区干净。

## 5. 不做范围

不新增“看门狗”或其他副标题、Logo、宣传文案、视觉改造和业务功能。不兼容旧主题设置、旧项目归档格式、旧事件、旧存储键或旧环境变量。不重写 Git 历史，不处理本地工作区目录名，不新增表面化字符串测试或真实浏览器测试。

## 6. 设计

主链路：盘点当前品牌和旧项目缩写 -> 机械替换三种大小写形式及项目自有缩写前缀 -> 重命名通用开发 skill -> 复核主题、全局 CSS token、归档、构建、类型和调用方 -> 验证当前内容与路径零残留 -> 核对并更新 GitHub/Pages 地址和 Git 远端 -> 提交推送。

显示文案留在页面和文档；浏览器契约留在主题模块和公共类型；归档格式留在项目归档模块；构建变量和临时目录留在构建脚本；维护入口留在 `.agents/skills/`。仅替换品牌及其派生标识，不改变模块职责和业务流程。失败时由 Git diff 审核和现有测试定位，不增加兼容分支；外部仓库无法改名时如实记录外部阻塞。

## 7. 实施任务

- [x] T001 统一替换当前内容中的品牌大小写形式和项目自有旧缩写前缀，并重命名通用开发 skill 目录；第三方库保持原样。
- [x] T002 同步全部 skill 引用，复核主题、全局 CSS token、归档、构建、下载与公共类型契约；新 skill 已通过官方校验。
- [ ] T003 运行 skill 校验、完整工程验证、BOM/diff 检查和当前内容及路径残留扫描。
- [ ] T004 核对 GitHub 仓库、Pages 地址和 Git 远端，完成提交推送并确认远端同步。

## 8. 验证计划

运行 `npm.cmd run build`、`npm.cmd run typecheck`、`npm.cmd test`、`npm.cmd run verify` 和 `git diff --check`。使用官方 skill 快速校验器检查 `watchdog-dev`。扫描当前工作区内容、已跟踪路径、项目自有旧缩写前缀和构建产物；构建产物只允许 `version.json` 中由 Git 历史原样导出的既有记录，第三方压缩库保持原样。页面视觉由 owner 人工检查，不启动真实浏览器。

## 9. 收口

任务完成后记录变更、验证、历史边界、外部仓库状态、提交和推送事实。
