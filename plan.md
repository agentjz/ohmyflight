# 工具首页新拟物重构 Plan

## 1. 需求文档

工具首页需要复刻 owner 指定的 StyleKit Neumorphism 视觉语言，在保持搜索、分类、公告和工具入口完整的前提下，形成柔和、现代、清晰的同色立体界面。完成后按 owner 最终批准提交并推送。

## 2. 当前事实

- `public/tool/index.html`、`public/tool/index.css` 和 `src/tool/tools-render.ts` 分别负责首页结构、样式与交互渲染。
- 首页当前展示 20 个工具，分类为全部、重型、轻型、自动化；数据源和入口 URL 无需修改。
- owner 提供的模板使用 `#e0e5ec` 背景、左上亮/右下暗双阴影、凸起按钮、内凹输入与激活态、大圆角和低饱和文字。
- 模板提示词存在互相矛盾的黑白粗边框片段；以最终禁止项和实际 `app/page.tsx` 为准，不使用黑白粗边框、渐变或位移式动效。
- 现有开发规格、用户手册和首页 smoke test 描述旧窄栏头像文章列表，需要同步。

## 3. 失败测试

- 当前视觉无法体现新拟物的凸起、内凹与交互状态。
- 当前 smoke test 固定要求每个工具重复吉祥物头像，与新结构不一致。
- 失败判定：入口或筛选行为回归、阴影光源方向不统一、使用纯黑白背景/渐变/粗边框/单边强调条、桌面或移动端文字溢出。

## 4. 目标

- 页面统一使用浅灰同色底和双向阴影，光源方向始终为左上亮、右下暗；首页不保留吉祥物。
- 搜索框和当前分类为内凹状态；导航与工具入口为凸起状态；所有交互有 hover、active、focus 和 reduced-motion 反馈。
- 首页首屏直接提供品牌、搜索、分类与工具入口，不做营销落地页。
- 品牌名只保留在顶栏，不在主内容区增加宣传标题或说明文案。
- 每张工具卡只显示标题、简介和只读开关状态；桌面五栏，窄屏依次降为四栏、三栏、双栏和单栏。
- spec、manual 和稳定入口测试同步当前事实。

## 5. 不做范围

- 不修改工具数据、分类业务规则、入口 URL、公告配置或门禁逻辑。
- 不引入 Next.js、React、Tailwind、远程字体或运行时网络依赖。
- 不复制模板业务内容，不下载第三方品牌素材。

## 6. 设计

主链路：工具数据 -> 搜索/分类过滤 -> 结果数量 -> 新拟物工具索引。`homepageState` 同时表达已启用、Beta 测试、维护中和已关闭；维护中与已关闭项仍显示但不可点击，固定 URL 不受影响，不维护独立白名单。

模块边界：HTML 定义语义结构；CSS 复刻新拟物 token 和响应式；TypeScript 只管理过滤、计数和条目输出。

视觉状态：页面和组件同为 `#e0e5ec`；凸起使用双外阴影；输入、分类槽和按下态使用双内阴影；强调色只用于文字、焦点和少量类别识别。

测试影响：保护工具数据、分类、搜索入口与渲染契约，不测试具体像素和内部旧实现是否存在。

## 7. 实施任务

- [x] T001 重排首页品牌、导航、搜索、分类和工具区结构。
- [x] T002 重写首页新拟物 token、组件状态和响应式样式。
- [x] T003 更新工具条目、结果数量和首页状态渲染。
- [x] T004 同步首页 spec、manual 和 smoke test。
- [x] T005 构建、类型检查、全量测试并检查桌面/移动截图。

## 8. 验证计划

- 局部测试：`npx.cmd vitest run tests/smoke/tool-index-data.test.ts`。
- 完整验证：`npm.cmd run build`、`npm.cmd run typecheck`、`npm.cmd test`。
- 静态检查：检查渐变、粗边框、单边强调条、纯黑白背景和非双向阴影。
- 页面检查：1440x1000、1024x768、390x844 三个视口下检查搜索、分类、工具条目、空状态和文字溢出。
- 最终审美由 owner 人工审核。

## 9. 收口

目标是否完成：已完成。首页已按指定新拟物风格重构，保留顶栏和默认关闭的锁屏，工具区为桌面五栏。

失败测试是否变绿：已通过首页局部测试和全量测试。

改了哪些文件：首页 HTML/CSS、工具数据与渲染、工具类型、首页与站点可见性规格/手册、首页 smoke test 和本计划。

跑了哪些验证：`npm.cmd run build`、`npm.cmd run typecheck`、`npm.cmd test`，并检查桌面与窄屏本地静态截图。

哪些内容没有验证：最终生产浏览器的审美仍以 owner 实机查看为准。

剩余风险：无已知功能风险；新拟物的阴影观感会受显示器亮度影响。

是否 commit / push：owner 已批准，待本次收口提交并推送。
