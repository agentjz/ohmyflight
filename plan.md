# 智能排班全年负载均衡重构 Plan

## 1. 需求文档

培训排班负责人需要从一份培训总表生成年度智能排班底稿。用户只需要理解两条规则：第一，所有可安排资质任务不得晚于当前轮次最晚完成日期；第二，在满足第一条后，让全年月度培训人天尽量均衡，避免某个月集中爆发。

用户只需要理解“月度培训负载（人天）”：页面用同一张年度图展示当前排班、独立计算的均衡方案和月均参考线，帮助用户判断自己的排班是否存在可消除的月度高峰。智能方案按资质事实独立计算，不以未来人工排班月份作为推荐依据，也不逐人评价人工方案。

当前范围包含 8 个具备现行规则的资质项目：应急训练、危险品、航空安保、TSA、疲劳管理、飞行作风、英语能力和汉语能力。CRM 及其他不参与资质优化的培训负载作为月度固定底座。

业务上完成必须同时满足：所有推荐月份合法且不逾期；不存在仍可把任务从高负载月合法移动到低负载月、却未改善全年峰值或平均偏差的方案；项目筛选不改变全局推荐结果；已经发生的培训不进入推荐明细，也不产生修改建议。

## 2. 当前事实

已确认代码事实：

- `smart-schedule.ts` 复用 `qualification-pressure.ts` 和 `rule-engine.ts` 生成当前轮次任务，没有复制有效期计算核心。
- 最新日期类候选月从排班年度首个可排月延伸到最晚完成月；窗口类继续使用现行规则窗口和锚点。
- `smart-schedule-optimizer.ts` 按同项目、同人天和同合法月份分组；连续模型依次求解全年总峰值、全年平均偏差、各项目月度峰值之和和项目内部平均偏差，每个阶段锁定前序最优上限。
- 整数化后的合法单任务移动使用同一严格顺序；项目内部均衡只能在资质合法、全年峰值和全年平均偏差都不变时决胜。
- 全部项目只生成一套年度方案；项目筛选通过 `buildView` 投影，不重新求解。
- 已发生且已覆盖的培训先滚动有效期；下一轮仍在排班年度到期时继续生成推荐任务，否则只作为历史固定负载。
- 未来人工安排只进入当前排班负载和可替换负载识别，不参与智能推荐月份偏好。
- 页面只保留排班年份和项目筛选，同图显示`当前排班`、`均衡方案`与`月均参考`，明细不评价人工方案。

已确认测试事实：

- `tests/tool/training-workbench/smart-schedule.test.ts` 的 13 个用例全部通过，覆盖集中到期分散、窗口合法范围、固定底座填谷、两层严格优先级、项目内部均衡、未来人工安排独立性、历史负载与下一轮滚动、无法安排、项目投影、人天估算和月均参考。
- `npm.cmd run verify` 已通过，包含类型检查、构建、全量前端与业务测试、Python 测试和确定性构建；本次最终执行耗时约 25 秒。

已确认文档/spec 事实：

- 开发 spec、用户手册和 training-workbench skill 已统一使用`智能排班方案`及全年负载均衡口径。
- 培训规则规范确认：窗口类只能在规则窗口内保持原锚点；最新日期类没有保护窗口，按培训开始日期重建有效期。
- 实际培训负载以人天为主要资源口径；同一人员同一自然日去重，多日培训逐日计人天。

已确认配置或入口事实：

- 培训工作台通过 esbuild 打包为浏览器 ESM，允许把 TypeScript 依赖打入独立工具产物。
- 仓库已锁定 `yalps@0.6.4`；它是 MIT 许可、带 TypeScript 类型和浏览器入口的轻量线性规划求解器，可打入本地浏览器产物。

已确认命令输出：

- 固定观察日 `2026-08-14`、排班年 `2027` 回放真实培训表时，识别 411 人、推荐 1732 人项、无法安排 0 人项、优化变量 647 个，四阶段优化约 516 ms。
- 真实表均衡方案全年月均 166.08 人天，月度为 168、168、167、168、168、168、167、167、166、166、166、154；TSA 原 5 月 133 人项峰值被拆分为 1 至 8 月的 28、46、47、47、46、47、31、23 人项。

当前已有能力：

- 能从培训表识别 8 个资质项目当前轮次、现有覆盖安排、忽略人员、合法窗口和项目常见培训天数。
- 能统计当前总表及模拟排班的月度实际培训人天。
- 能按月份点击图表并在固定高度滚动区展示人员项目。

当前缺口：

- 页面最终视觉效果按仓库约定由 owner 人工检查。

仍不明确：

- 具体培训日期、教员、场地、班级容量、合班人数和人员冲突没有输入数据，本次明确不进入求解。
- 月度推荐只确定月份；选择到期月份时，实际落日仍需人工保证不晚于明细中的最晚完成日期。

## 3. 失败测试

自动测试：

- 同一最新日期项目的大批任务集中在同一到期月时，候选月份必须覆盖排班年度开始月至最晚完成月，推荐结果必须分散到多个合法月份，而不是整体平移。
- 没有固定底座且所有任务可在全年任意月安排时，各月推荐人天最大值与最小值之差不得超过单个不可拆任务的人天。
- 存在固定高低负载时，优化器必须优先填补低负载月份，并取得不高于逐项贪心方案的最高月负载。
- 当降低项目峰值会抬高全年总峰值时，必须保持全年总峰值最优；低优先级目标不得牺牲任何已锁定的高优先级结果。
- 所有推荐任务必须恰好安排一次，推荐日期不得早于合法窗口或晚于最晚完成日期；已无合法月份的任务必须单独标记为无法安排。
- 两份资质事实相同、仅未来人工安排月份不同的输入必须生成相同智能推荐月份。
- 已发生且已覆盖的任务不得进入推荐明细，历史人天仍保留在全年当前负载和均衡方案底座中。
- 先求解全部项目再选择单个项目时，人员推荐月份必须与全局方案一致，筛选不得重新排程。
- 月度结果必须同时提供当前排班人天、均衡方案人天和基于全年总人天计算的月均参考值。

类型检查：

- 新增稳定业务函数必须有明确参数和返回类型；求解器模型和结果边界不得扩散隐式 `any`。

构建命令：

- 浏览器 ESM 构建必须能打包所选求解器，不依赖 CDN、网络或运行时服务。

静态检查：

- 页面不再出现固定提前月输入、`建议调整`、逐人方案差异或旧的智能排班对比说明。
- 推荐明细继续使用固定高度纵向滚动壳，表格铺满容器且不要求横向滚动。

真实用户路径演练：

- 使用 `C:\Users\Administrator\Desktop\南货航2026年培训表.xlsx` 本地回放，确认 TSA 等集中项目被分散，所有推荐仍在合法月份内，并记录求解耗时和月度分布；真实文件不提交。

失败判定：

- 出现逾期推荐、漏排或重复排任务、项目筛选改变推荐、人工未来月份改变理想方案、可消除的单月集中峰值、页面仍要求填写提前月数，任一情况均判定失败。

## 4. 目标

用户路径完成到：

- 导入培训总表后选择排班年份，在一张图中看到当前排班、均衡方案和月均参考线；点击月份查看该月智能推荐人员项目。

代码主链路接到：

- 统一规则生成合法月份 -> 任务分组 -> 连续负载优化 -> 人数整数化与合法局部移动 -> 人员映射 -> 月度方案投影 -> 页面展示。

状态和记录落到：

- 应用状态只保存当前年份的一套全局智能方案和当前展示项目；项目筛选只读取已有方案。

输出呈现到：

- 图表表达月度人天是否接近平均；明细表达建议谁在哪个月完成什么项目及最晚完成日期，不评价人工排班。

测试和文档同步到：

- 核心算法测试、培训工作台开发 spec、用户手册和页面文字全部替换为新口径。

完成判定：

- 局部测试、真实表回放、构建、类型检查、全量测试和确定性验证通过，页面入口与滚动明细静态检查通过。

## 5. 不做范围

不做：

- 不排具体培训日，不分配教员、场地或班级，不判断人员航班和休假冲突。
- 不设置人工月容量上限；当前唯一资源尺度是培训表推导出的月度培训人天。
- 不优化 CRM 的参加人员和月份，只把其现有负载保留为固定底座。

不保留：

- 不保留最新日期类固定提前月数、紧急降级文案和相关页面控件。
- 不保留`已排`、`待排`、`建议调整`等人工方案评价状态。

不兼容：

- 不为旧的智能排班结果类型、旧选项字段或旧页面元素保留兼容层。

不处理：

- 不声称月度数字必然完全相等；规则窗口、已经发生的负载和不可拆的人天允许形成不可避免的差异。

## 6. 设计

主链路：

培训表 -> 复用有效期与覆盖规则生成当前轮次 -> 排除已发生且已覆盖任务 -> 生成每个人项的合法月份 -> 按项目、人天和候选月份分组 -> 全年一次性求解 -> 把分组数量确定性映射回人员 -> 叠加历史及非优化固定负载 -> 输出全局方案和展示投影。

输入 - 判断 - 状态 - 执行 - 输出 - 记录：

- 输入：分析结果、排班年份、观察日、模拟排班、全年当前实际负载。
- 判断：窗口类取现行规则窗口与排班年度/观察日的交集；最新日期类仅处理最晚完成日在排班年度内的当前轮次，候选月从本年度首个可排月延伸到最晚完成月。
- 状态：推荐任务只有`可安排`或`无法安排`业务结果；当前人工月份只用于从实际负载中识别可替换部分，不参与推荐目标或择月偏好。
- 执行：使用 `yalps` 做四阶段连续线性规划，依次最小化全年总峰值、全年平均偏差、各项目月度峰值之和和项目内部平均偏差；每一阶段把最优值写成下一阶段不可突破的上限，并只在进入该阶段时加入所需约束。连续解按最大余数法整数化，再持续执行使用同一严格顺序的合法单任务移动。
- 输出：一套全项目全局方案、每月当前人天、每月均衡人天、月均参考值、项目列表和推荐人员明细。
- 记录：工具只读，不写回 Excel；状态只存在当前浏览器页面。

模块边界：

- `rule-engine.ts` 和 `qualification-pressure.ts` 继续作为日期规则与当前轮次事实来源。
- `smart-schedule.ts` 负责候选月份、任务分组、全局优化、人员映射和方案投影，不承载页面 DOM。
- `training-load.ts` 继续提供当前实际人天；智能排班只消费其月度结果。
- `app-training-capacity.ts` 负责构建一次全局方案、切换展示项目和渲染推荐明细。
- `app-charts.ts` 只负责同图展示当前排班、均衡方案和月均参考线。

文件职责：

- 若 `smart-schedule.ts` 在重构后同时混入候选规则与求解器模型且职责无法一句话说明，则拆出 `smart-schedule-optimizer.ts`；否则保持一个业务模块，避免无意义分层。
- `models.ts`、`app-elements.ts`、`app-controls.ts` 和 `app.ts` 删除提前月元素与事件接线，增加全局方案/投影所需的明确类型。
- `index.html` 和 `dashboard.css` 删除差异心智，保留紧凑控制区、年度图和固定高度推荐明细。

状态归属：

- 排班年份变化、重新导入或模拟排班变化时重新生成全局方案。
- 项目筛选只更新展示投影和图表，不调用优化器生成另一套方案。

数据或事件流：

- 当前排班系列读取实际培训负载。
- 均衡方案系列保留已经发生和不参与优化的固定负载，用智能推荐替换尚未发生的 8 类当前轮次负载。
- 单项目投影只展示该项目的当前人天和已求解推荐人天；人员推荐月份始终来自全项目方案。

错误、恢复、中断、重试边界：

- 求解器返回非最优或不可行时不得伪造推荐；页面显示无法生成方案，测试和真实表回放必须暴露该情况。
- 无合法月份的人项不进入求解器，作为无法安排明细返回。
- 重新导入文件按现有流程清空旧状态并重新计算。

与现有架构的关系：

- 保留统一有效期、忽略清单、培训记录有效性和实际负载核心；断裂式替换智能择月算法、结果模型、页面文案和交互，不保留旧兼容字段。

测试和文档影响：

- 重写 `smart-schedule.test.ts` 的固定提前月和差异状态用例，增加全局均衡、方案独立性与投影用例。
- 同步 `spec/dev/training-workbench/spec.md`、`spec/user/training-workbench/manual.md` 和 training-workbench skill 的模块边界描述。

## 7. 实施任务

- [x] T001 动作：先用失败测试定义全年削峰、平均偏差、合法月份、人工方案独立性和筛选投影；文件/模块：`tests/tool/training-workbench/smart-schedule.test.ts`；依赖：无；验收：8 个新用例在旧实现上全部失败，失败原因为旧模块没有全局方案与投影接口。
- [x] T002 动作：安装轻量线性规划依赖并实现分组负载优化器；文件/模块：`package.json`、`package-lock.json`、`smart-schedule.ts`、`smart-schedule-optimizer.ts`；依赖：T001；验收：连续模型分四阶段锁定前序最优值，整数化后按同一严格顺序局部改进；13 个核心测试确认所有可安排任务恰好分配一次，低优先级目标不能抬高总峰值或扩大全年平均偏差。
- [x] T003 动作：断裂式替换智能方案结果模型和项目投影，删除提前月、当前月份偏好和逐人差异状态；文件/模块：`smart-schedule.ts`、`models.ts`；依赖：T002；验收：未来人工安排月份不改变推荐，项目视图复用全局人员推荐月份。
- [x] T004 动作：重接页面控制、图表与推荐明细；文件/模块：`index.html`、`app-training-capacity.ts`、`app-charts.ts`、`app-elements.ts`、`app-controls.ts`、`app.ts`、相关 CSS；依赖：T003；验收：页面只有年份和项目筛选，同图展示当前/均衡/月均，明细无人工评价且内部纵向滚动。
- [x] T005 动作：同步开发事实、用户操作说明和 skill 模块边界；文件/模块：培训工作台 spec、manual、skill；依赖：T003、T004；验收：不再描述固定提前月或逐人建议调整，代码、测试和文档口径一致。
- [x] T006 动作：使用真实培训表回放并检查模型规模、耗时、TSA 分布、逾期和月度负载；文件/模块：只读本地真实文件；依赖：T002-T005；验收：真实文件未提交，411 人回放生成 1732 个推荐、0 个无法安排，TSA 5 月峰值已分散，四阶段优化约 516 ms。
- [x] T007 动作：运行局部测试、构建、类型检查、全量测试、确定性验证和最终 diff/status 检查；文件/模块：全仓验证；依赖：T001-T006；验收：全部命令通过，剩余风险如实记录。

## 8. 验证计划

相关局部测试：

- `npx.cmd vitest run tests/tool/training-workbench/smart-schedule.test.ts`
- 必要时联跑 `training-load.test.ts`、`qualification-pressure.test.ts` 和页面构建测试。

完整验证命令：

- `npm.cmd run build`
- `npm.cmd run typecheck`
- `npm.cmd test`
- `npm.cmd run verify`

构建或安装检查：

- 检查 `package-lock.json` 锁定求解器版本。
- 检查培训工作台浏览器产物包含求解器且页面不依赖外部网络。
- 记录构建后培训工作台入口产物体积变化。

页面/工具入口检查：

- 静态确认入口元素、事件接线、图表系列、固定高度滚动壳和项目筛选投影完整。
- 页面视觉效果由 owner 按仓库约定人工检查。

恢复或中断演练：

- 不涉及外部写入或可恢复任务；重新导入总表必须重建方案并清除旧月份选择。

文档同步检查：

- 全仓搜索`固定提前`、`提前月数`、`建议调整`和`智能排班对比`，确认培训智能方案生产页面、spec、manual 和测试不再保留旧口径。

未验证内容：

- 具体日期、教员、场地、班级和人员冲突按范围不验证。
- 页面视觉由 owner 人工确认，不以自动化测试代替。

剩余风险：

- 连续解整数化后的方案经过合法单任务局部改进，但不声明为全局整数最优；真实表已确认总量峰值接近月均且 TSA 集中峰值被拆散。
- 项目常见培训天数仍由现有历史众数估算，缺少历史记录时按 1 人天；这是月度规划参考，不是具体班次资源承诺。

## 9. 收口

目标是否完成：已完成。所有可安排人项只在合法月份内排入一次，全年方案按严格优先级先压低最高月人天，再缩小各月相对月均的偏差；低优先级项目均衡不能破坏前序结果。

失败测试是否变绿：已变绿。智能排班专项 13 个用例全部通过，全量验证通过。

改了哪些文件：新增独立负载优化器，重写智能排班方案生成和项目投影，删除固定提前月与人工方案评价，重接年度图和固定高度滚动明细，同步测试、开发 spec、用户手册、skill 和依赖锁文件。

跑了哪些验证：智能排班专项测试、真实培训表回放、`npm.cmd run typecheck`、`npm.cmd run verify`、生产依赖审计、确定性构建、`git diff --check` 和修改文件 UTF-8 BOM 检查。

哪些内容没有验证：页面最终视觉由 owner 人工确认；具体培训日、教员、场地、班级、人员冲突和未来合班不在本次输入与范围内。

剩余风险：连续解整数化后通过合法单任务移动继续优化，不声明为全局整数最优；项目人天来自历史最常见持续天数，缺少历史时按 1 人天，因此结果是月度规划参考，不是具体班次资源承诺。

是否 commit / push：已提交本次智能排班重构；未推送。
