# 智能排班滚动压力优化 Plan

## 1. 需求文档

资质管理员需要在人员尚未进入“必须排”之前，看见未来连续月份的资质到期峰值，并生成一份能够提前消化峰值的月度排班底稿。智能排班与手动排班总览职责不同：手动排班继续回答“当前评估区间内谁必须排、谁推荐排”；智能排班直接读取全部人员有效期，不使用手动评估月份或状态作为候选入口。

智能方案必须遵守以下顺序：

1. 每个人、每个项目的当前有效期任务不得过期。
2. 同一当前有效期任务只安排一次，不为了图表均衡增加重复培训，也不在同一次方案中链式生成下一轮。
3. 在合法方案中优先压低未来连续月份的最高培训人天。
4. 在不抬高最高负载的前提下，尽量满足可配置的安全提前量。
5. 在不破坏前序结果的前提下，尽量避开用户勾选的月份。
6. 最后缩小月份之间的负载偏差，并以项目内部均衡作为末级择优。

页面不再选择自然年。用户选择第一个未排班月份，系统默认观察未来连续 12 个月，并允许调整观察月数。安全提前量默认 2 个月，可调整；避让月份默认勾选 2、7、8 月，1 至 12 月均可动态勾选或取消。项目筛选只投影同一套全局方案。

图表用同一时间轴展示“原始到期压力”“均衡方案”和月均参考，使原本可能在某月集中到期的压力及其提前分摊结果直接可见。点击月份查看该月推荐人员项目与最晚完成日期。

业务完成标准：滚动范围可跨自然年；所有可安排任务恰好安排一次且不晚于最晚完成日期；窗口类不突破现行规则窗口；未来集中到期任务可被提前分散；未能合法安排的任务单独暴露；参数变化会重算一套全局方案；真实培训表中的 2026 年 10 月集中压力能够在原始压力图中被识别。

## 2. 当前事实

已确认用户工作方式与业务事实：

- 手动排班通过选择评估区间查看“必须排”和“推荐排”，适合当前月份执行，但无法主动削减尚未进入管理提前期的远期峰值。
- 智能排班必须独立读取全部有效期并进行全局优化，不能把“必须排/推荐排”作为候选入口。
- 每人每项当前有效期只培训一次；提前培训用于转移当前轮次压力，不允许为了数学均衡增加重复轮次。
- 资质规则保持现行事实：应急训练使用基准月窗口；危险品、英语和汉语能力使用保护窗口；航空安保、TSA、疲劳管理和飞行作风按最新培训日期重建有效期。
- 航空安保和 TSA 在智能排班中继续作为两个独立项目，不合并推荐任务。
- 避让月份属于软偏好；若资质安全要求必须安排在被勾选月份，仍须安排。

已确认代码事实：

- `smart-schedule.ts` 使用起始月份和 12 至 60 个连续月份生成滚动时间轴，最新日期类可跨自然年使用起始月至最晚完成月，窗口类继续取规则窗口与滚动范围的交集。
- 智能方案复用 `qualification-pressure.ts` 和 `rule-engine.ts` 取得当前轮次、覆盖状态、规则窗口和最晚完成日期；未来人工安排和模拟排班不改变智能推荐，已发生且覆盖当前轮次的培训滚动到下一到期事实。
- `smart-schedule-optimizer.ts` 连续锁定总峰值、安全提前目标、避让月份、总偏差和项目峰值；整数化后按相同顺序继续执行合法单任务移动，并在末级改善项目内部偏差。
- 页面提供滚动起始月份、观察月数、安全提前量、12 个月动态避让复选框和项目筛选；图表显示原始到期压力、均衡方案和月均参考。
- 全部项目视图只把 CRM 作为不可移动固定底座，单项目视图不叠加 CRM。
- 推荐明细继续使用现有 420px 固定高度、纵向滚动的 `selected-person-shell`。

已确认真实表证据：

- 使用 `C:\Users\Administrator\Desktop\南货航2026年培训表.xlsx` 回放识别 411 人。
- 2026 年 10 月人员信息表当前有效期压力为 71 人项、涉及 66 人，其中 TSA 46 人；现有安排已覆盖 38 人项，仍有 33 人项未覆盖，其中 TSA 30 人。
- 谭健的应急训练已于 2026-08-12 正确覆盖 2026-10 当前轮次并顺延至 2028-10，因此智能方案不重复推荐该轮；智能方案中的 2026-10 可执行任务为 70 人项，TSA 仍为 46 人项。
- 以 2026-09 为起始月、观察 12 个月、安全提前量 2 个月、避让 2/7/8 月回放时，共生成 1394 人项，其中 1393 人项可安排、1 人项无法安排；87 个任务组的求解约耗时 0.47 秒。
- 同一真实表回放中，全部项目加 CRM 的原始最高月为 2027-05 的 261 人天；均衡方案前 11 个月为 131 人天、最后 1 个月为 123 人天，最高月降为 131 人天。

当前缺口：

- 页面最终视觉效果按仓库约定由 owner 人工检查。

已确认无业务歧义：

- 滚动范围默认 12 个月，可配置为 12 至 60 个月；它是有限观察范围，不表示无限远求解。
- 安全提前量默认 2 个月，可配置为 0 至 12 个月；它是软目标，不替代真实截止日期或规则窗口。
- 第一个未排班月份默认下个月，可由用户选择；算法无法仅凭 Excel 自动判断某个未来月份是否已人工封盘。
- 避让月份默认 2、7、8 月，但全部月份均由页面复选框动态控制。

## 3. 失败测试

自动测试先定义以下回归：

- 起始月份为 2026-09、观察 12 个月时，月份轴必须是 2026-09 至 2027-08，不得重置到自然年 1 月。
- 2027-05 集中到期的 TSA 必须能够分散到 2026-09 至 2027-05 的合法月份，且每个人恰好出现一次。
- 当前轮次最晚完成日期晚于滚动范围末月的最新日期类任务不得被过早拉入；最晚完成日期落在范围内或早于起始月份的任务必须分别进入可安排或无法安排结果。
- 窗口类候选月份必须是现行规则窗口与滚动范围的交集，不能因安全提前量或避让月份突破窗口。
- 在同一最高月负载下，安全提前量设为 2 时，安排在建议完成月之后的惩罚必须低于未优化方案；无法满足时仍可使用真实截止日前月份。
- 勾选任意避让月份后，在不抬高最高月负载且不破坏提前量结果时，优化器必须减少这些月份的人天；任务只能在避让月份完成时仍须保留。
- 原始到期压力按每个人项的当前轮次最晚完成月份和项目人天生成；均衡方案使用完全相同的任务总量，不增加或遗漏培训。
- 已发生并覆盖当前轮次的培训不重复进入当前轮次，未来人工排班月份不决定智能推荐月份。
- 项目筛选只投影全局方案，不能重新求解或改变人员推荐月份。

真实表回放：

- 以 2026-09 为起始月份回放原始培训表，资质压力必须识别 2026-10 的 71 人项和 TSA 46 人项；智能方案必须排除已于 2026-08-12 覆盖当前轮次的 1 个应急训练任务，保留 70 个可执行任务和 46 个 TSA。
- 记录滚动方案的任务数、无法安排数、优化耗时、原始月度压力和均衡后月度人天；真实 Excel 与临时脚本不得提交。

静态与页面检查：

- 页面不再出现“排班年份”。
- 页面必须提供排班起始月份、观察月数、安全提前量、项目筛选和 12 个月动态避让复选框。
- 推荐明细继续使用现有固定高度纵向滚动壳。
- 图表系列名称与数据口径必须是原始到期压力、均衡方案和月均参考。

## 4. 目标

用户路径完成到：

- 导入培训表后，默认从下个月开始观察未来 12 个月；用户可调整起始月份、观察月数、安全提前量、避让月份和展示项目。
- 图表直接显示远期原始到期峰值与智能分摊结果；点击月份查看该月推荐人员项目。

代码主链路接到：

- 全部有效期事实 -> 当前轮次任务 -> 滚动范围与合法月份 -> 提前量和避让偏好 -> 分阶段全局优化 -> 原始压力与均衡方案投影 -> 页面展示。

完成判定：

- 失败测试、专项测试、真实表回放、构建、类型检查、全量测试、确定性验证和静态检查全部通过；代码、spec、manual、skill 口径一致。

## 5. 不做范围

- 不使用手动排班的“必须排/推荐排”状态作为智能方案输入。
- 不生成具体培训日，只推荐月份；若落在截止月份，明细继续显示具体最晚完成日期。
- 不分配教员、场地、班级、合班人数，不判断人员航班、休假或其他个人冲突。
- 不为了均衡增加重复培训，不在一次方案中继续生成培训后的下一轮任务。
- 不把航空安保和 TSA 合并成同一个推荐项目。
- 不开放有效期、规则窗口、忽略人员或优化优先级供页面修改；这些是业务安全规则，不是排班偏好参数。
- 不把自然年保留为求解边界或旧接口兼容字段。

## 6. 设计

输入：

- 培训总表分析结果。
- 排班起始月份，默认当前月的下一个月。
- 连续观察月数，默认 12，允许 12 至 60。
- 安全提前量，默认 2，允许 0 至 12。
- 避让月份集合，默认 `{2, 7, 8}`，允许任意月份组合。
- 当前浏览器模拟排班仅用于现状模块，不作为智能推荐月份偏好。

任务生成：

- 继续由 `qualification-pressure.ts` 和 `rule-engine.ts` 提供人员、项目、当前有效期、覆盖事实和最晚完成日期。
- 已发生且正确覆盖当前轮次时，以预测后的下一轮事实判断是否落入滚动范围；尚未发生的人工安排不改变理想任务。
- 只纳入最晚完成月份不晚于滚动范围末月的当前轮次；最晚完成日期早于起始月份的任务进入无法安排，范围末月之后的任务留待后续滚动方案。
- 窗口类候选月份取规则窗口、滚动范围和真实截止日期的交集。
- 最新日期类候选月份从滚动起始月份延伸到真实截止月份。
- 安全建议完成月按真实截止月份向前移动配置月数；它只产生软惩罚，不能扩张或收缩合法候选范围。
- 每个任务只生成一次，任务总人天由项目有效历史记录的最常见持续天数估算，缺少历史时按 1 人天。

优化优先级：

1. 所有任务恰好分配一次且只能使用合法月份，这是模型约束。
2. 最小化全部项目与固定底座合计的最高月人天。
3. 锁定最高月后，最小化晚于安全建议完成月的加权月数。
4. 锁定前序结果后，最小化落在动态避让月份的人天。
5. 锁定前序结果后，最小化各月相对总月均的绝对偏差。
6. 最后最小化项目峰值和项目内部偏差。

固定底座与对比：

- 全部项目视图把 CRM 已有负载作为不可移动固定底座；智能方案不把未来人工资质安排当作固定约束。
- 原始到期压力把同一批资质任务放在各自最晚完成月份，并叠加相同固定底座。
- 均衡方案把同一批任务放在优化月份，并叠加相同固定底座。
- 单项目视图只投影该项目的原始压力和推荐负载，不叠加 CRM。

状态与页面：

- `SmartSchedulePlan` 保存滚动月份轴、参数、一套全局任务与优化结果；项目筛选只调用 `buildView`。
- 起始月份、观察月数、安全提前量或避让月份变化时重建全局方案；项目筛选变化时不重算。
- 页面控制区使用月份输入、数值输入、项目下拉和原生复选框；避让月份使用稳定网格，不增加说明卡片。
- 图表点击只展开推荐明细；无合法月份的人项在没有选择月份时单独展示。

错误边界：

- 求解器非最优时抛出明确错误，不伪造结果。
- 起始月份、观察月数和提前量非法时使用已记录默认值。
- 已过期或起始月份之后已无合法月份的任务单独返回，不进入优化器。
- 工具只读，不写回 Excel。

模块职责：

- `smart-schedule.ts`：滚动月份、任务与偏好生成、固定底座、全局计划和视图投影。
- `smart-schedule-optimizer.ts`：严格分阶段优化与整数化，不承载有效期规则。
- `app-training-capacity.ts`：读取控件、构建方案和明细。
- `app-charts.ts`：渲染原始压力、均衡方案和月均参考。
- `models.ts`、`app-elements.ts`、`app-controls.ts`、`app.ts`：状态、元素和事件接线。
- `index.html`、`dashboard.css`：滚动参数与动态避让月份布局。

## 7. 实施任务

- [x] T001 重写本计划，固化滚动范围、全部有效期输入、一次当前轮次、动态参数、严格优化优先级和真实表验收口径。
- [x] T002 已重写智能排班测试；旧实现运行 12 个场景时 11 个失败，失败点覆盖跨年滚动、原始压力、范围边界、安全提前量和动态避让。
- [x] T003 重构优化器，加入安全提前量与避让月份的独立锁定阶段，并让整数化后的局部改进使用相同优先级。
- [x] T004 重构智能任务生成和视图模型，删除自然年边界，输出原始到期压力与滚动均衡方案。
- [x] T005 重接页面参数、状态、图表与滚动明细，删除排班年份控件。
- [x] T006 同步开发 spec、用户手册和 training-workbench skill，只保留新的滚动智能排班事实。
- [x] T007 使用真实培训表回放 2026-10 压力与滚动优化结果，删除临时脚本。
- [x] T008 串行运行专项测试、构建、类型检查、全量测试、确定性验证、依赖审计和最终 diff/BOM/status 检查。

## 8. 验证计划

局部验证：

- `npx.cmd vitest run tests/tool/training-workbench/smart-schedule.test.ts`
- 按影响联跑 `qualification-pressure.test.ts`、`training-load.test.ts` 和培训工作台构建测试。

完整验证：

- `npm.cmd run build`
- `npm.cmd run typecheck`
- `npm.cmd test`
- `npm.cmd run verify`
- `npm.cmd audit --omit=dev`

静态检查：

- `git diff --check`
- 检查修改文件 UTF-8 无 BOM。
- 搜索自然年字段、旧图表系列和旧文档口径。
- 确认真实 Excel、临时回放脚本和构建临时文件未进入提交范围。

页面检查：

- 自动验证只确认页面元素、事件接线和构建资源完整；视觉效果由 owner 人工检查。
- 避让月份网格、参数标签、图表和固定高度明细在常见宽度下不得互相覆盖。

未验证范围：

- 具体培训日期、教员、场地、班级、合班人数和人员冲突不在输入中，不做结果承诺。

## 9. 收口

目标是否完成：已完成代码、规则、页面接线、测试、文档和自动化验收；页面视觉按仓库约定交由 owner 人工确认。

失败测试是否变绿：已扩展为 17 个智能排班场景并全部通过。

改了哪些文件：已重构滚动任务生成、分阶段优化器、页面参数和图表接线，并同步测试、开发 spec、用户手册、skill 与本计划。

跑了哪些验证：智能排班专项测试 17 项通过；真实表回放通过并确认 71 个当前有效期压力、70 个智能可执行任务、46 个 TSA、1394 个滚动任务及约 0.47 秒求解耗时；最终 `npm.cmd run verify` 通过 68 个测试文件、280 个 TypeScript/JavaScript 测试、9 个 Python 测试和 173 个文件的确定性构建；生产依赖审计为 0 个漏洞；diff、旧字段、临时文件和 BOM 检查通过。

哪些内容没有验证：页面最终视觉效果未自动验证，由 owner 人工确认。

剩余风险：页面最终视觉仍由 owner 人工确认。

是否 commit / push：功能改动已提交并推送，提交为 `ed0d0e2`；本收口状态随当前文档提交同步。
