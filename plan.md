# 培训排班压力断裂式重建 Plan

## 1. 需求文档

培训皇帝需要用一套统一视角回答排班是否负载均衡。删除“年度有效期到期压力”“已排培训分布”“年度已培训人次”三个口径割裂且低使用价值的入口，重建为“排班后资质压力”主视图和“实际培训负载”辅助视图。两个视图不再堆叠数字摘要，改为选择培训项目后直接读一张月份图。

排班后资质压力以当前人员资质、有效培训安排和培训规则为依据，比较当前到期压力与现有安排正常执行后的未来到期压力。实际培训负载展示各月当前安排占用的人天和班次。使用者应能按全部项目或单个项目查看月份分布，并通过图形判断集中程度；需要追溯时可点击压力月份查看对应人员项目。

业务完成标准：资质压力和培训负载均来自当前导入总表及模拟排班；忽略人员不进入资质压力；CRM 只进入实际负载；航空安保与 TSA 同期合班按一个联合班次和同人同日一个人天计算；压力和负载都能按项目筛选且不显示数字摘要卡片；旧统计入口和旧口径全部移除，不保留兼容层。

## 2. 当前事实

- 排班总览已能从人员信息表、项目 sheet、模拟排班、取消备注、培训规则和特判名单生成逐人逐项目判断。
- `RuleEngine.evaluatePlanCoverage` 已维护应急训练、危险品、英语能力、汉语能力的窗口/锚点规则，以及航空安保、TSA、疲劳管理、飞行作风的最新培训日重算规则。
- 当前“年度有效期到期压力”基于排班总览筛选行聚合；有效安排覆盖后状态变为正常并通常被隐藏，不能表示排班执行后的预测有效期。
- 当前“已排培训分布”统计项目 sheet 中未取消且日期有效的记录条数，不判断覆盖，不含 CRM，也不按人天或班次去重。
- 当前“年度已培训人次”只统计项目 sheet 中“培训信息是否录入 = 是”的历史记录，不表示未来负载。
- CRM 是年度参加项目，不进入资质有效期规则；CRM 有效记录可进入实际培训负载。
- 程春林、宋云龙、邢晓楠为所有资质均不监控的不飞人员；安保教员和其他按项目特判继续复用现有忽略清单。
- 本地真实文件 `C:\Users\Administrator\Desktop\南货航2026年培训表.xlsx` 可用于只读回放，不能提交或成为测试依赖。
- 仓库不维护真实浏览器测试；前端视觉由 owner 人工检查。
- 当前新页面仍展示压力八项摘要和负载四项摘要，且负载没有项目筛选；这与 owner 最新确认的“一图为主、分类选择”不一致。

## 3. 失败测试

- 窗口内安排必须保留应急训练、危险品、英语能力和汉语能力的原到期锚点，并把压力顺延到正确月份。
- 航空安保、TSA、疲劳管理和飞行作风必须按覆盖培训日重建有效期和未来压力月份。
- 已排但不覆盖当前轮次时，预测压力必须保留在当前到期月份并标记未覆盖；晚于截止日的安排必须单独标记。
- 同一人同一项目存在多条安排时，只采用能够覆盖当前轮次的最早有效安排，不能把多条历史或未来记录链式顺延。
- 特判忽略的人员项目不得进入资质压力。
- 实际负载中，同一人同一天参加航空安保与 TSA 只计一个人天，同起止日期的两个项目只计一个联合班次。
- CRM 有效安排必须进入实际负载，但不得进入资质压力；取消记录不得进入负载。
- 页面源码、状态、事件、类型、文档和测试中不再保留三个旧统计入口及其旧口径。
- 实际负载按单个项目筛选时，只能统计该项目的记录、人天和班次；选择航空安保或 TSA 时不能把另一项目的人员或班次混入结果。
- 压力图按单个项目筛选时，当前、预测和前后对比只能使用该项目的人项；月份点击明细也只能显示所选项目。

## 4. 目标

- 页面展示未来连续月份的当前资质压力、排班后预测压力和前后对比，统计单位统一为“人项”。
- 不展示峰值、月均、峰均比、未覆盖、已后移、提前和逾期等数字摘要卡片。
- 可按项目、观察范围和视图模式查看压力；月份图沿用现有明细交互查看对应人员项目。
- 页面按项目展示月度培训人天和班次，不显示有效记录数系列或负载数字摘要；CRM 纳入负载，联合培训正确合并。
- 核心规则有独立自动测试，代码、开发 spec、用户手册与页面行为一致。

## 5. 不做范围

- 不修改培训 Excel，不替用户自动调整或提交排班。
- 不把 CRM 解释为有效期资质，不为 CRM 生成到期压力。
- 不改变现有排班总览、漏项检查、人员资质查询、月历、CRM 年度核对和有效期更新的业务口径。
- 不保留旧统计名称、旧筛选器、旧状态字段、旧测试或旧模块兼容导出。
- 不引入真实业务 Excel 作为仓库测试数据，不新增真实浏览器测试。

## 6. 设计

`qualification-pressure.ts` 接收统一分析结果、模拟排班、项目和观察月份范围，复用排班评估生成的人员项目、有效安排、取消策略、特判和规则引擎。每个人项只评估当前这一轮：保留当前有效期与最晚完成日，选择最早能够覆盖的安排，计算预测有效期与预测最晚完成日；无覆盖时保留当前压力并记录未覆盖或逾期安排。输出逐项预测和连续月份当前/预测项目分布，不计算峰值、均值、大批次等人工摘要。

`training-load.ts` 接收项目 sheet 和精确 CRM sheet 的未取消有效记录。人天按员工号优先、姓名兜底的人员键与自然日去重；班次按起止日期和项目聚合，航空安保与 TSA 起止日期一致时合并为联合班次；资质记录数只保留在内部结果，CRM 只进入本模块。模块输出可用项目列表，并在聚合前按所选原始项目过滤，保证查看航空安保或 TSA 时不混入另一项目。跨日培训的每个自然日均计入人天，班次归入开始月份。

页面删除旧年度月份图、已排培训分布和年度已培训人次区块，新增一个主压力区和一个紧凑负载区。两个区块均使用项目选择框和单一图表，不展示数字摘要卡片；负载图只保留人天柱和班次线，并使用双纵轴保持班次曲线可读。业务状态保留在 `app-state.ts`，DOM 引用保留在 `app-elements.ts`，计算接线在 renderer/controller，ECharts 配置只在 `app-charts.ts`。月份点击通过压力明细筛选接入现有人员明细展示，不在图表层复制规则。

错误边界：缺少人员表或无法解析有效期的人项不进入压力计算，仍由现有健康检查提示；日期无效或取消的培训记录不进入负载；缺少员工号时才使用标准化姓名作为人员键。模块不写 Excel、不持久化预测结果。

## 7. 实施任务

- [x] T001 核实旧统计口径、规则引擎、页面接线、测试和文档，确定断裂式替换边界。
- [x] T002 为资质压力预测和实际培训负载补充失败测试。
- [x] T003 实现 `qualification-pressure.ts` 和 `training-load.ts`，复用既有规则、记录策略和特判清单。
- [x] T004 删除旧统计模块、测试、类型、状态、事件、图表和 HTML/CSS 接线。
- [x] T005 重建页面主压力视图、实际负载视图、项目筛选和月份明细交互。
- [x] T006 同步培训开发 spec、规则说明、用户手册和 training-workbench skill 的当前模块边界。
- [x] T007 使用真实培训表只读回放，核对关键月份、批次和负载中间值，不提交输入或临时输出。
- [x] T008 运行局部测试、构建、类型检查、全量测试、verify、diff/BOM/旧入口残留检查并收口。
- [x] T009 删除压力与负载数字摘要，新增两类项目选择器及禁用状态、事件和动态选项接线。
- [x] T010 扩展实际负载项目筛选，令压力图、明细和负载图都服从当前项目选择，移除有效记录图线。
- [x] T011 更新局部测试、开发 spec 和用户手册，删除旧摘要说明并明确压力与负载口径。
- [x] T012 重新运行局部测试和全量验证，检查页面源码、文档、BOM、diff 并更新收口。

## 8. 验证计划

先运行资质压力和实际负载相关 Vitest；再运行 `npm.cmd run build`、`npm.cmd run typecheck`、`npm.cmd test`、`npm.cmd run verify` 和 `git diff --check`。扫描旧模块名、旧 DOM ID、摘要卡片 ID 和旧展示文案，确认生产源码、测试、spec 与手册零残留。用真实 Excel 只读执行生产解析和新统计模块，核对项目选项、单项目压力、窗口保锚、最新日重算、单项目负载和联合班次样例。检查构建后的培训页面包含新选择器且资源接线完整；不启动 `start_index.py` 或真实浏览器，视觉布局由 owner 人工检查。

## 9. 收口

目标已完成。页面删除三个旧统计入口及其模块、类型、状态、事件、图表和测试；新页面只保留排班后资质压力和实际培训负载两张主图。两张图都支持全部或单个项目选择；压力图支持当前有效期、排班后预测和前后对比，点击月份才展开人员明细；负载图按人天和班次展示，使用双纵轴，移除有效记录曲线和所有数字摘要。资质压力模块不再计算或输出峰值、均值、峰均比、大批次、提前或逾期摘要，用户直接通过月份图判断集中程度。

当前总表口径已明确：实际培训负载是当前导入总表中所有有效且未取消的安排现状，包含已录入、未录入和模拟排班；CRM 计入负载但不进入资质压力。资质压力表示每个人每项资质的下一次最晚完成日期所在月份；窗口项目保留规则锚点，最新日期项目按培训日重建。

失败测试已转绿，覆盖单项目压力过滤、单项目负载过滤、联合班次、人天去重、CRM边界以及上一轮排班规则。真实文件 `C:\Users\Administrator\Desktop\南货航2026年培训表.xlsx` 已只读回放：8 个资质项目均可独立出图；2026 年全部负载为 1527 人天、120 班次，单项目人天和班次可以分别核对；原文件未修改，临时脚本已删除。

已运行 `npx.cmd vitest run tests/tool/training-workbench/qualification-pressure.test.ts tests/tool/training-workbench/training-load.test.ts`、`npm.cmd run typecheck`、`npm.cmd run build`、`npm.cmd test`、`npm.cmd run verify` 和 `git diff --check`。最终结果为 67 个测试文件、263 个单元测试、9 个 Python 测试全部通过，确定性构建验证 173 个文件。未启动 `start_index.py` 或真实浏览器，页面视觉由 owner 人工检查。当前变更尚未提交，下一步提交并推送。
