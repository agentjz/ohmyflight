# 首页独立应用导出 Plan

## 1. 需求文档

首页工具清单中的全部工具都要能够独立导出，当前清单共有 20 项，但实现不能写死数量。每个工具对应一个单独 ZIP，解压后从包内首页进入，不依赖 watchdog 仓库目录或网络资源。首页工具卡片不增加文字按钮，只增加 Bootstrap 下载图标，直接下载对应工具包。

浏览器工具和带 Python 下载能力的工具统一按网页应用导出；Python 文件、模板和依赖说明作为对应工具包内的静态资源保留，页面中的下载入口继续可用。

## 2. 当前事实

- 首页工具清单位于 `src/tool/tools-data.ts`，共 20 项，入口字段可用于生成包名和导出映射。
- 构建入口位于 `scripts/build.mts`，页面脚本由 esbuild 输出到 `dist/tool/app/<entry>/app/`，静态页面和公共资源先复制到 `dist/`。
- 现有页面大多引用公共库、模板和相对路径资源；部分页面通过 `fetch()` 读取包内 JSON/模板，审计对比页使用 module worker。
- 部分工具页面提供 Python 文件、`requirements.txt` 或 Excel 模板下载；这些文件位于 `public/tool/app/<entry>/`，构建时会进入 `dist/`。
- 仓库已有 `src/tool/project-archive.ts`，它用于业务项目数据包，不适合作为构建产物 ZIP 的目录编排。
- 首页已有 Bootstrap 依赖和工具卡片渲染，但没有独立应用下载链接。
- GitHub Pages 工作流在 `master` 分支推送后执行 `npm run verify`，并将完整 `dist/` 目录作为 Pages 产物发布；工具页面、独立 ZIP 和包内静态下载资源都会随之上线，Python 文件只能下载后在本机运行。

## 3. 失败测试

- 构建后不能为每个首页工具生成一个独立 ZIP，或 ZIP 缺少根目录首页。
- 导出包内页面仍引用仓库外部路径、`dist` 路径或网络资源，导致解压后无法运行或资源 404。
- 首页筛选/搜索后下载图标丢失、链接错误，或图标没有可访问名称。
- 工具清单没有与 ZIP 一一对应，或包名与入口映射不稳定。
- 导出 ZIP 将 `__pycache__`、源码仓库其他工具或临时文件带入包内。

## 4. 目标

- 每次生产构建在 `dist/exports/` 为当前工具清单逐项生成稳定命名的 ZIP，数量随清单自动变化。
- 每个 ZIP 根目录有 `index.html`，所有页面脚本、样式、公共库、Worker、模板、JSON 和 Python 下载资源均在包内。
- 首页每个工具卡片右侧显示 Bootstrap 下载图标，链接到对应 ZIP；图标带 `title` 与 `aria-label`，不显示“导出应用”文字。
- 生成逻辑从工具清单和构建产物推导，不写死工具数量或重复映射；构建失败时明确指出缺少入口或资源。
- 增加 ZIP 清单/路径完整性测试，并保持确定性构建验证可用。

## 5. 不做范围

- 不把整个仓库、所有工具或开发资料打成总包。
- 不制作 Python 可执行文件，不安装或捆绑用户机器上的 Python 环境和第三方轮子。
- 不修改工具业务逻辑、页面功能或现有项目数据包格式。
- 不在浏览器运行时临时压缩源码；ZIP 由构建阶段生成。

## 6. 设计

### 导出目录

- 构建先生成完整 `dist/`，再为每个工具创建临时导出目录。
- 将 `dist/tool/app/<entry>/index.html` 复制为导出目录根部 `index.html`，将该工具页面目录中的应用脚本和资源复制到包内。
- 分析页面和构建清单中的相对引用，将需要的公共库、主题资源、Worker、模板和数据文件复制到导出目录，并重写入口 HTML 的相对路径为包内路径。
- 对页面脚本统一输出适合本地打开的独立脚本格式；需要本地读取的数据优先内嵌或改写为包内可访问资源，不能依赖仓库路径或网络。
- 排除 `__pycache__`、source map、构建元数据和其他工具目录；包内保留页面实际提供的 Python/模板下载文件。

### 首页接线

- `ToolItem` 增加导出包名或由 `entry` 统一推导 `./exports/<entry>.zip`。
- `tools-render.ts` 在卡片标题区域加入 Bootstrap `file-earmark-arrow-down` 图标链接；保留工具卡片主链接和搜索/分类行为。
- 下载图标独立阻止卡片主链接冒泡，使用 `download` 属性，并对未启用入口仍提供包下载。

### 测试与记录

- 构建后读取 ZIP 中央目录，验证 ZIP 数量等于当前工具清单数量、各包有根 `index.html`、无越界路径、无 `__pycache__`，并检查 HTML 中不存在仓库绝对/相对前缀。
- 首页渲染测试验证每个工具有稳定 ZIP 链接、Bootstrap 下载类名和无障碍标签。
- 同步 `spec/dev` 与用户手册，记录导出位置、包结构和 Python 文件作为资源的事实。

## 7. 实施任务

- [x] T001 核对当前全部工具页面的入口 HTML、公共资源、Worker、模板和 Python 下载资源引用。
- [x] T002 实现构建期独立工具包生成器，并接入 `scripts/build.mts`。
- [x] T003 处理导出包路径重写和本地打开所需的脚本/资源兼容。
- [x] T004 首页工具卡片增加 Bootstrap 下载图标和稳定 ZIP 链接。
- [x] T005 增加导出结构、资源完整性和首页接线测试。
- [x] T006 同步开发规格和用户手册，清理不应进入包的临时资源。
- [x] T007 运行构建、类型检查、全量测试、确定性验证和导出包回放。
- [x] T008 检查工作区文件边界并记录提交、推送状态。

## 8. 验证计划

- 相关导出与首页测试。
- `npm.cmd run build`
- `npm.cmd run typecheck`
- `npm.cmd test`
- `npm.cmd run verify`
- `git diff --check`
- 随机抽查浏览器工具和含 Python 下载资源的工具，解压后从根 `index.html` 打开并确认静态资源可加载。
- 检查 ZIP 数量与当前工具清单一致，并核对清单、大小、文件路径和入口映射。

## 9. 收口

目标是否完成：已完成。工具清单中的每个入口都会生成独立 ZIP，首页卡片提供图标下载。

失败测试是否变绿：已通过。导出数量按当前工具清单动态比较，各包根入口、离线运行时、清单、路径安全和缓存排除均有测试。

改了哪些文件：构建脚本、独立包生成器、首页渲染和样式、工具首页测试、开发规格、用户手册、依赖清单和本计划。

跑了哪些验证：`npm.cmd run verify`、`npm.cmd audit --omit=dev`、`git diff --check`、UTF-8 BOM 检查、ZIP 清单抽查，以及 Edge 直接打开解压后的菜鸟教程 `index.html` 回放。全量结果为 68 个测试文件、264 项断言和 9 项 Python 测试通过，两次确定性构建一致。

哪些内容没有验证：首页图标最终视觉、全部工具的逐项人工操作，以及校对之王导入真实手册后 Worker 的完整流程仍需 owner 实际使用确认。Edge 和 Chrome 的无头截图输出异常，未将其作为视觉结论。

剩余风险：离线运行时已覆盖当前本地 `fetch()` 和 Worker 资源，并由 Edge 的菜鸟教程回放确认；其他浏览器及大型真实输入仍存在人工验证风险。
