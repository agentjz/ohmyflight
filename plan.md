# 智能排班差异明细与固定提前月数修复 Plan

## 1. 需求文档

培训排班负责人需要在点击智能排班图中的月份后，始终能在固定高度的明细区域内纵向滚动查看全部人员项目。该明细应沿用项目风险矩阵下方人员明细的布局方式，表格列铺满可用宽度，不制造额外的横向宽表。

“最新日期类提前范围”改为“最新日期类提前月数”：输入 `1` 表示固定安排在到期月前 1 个月，输入 `2` 表示固定安排在到期月前 2 个月，默认值为 `2`。固定提前月份已经错过但资质尚未过期时，才降级到截止日前尽快安排。

## 2. 当前事实

- 项目风险矩阵下方人员明细使用 `.selected-person-shell { height: 420px; overflow: auto; }` 形成固定高度滚动区。
- 智能排班差异明细当前只设置 `max-height: 430px`，表格另有 `min-width: 1040px`，没有复用上述固定高度模式。
- 公共 `.table-shell` 默认 `overflow-y: hidden`，差异明细虽用后置规则改为 `overflow: auto`，但没有固定高度时无法保证形成稳定的内部滚动区。
- 页面已改为整数输入，默认值为 `2`，输入值表示固定提前月数。
- 算法已改为优先使用到期月减去固定提前月数的唯一目标月份。
- 排班年份已经是数字输入，本次不改变其规则。

## 3. 失败测试

- 最新日期类到期月份为 2027-06、提前月数为 `2` 时，两个人项都必须推荐到 2027-04，不得分散到 2027-05。
- 固定提前月份已过去但截止日尚未到来时，仍应给出不晚于截止日的紧急安排；截止日已过去时显示无法安排。
- 到期月在所选年份 1 月、固定提前月份属于上一年且尚未错过时，不得错误放入本年到期月。
- 静态检查确认差异表使用固定高度纵向滚动壳、表格宽度为 100%，不再设置强制横向最小宽度。

## 4. 目标

- 差异明细复用人员明细的 420px 固定滚动壳，数据在区域内部纵向滚动。
- 差异表完整占用容器宽度，短字段保持紧凑，说明列换行，不要求用户横向滚动。
- 最新日期类提前量使用数值输入，默认 2，输入 N 的业务含义是固定提前 N 个月。
- 算法、页面接线、测试、开发 spec 和用户手册使用同一口径。

## 5. 不做范围

- 不改变窗口类项目的基准月或保护窗口算法。
- 不改变排班年份输入、图表交互、人员状态定义和人天估算。
- 不增加排程库，不考虑具体日期、教员、场地、合班人数或人员冲突。
- 不修改项目风险矩阵本身。

## 6. 设计

页面将提前量下拉框替换为 `type="number"` 的正整数输入，不写死最大月数，默认 2；DOM 类型和事件接线同步改为输入元素。算法先计算 `到期月 - N` 这个唯一目标月：目标月属于所选年份且尚可安排时，只返回该月；目标月已经错过且截止日仍在所选年份时，返回当前可执行月份作为紧急兜底；目标月属于上一年度且尚未错过时，由上一年度方案负责，不把任务塞入到期月。

差异明细沿用现有人员明细的固定滚动壳模式。滚动壳固定高 420px，仅纵向滚动；表格使用 100% 宽度和固定布局，为状态、项目、姓名、员工号及日期设置紧凑列宽，说明列承接剩余宽度并换行。表头设为粘性定位，保证长列表滚动时仍可识别各列。

## 7. 实施任务

- [x] T001 先更新智能排班测试，使固定提前 2 个月的预期在旧算法上失败。
- [x] T002 修改最新日期类候选月份算法和提示文案。
- [x] T003 将提前量控件改成默认值为 2 的数字输入并同步类型、事件和状态接线。
- [x] T004 复用人员明细模式修复差异表固定高度纵向滚动和铺满列宽。
- [x] T005 同步开发 spec 和用户手册。
- [x] T006 完成局部与全量验证，检查提交内容后提交并推送。

## 8. 验证计划

- 局部规则测试：`npx.cmd vitest run tests/tool/training-workbench/smart-schedule.test.ts`
- 类型检查：`npm.cmd run typecheck`
- 构建：`npm.cmd run build`
- 全量测试：`npm.cmd test`
- 完整验证：`npm.cmd run verify`
- 静态检查：`git diff --check`，并检查差异表 CSS 不再强制横向最小宽度。
- 页面视觉和真实滚轮手感由 owner 人工确认；自动验证只覆盖算法、类型、构建和静态结构。

## 9. 收口

目标已完成。智能排班差异明细现在直接复用项目风险矩阵人员明细的固定 420px 滚动壳，表格 100% 铺满并在说明列换行；最新日期类提前量已改为默认 2 的正整数输入，算法按固定提前月数生成目标月份，错过后才使用到期前紧急兜底。代码、测试、开发 spec 和用户手册已同步。

验证已完成：局部智能排班测试 7 项通过；全量 68 个测试文件、270 个单元测试和 9 个 Python 测试通过；`npm.cmd run typecheck`、`npm.cmd run build`、`npm.cmd run verify` 通过；确定性构建验证 173 个文件通过；`git diff --check` 无错误。页面视觉和实际滚轮手感仍由 owner 人工确认。

提交与推送：待执行。
