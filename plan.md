# 人员结构统计闭环 Plan

## 1. 需求文档

人员结构统计需要以人员信息 Excel 的原始人员行、技术信息和资格代码为唯一统计事实，浏览器统计与 Python Word APP 使用同一口径。使用者应能从教员、机长、副驾驶总结构继续核对技术等级、航线资格、报务、居住和原单位，任一构成表的人数合计都必须等于明确母数。

当前范围包含浏览器页面、Excel 导出、Python Word APP、开发规格、用户手册和自动测试。飞行管理人员不统计；`是否运行`不参与输入判断；输出术语统一使用“转机型机长”和“转机型副驾驶”，不使用“在训机长”或“在训副驾驶”。

业务完成标准：

- 教员、机长、副驾驶构成合计等于结构统计人员总数。
- 机长含以上等于教员、普通机长、转机型机长；机长航线和机长报务母数等于机长含以上减转机型机长。
- 副驾驶级别包含转机型副驾驶；副驾驶报务母数等于副驾驶级别减转机型副驾驶。
- 人员居住和原单位均回到结构统计人员总数。
- 检查员作为“其中项”展示，不参与技术等级构成求和。
- E 类副驾驶和无法归入既定航线资格的人员必须有可见分类，不得从页面、Excel 或 Word 中消失。
- Word 标题母数、目标月份、本月变化、本月占比和表内构成同步更新，原始 Word 不被覆盖。

## 2. 当前事实

已确认代码事实：

- `logic.ts` 与 `app.py` 当前都统计固定地面人员 29 人并输出飞行管理人员，仍使用“在训机长”。
- 当前浏览器总人数为已注册空勤加固定地面人员，后续结构表则包含转机型人员，两者不是同一母数。
- 当前 Word APP 写入第 1 张飞行管理人员表，不更新各节标题母数，并跳过模板不存在的“其他”。
- 当前百分比逐项独立四舍五入，构成项合计可能为 99% 或 101%。

已确认真实样本事实：

- `C:\Users\Administrator\Desktop\20260724.xlsx` 的“人员信息”表有 411 条记录，员工号无空值、无重复。
- 411 人由教员 102 人、机长 113 人、副驾驶 196 人构成。
- 机长含以上 215 人，其中转机型机长 8 人；机长航线和报务母数均为 207 人。
- 副驾驶级别 196 人，其中转机型副驾驶 24 人；副驾驶报务母数为 172 人。
- 当前数据有 E 类副驾驶 1 人；机长航线资格有 2 名 D 类机长进入“其他”。
- 浏览器和 Python 当前对真实样本输出的人数一致。

已确认 Word 事实：

- `C:\Users\Administrator\Desktop\南货航人员结构报告202607.docx` 有 11 张表，飞行部统计位于前 9 张表。
- 第 2 至第 9 张表的关系与目标结构一致；第 3 张表仍使用“在训机长”。
- 第 4 张机长航线资格表没有“其他”行，第 6 张副驾驶级别表没有“E类副驾驶”或“其他”行。
- 使用当前数据直接映射时，机长航线表只能显示 205/207 人，副驾驶级别表只能显示 195/196 人。

已确认测试事实：

- 现有浏览器逻辑测试只用固定样例验证闭环。
- 现有 Word 测试依赖仓库根目录可选的 `1111.docx`，文件不存在时会跳过关键写回验证。

当前缺口：

- 没有统一的构成项/其中项语义和自动闭环结果。
- 没有保证整数组成百分比合计为 100%。
- 没有动态补充 Word 模板缺少的有效分类行。
- 浏览器导出没有独立闭环核对结果。

仍不明确：无阻塞项。外部 Word 只作为结构参考，真实统计以人员信息 Excel 为准。

## 3. 失败测试

自动测试：

- 浏览器结果不得再包含“总人数及空地人员占比”和“飞行管理人员占比”。
- 缺少`行政职务`时仍能解析；`是否运行`任何取值不改变统计结果。
- 所有 section 的构成合计必须等于闭环母数，构成项百分比必须合计 100%。
- 检查员必须标记为其中项，不进入机长级别构成合计。
- E 类副驾驶必须单列；转机型术语必须统一。
- Word APP 必须保持第 1 张管理人员表不变，更新第 2 至第 9 张表标题和数据，动态补齐 E 类副驾驶和航线“其他”。
- 生成 Word 读回后，目标月份列逐表闭环，标题母数与统计结果一致。

类型检查：`npm.cmd run typecheck` 必须通过。

构建命令：`npm.cmd run build` 必须通过并分发最新 `logic.js`、`main.js`、`app.py`。

静态检查：`git diff --check` 必须通过；真实 Excel、Word 和生成报告不得进入 Git。

真实用户路径演练：使用 `20260724.xlsx` 和参考 Word 生成新报告，读回目标月份、标题、分类行和构成合计。

失败判定：任一表少人、多人、母数标题未更新、构成百分比不闭环、转机型术语不一致、管理人员被统计或真实文件被覆盖，均判定失败。

## 4. 目标

用户路径完成到：上传人员信息表后直接获得八张闭环统计表和可审计 Excel；Python APP 生成闭环 Word 新文件。

代码主链路接到：共享业务口径分别在 TypeScript 和单文件 Python APP 中实现一致的人员集合、分类、百分比和闭环元数据。

状态和记录落到：页面、Excel 与 Word 都显示明确母数；异常分类保留人员明细并进入核对提示。

输出呈现到：页面突出结构总人数、机长含以上、副驾驶三项根数据；表头显示构成合计与闭环状态。

测试和文档同步到：开发规格、用户手册、逻辑测试、Word 读回测试与实现一致。

完成判定：局部测试、真实样本回放和全量 `verify` 均通过。

## 5. 不做范围

- 不统计飞行管理人员，不要求`行政职务`表头。
- 不读取或筛选`是否运行`。
- 不使用“实力简介”等二次统计 sheet 代替人员信息原表。
- 不覆盖用户原始 Excel 或 Word。
- 不处理参考 Word 第 10 张及以后的其他部门内容。
- 不提交真实业务文件或生成报告。

## 6. 设计

主链路：

人员信息 Excel -> 按表头读取人员 -> 按技术信息形成结构人员集合 -> 生成八张统计表 -> 分配闭环百分比 -> 页面/Excel 展示，或写入 Word 第 2 至第 9 张表 -> 读回核对。

模块边界：

- `logic.ts`：浏览器人员解析、集合分类、闭环百分比和异常明细。
- `main.ts`：上传、渲染、闭环提示和 Excel 导出。
- `app.py`：同口径计算、Word 标题更新、缺失行补齐、目标月份写入和日志。
- `index.html`：稳定的上传、摘要、核对和结果布局。
- spec/manual：业务口径与用户操作事实。

数据设计：

- 统计项增加“构成项/其中项”关系；section 增加构成合计、闭环母数和闭环状态。
- 闭环构成项使用最大余数法分配整数百分比，保证人数闭环时百分比合计 100%；其中项独立计算比例。
- 技术信息源值`划转机长/划转副驾驶`对外显示为`转机型机长/转机型副驾驶`。
- Word 标题按 section 母数重写；模板缺少的非零分类复制相邻行样式后插入。

错误边界：缺少必要表头、Word 少于 9 张表、月份列不存在或存在多个候选时明确失败；无法归类人员进入可见“其他”，不静默跳过。

测试和文档影响：更新浏览器逻辑测试、Word 生成与读回测试、开发规格和用户手册。

## 7. 实施任务

- [x] T001 建立真实 Excel 与参考 Word 的人数、表序、术语和缺口基线；验收：已定位 408/411、检查员叠加、E 类副驾驶和航线其他问题。
- [x] T002 先更新失败测试，覆盖管理人员移除、转机型术语、E 类、闭环百分比、Word 标题与动态行；验收：旧实现 6 项失败，失败原因首先命中旧的行政职务必需表头。
- [x] T003 重构 TypeScript 统计结果和页面/Excel 输出，保证八张表闭环；验收：类型检查通过，浏览器逻辑 4 项测试通过。
- [x] T004 同步 Python 统计与 Word 写回，保持管理人员表不变并补齐模板分类；验收：Python 语法检查通过，浏览器/Python/Word 共 7 项局部测试通过。
- [x] T005 更新开发规格和用户手册；验收：输入表头、八张闭环表、转机型术语、四个导出 sheet 和 Word 写回行为均已同步。
- [x] T006 使用真实 Excel/Word 回放并读回核对，再运行局部与全量验证；验收：真实 411 人样本八张表人数和百分比闭环，原文件哈希不变，`npm.cmd run verify` 全部通过。
- [x] T007 更新收口事实并检查 Git 范围；验收：仅 9 个任务相关文件有变更，临时回放目录被忽略，`git diff --check` 通过，页面与三个分发资源均返回 HTTP 200。

## 8. 验证计划

相关局部测试：

```powershell
npm.cmd run build
npx.cmd vitest run tests/tool/personnel-structure-stats/logic.test.ts tests/tool/personnel-structure-stats/docx-app.test.ts
```

完整验证命令：`npm.cmd run verify`。

构建或安装检查：确认 `dist/tool/app/personnel-structure-stats/` 和 `public/tool/app/personnel-structure-stats/app.py` 为最新构建结果。

页面/工具入口检查：静态检查摘要、八张结果表、闭环状态、异常提示和 Excel sheet 结构；视觉最终由 owner 人工确认。

恢复或中断演练：本任务无长时间外部写入；Word 始终另存新文件，失败不得覆盖原文件。

文档同步检查：spec、manual、代码术语和测试断言不得再描述固定地面人员、管理人员统计或“在训机长”。

未验证内容：外部 Word 在 Microsoft Word/WPS 中的最终分页和视觉由 owner 人工确认。

剩余风险：动态插入 Word 行需要读回验证样式和表序；闭环通过不代表未知业务分类已经获得业务命名，仍需在“其他”中保留人员证据。

## 9. 收口

目标是否完成：已完成。浏览器、Excel 导出和 Python Word APP 均使用八张闭环统计口径，飞行管理人员不统计。

失败测试是否变绿：浏览器逻辑与 Python Word APP 共 7 项局部测试已全部通过。

改了哪些文件：已修改浏览器统计、页面和导出，重构 Python 统计与 Word 写回，并同步测试、开发规格和用户手册。

跑了哪些验证：`python -m py_compile` 和 7 项局部测试通过；真实 411 人 Excel/Word 回放的八张表人数与百分比全部闭环，原文件哈希不变；全量 `npm.cmd run verify` 通过（220 项单元测试、7 项 Python 测试）；页面及资源 HTTP 200；`git diff --check` 通过。

哪些内容没有验证：外部 Word 在 Microsoft Word/WPS 中的最终分页和视觉仍由 owner 人工确认。

剩余风险：见验证计划。

是否 commit / push：本轮 owner 未要求提交或推送，当前未执行。
