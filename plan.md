# 菜鸟教程知识结构与超文本改造 Plan

## 1. 需求文档

用户要解决什么实际问题：菜鸟教程当前把技术等级、转机型训练、持续有效条件、失效恢复和办理记录挤在同一张表中。信息虽然较全，但主流程不突出，副驾驶转机型和机长转机型没有作为完整业务链独立呈现，A1 建立经历等相近数字也容易被误读成两套要求。

谁会使用：需要理解南货航飞行人员成长、技术等级、运行资质和资格恢复规则的新员工及业务人员。

用户完成任务时应该看到什么体验：从当前身份或失效原因进入，就地看到进入条件、按顺序执行的训练和检查、合格后的状态、运行限制以及失败分支；可以跳转到关联的详细恢复流程和来源，不必先理解两本手册的章节结构。

当前范围包含：重新阅读并落实《飞行人员训练大纲》R03-01 V6 和《飞行技术管理手册》R01-08 V1 中的副驾驶转机型、机长转机型、技术等级、资质管理和重新获得资格规则；拆分知识源；增加可复用规则装配和超文本跳转；收敛审计篮子字号层级并修复全部详情命中定位；下线仓库级真实浏览器和临时性能验证入口；同步页面、开发规格、用户手册、项目 skill 和核心结构测试。

当前范围不包含：《运行手册》内容；原始 Word 在线阅读；审批自动化；真实浏览器或视觉回归测试；只锁定颜色、选择器、文件数量或目录形状的表面测试。

业务上怎样算完成：副驾驶和机长转机型形成独立完整流程；A1 到 A2 的 25 小时、航段、起落和人工飞行要求作为同一阶段的并列字段清楚表达；A1、A2、B、C、D 页面就地显示完整的保持、失效和恢复答案，但共享规则只维护一次；按失效原因的恢复分支能够闭合到恢复、降级或停飞状态；所有页面链接和来源 ID 可在构建时解析。

## 2. 当前事实

已确认代码事实：知识源由 manifest.json、sources.json、按业务主题拆分的 modules/*.json 和自由文章 special-categories.md 组成；构建脚本校验来源、复用记录和关联记录后生成单一 dist/tool/beginner-tutorial-data.json；页面支持模块和记录级 hash、跨模块关联跳转、共享恢复规则就地展开。

已确认测试事实：菜鸟教程专项测试保护 A1/A2 经历字段、近期经历 18 分钟要求、机长重新获得资格失败终态和断开复用记录的构建失败；不测试 CSS、颜色或目录形状。全量非浏览器测试和确定性构建验证已通过。

已确认文档/spec 事实：开发规格、用户手册和项目 skill 已同步拆分知识源、独立转机型入口、页面内完整答案、关联跳转和恢复规则维护方式。

已确认配置或入口事实：菜鸟教程是工具首页第一项，页面接入共享主题；构建产物仍可保持单一 JSON，源文件拆分不会增加浏览器请求或运行部署复杂度。

已确认命令输出：当前仓库 HEAD 为 8340ffa，开始研究时工作树干净；原始手册为桌面“手册”目录中的训练大纲 R03-01 V6 和技术管理手册 R01-08 V1。

当前已有能力：模块导航、全文搜索、来源解析、独立副驾驶/机长转机型主链、技术等级纵向详情、资质目录、恢复分支、记录间超文本跳转、共享恢复规则构建期装配和 Markdown 特殊类别说明；审计篮子检查项标题不会挤压关键词，选择检查项或命中项时检查单参考和全部详情分别定位当前标记。条件、保持、失效、恢复在同一详情内按单列顺序展示，只使用完整横向中性分隔线；折叠控件使用加号和减号，不使用箭头。

仍不明确：两本手册当前存在章节编号文字引用与实际章节标题不完全一致的情况，教程只按已核实章节标题和语义维护，不自行纠正文书编号；页面最终视觉效果由 owner 人工确认。

## 3. 失败测试

自动测试：若来源 ID、共享规则 ID、记录目标 ID 无法解析，或副驾驶/机长转机型和恢复分支未进入构建产物，知识构建测试失败；不测试具体 CSS 和目录数量。

类型检查：新增稳定数据模型的参数、返回值或引用类型不完整时失败。

构建命令：任一知识源 JSON 无效、ID 重复、引用断裂、Markdown 无法读取或构建产物无法生成时失败。

静态检查：员工正文出现无法解析的 sourceIds、ruleRefs、links，或页面 CSS 出现彩色单边强调时失败。

真实用户路径演练：不启动真实浏览器；通过构建产物静态检查确认模块、记录、完整恢复文本和目标锚点均存在。视觉阅读由 owner 后续人工检查。

失败判定：任一转机型链缺少进入条件、训练、检查、经历、合格状态或失败结果；A1/A2 数字被写成两套独立阶段；恢复分支停在“按手册处理”；等级页没有就地答案或链接目标不存在。

## 4. 目标

用户路径完成到：用户可从“副驾驶转机型”“机长转机型”“副驾驶技术等级”“机长技术等级”“资质总表”或“重新获得资格训练”进入，并在当前页面获得完整答案或跳到对应详细分支。

代码主链路接到：分主题知识源 -> 构建期解析来源、共享规则和链接 -> 单一 beginner-tutorial-data.json -> 动态导航、详情条目和锚点跳转。

状态和记录落到：获取条件、当前权限、保持条件、失效影响、恢复流程、合格结果和不合格分支使用明确结构；审批发布和排班系统生效作为最终状态的一部分表达。

输出呈现到：单向成长流程使用顺序步骤；恢复流程使用分支条目；普通等级和资质使用可扫描的详情区；页面允许重复显示已装配的规则正文。

测试和文档同步到：核心知识装配测试、开发规格、用户手册和 beginner-tutorial-dev skill 与新结构一致。

完成判定：构建、类型检查、全量自动测试和不含真实浏览器的确定性验证通过；构建产物中的来源和超文本引用全部闭合；计划收口记录完整。

## 5. 不做范围

不做：真实 Playwright/Chromium 测试、视觉快照、原始手册下载和审批办理功能。

不保留：单文件 knowledge.json 作为唯一维护入口；对普通员工没有决策价值的“依赖”固定栏目。

不兼容：旧知识源内部结构不作为外部 API 保留；只保证构建产物和页面入口继续可用。

不处理：两本手册之外的运行政策和业务规则；没有完整原文依据的 E 类副驾驶或有意留空课程。

## 6. 设计

主链路：用户选择业务模块或搜索 -> 页面读取已装配知识 -> 展示当前记录的取得/权限/保持/失效/恢复 -> 用户按需展开详细分支或跳转关联记录 -> 来源落到手册版本、章节和语义范围。

输入 -> 判断 -> 状态 -> 执行 -> 输出 -> 记录：现行手册事实 -> 当前身份或失效原因 -> 技术等级/资质有效状态 -> 训练、考试、检查、经历、审批 -> 新等级/恢复/降级/停飞 -> 执照签注、训练检查记录、资质通告和系统生效。

模块边界：来源索引只维护证据；共享规则只维护跨页面复用的保持/失效/恢复语义；转机型文件维护事件链；等级文件维护成长和权限；资质文件维护五类目录中的员工可用条目；恢复文件维护按原因和失败次数拆分的决策树；页面只负责渲染和导航。

文件职责：manifest.json 声明来源文件和模块顺序；sources.json 声明手册来源；主题 JSON 各自维护一种业务变化原因；恢复模块中的完整记录同时作为等级页共享规则源，不另建重复规则文件；types.ts 定义源数据和构建数据；构建脚本负责校验、装配和生成；页面渲染模块负责把结构字段转换为 HTML。

状态归属：技术等级不等同于运行资格；近期经历等持续条件失效时明确写“暂停运行资格”，仅在手册明确规定时写技术等级降级；训练完成不自动等于可排班，最终状态包含适用的签注、聘任、审批发布和系统入库。

数据或事件流：等级记录通过 reuseRecordIds 引用共享规则，构建时展开为页面内完整内容；relatedRecordIds 使用目标记录 ID，构建时校验并生成记录关联；页面点击链接时切换模块并滚动到目标记录。

错误、恢复、中断、重试边界：构建遇到未知来源、规则或目标 ID 直接失败；恢复分支必须明确合格和再次不合格去向；原手册未给完整条件的内容标记为“条件未完整给出”，不推测补齐。

与现有架构的关系：保留静态本地 APP、单一构建产物、共享主题和现有工具入口；仅改变菜鸟教程源数据组织、构建装配和详情渲染。

测试和文档影响：新增只保护引用闭合、关键模块与业务分支的核心测试；不为视觉细节和目录形状写测试；同步两份 spec 和项目 skill。

## 7. 实施任务

- [x] T001 动作：完成两本原始手册的相关章节语义审计并记录结构结论；文件/模块：桌面原始 Word、临时只读抽取；依赖：无；验收：转机型、等级、资质和六类主要失效原因关系已确认。
- [x] T002 动作：建立拆分知识源、共享规则引用和超文本链接模型；文件/模块：content/、types.ts、scripts/build.mts；依赖：T001；验收：来源、规则、记录和链接 ID 构建期严格闭合。
- [x] T003 动作：新增副驾驶与机长转机型完整模块并修正 A1/A2 表述；文件/模块：转机型与技术等级知识源；依赖：T002；验收：完整链路与手册事实一致，25 小时相关字段合并为同一阶段要求。
- [x] T004 动作：重组资质和重新获得资格内容，保留清晰失败分支并供等级页就地复用；文件/模块：共享规则、资质、恢复知识源；依赖：T002；验收：每类恢复落到恢复、降级或停飞状态，近期经历含 18 分钟要求。
- [x] T005 动作：实现详情渲染、模块内锚点和跨模块跳转；文件/模块：main.ts、tutorial.css；依赖：T002-T004；验收：等级页完整显示装配内容，关联链接可定位详细分支，无彩色单边强调。
- [x] T006 动作：同步开发规格、用户手册和项目 skill；文件/模块：spec/、.agents/skills/beginner-tutorial-dev/SKILL.md；依赖：T002-T005；验收：不再把单文件和统一表格写成现行事实。
- [x] T007 动作：增加核心知识装配测试并执行验证；文件/模块：tests/tool/、构建产物；依赖：T002-T006；验收：局部测试、构建、类型检查、全量测试和确定性验证通过，不运行真实浏览器测试。
- [x] T008 动作：审查变更并完成计划收口；文件/模块：plan.md、Git 工作树；依赖：T007；验收：只包含本任务文件，风险和未验证项如实记录。
- [x] T009 动作：下线仓库级真实浏览器和临时性能验证入口；文件/模块：package.json、scripts、CI、ESM 交付文档；依赖：T007；验收：verify 只运行类型、业务测试和确定性构建，生产 Python 工具的 Playwright 依赖不受影响。

## 8. 验证计划

相关局部测试：运行菜鸟教程知识装配测试，验证 ID 唯一、来源/规则/链接闭合、两类转机型模块存在、关键恢复结果分支存在、构建产物可解析。

完整验证命令：npm.cmd run build、npm.cmd run typecheck、npm.cmd test、npm.cmd run verify、npm.cmd run verify:deterministic；verify 不再包含真实浏览器测试。

构建或安装检查：确认 dist/tool/beginner-tutorial-data.json 只引用两本现行手册，模块和记录可独立查询，页面脚本和专属样式成功生成。

页面/工具入口检查：静态读取生产 HTML、脚本和构建 JSON，确认页面入口、模块 ID、记录锚点和关联目标一致；视觉由 owner 人工检查。

恢复或中断演练：对机长和副驾驶重获资格实践考试不合格、熟练检查不合格、机长年度航线检查不合格、90 天近期经历不足和 120 天 100 小时不足逐条检查成功与失败终态。

文档同步检查：开发规格、用户手册、skill 与实际源文件、构建和页面行为一致。

未验证内容：不使用真实浏览器验证实际滚动位置和最终视觉密度；不验证外部审批系统行为。

剩余风险：原手册跨章节存在相似但维度不同的经历字段，后续手册修订仍需逐条语义审计，不能只比较数字。

## 9. 收口

目标是否完成：已完成。两类转机型、等级与资质生命周期、失效原因恢复分支、共享规则装配和记录间跳转均已进入构建产物；详情信息改为全宽单列纵向展示；审计篮子字号和详情定位已修复；仓库级真实浏览器和临时性能验证入口已删除。

失败测试是否变绿：是。菜鸟教程知识装配专项测试 2 项通过，断开记录引用会按预期构建失败。

改了哪些文件：拆分并重写菜鸟教程知识源；新增知识装配脚本、数据校验和渲染模块；更新审计篮子页面和详情定位；删除 browser-validation.py、legacy-performance.json 及全局浏览器验证命令；更新构建接线、类型、页面样式、核心测试、开发规格、用户手册、history.md 和相关项目 skill。

跑了哪些验证：npm.cmd run build、npm.cmd run typecheck、npx.cmd vitest run tests/tool/beginner-tutorial-content.test.ts、npm.cmd test、npm.cmd run verify、npm.cmd run verify:deterministic、git diff --check；全量单元测试 257 项和 Python 测试 9 项通过，172 个构建文件确定性校验通过；verify 未启动真实浏览器。

哪些内容没有验证：页面最终视觉和实际浏览器滚动由 owner 人工确认。

剩余风险：未使用真实浏览器检查折叠、跳转滚动和最终视觉密度；按 owner 要求由人工页面检查承担。手册后续修订仍需区分相似数字对应的不同经历字段。

是否 commit / push：本次任务要求提交并推送，收口时记录实际提交和远端结果。
