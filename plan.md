# 培训智能排班与负载对比 Plan

## 1. 需求文档

用户要解决什么实际问题：

培训总表已经有一套人工排班，用户需要知道这套排班与一套按规则自动均衡后的推荐方案相比，哪些月份负载过高、哪些月份可以削峰，以及推荐方案具体会新增或调整哪些培训安排。

谁会使用：

培训排班负责人。

用户完成任务时应该看到什么体验：

导入一份培训总表后，页面自动生成“当前排班”和“智能推荐”两组月度人天图。图表优先表达总量差异；点击月份或差异项后，再查看项目和人员明细。已经排好的内容有明确的“已排”状态，新增或建议调整的内容能追溯原月份和推荐月份。

当前范围包含：

- 只使用导入的培训总表作为输入。
- 按现有 8 个资质项目规则识别当前轮次和有效培训安排。
- 窗口/基准月类项目在规则允许的窗口内选月并保留原到期锚点。
- 最新日期类项目按可配置的安全提前量选月。
- 在不逾期且满足提前量后，尽量均衡各月培训人天。
- 对比当前已排人天与推荐人天。
- 以人员项目明细解释新增、保留和建议调整。

当前范围不包含：

- 具体培训日、教员、场地、班级容量、合班人数和人员冲突。
- 写回原 Excel、自动提交外部系统或替用户确认推荐方案。
- CRM 资质有效期预测；CRM 仅沿用实际培训负载的现有口径。
- 复杂整数规划或新增第三方排程库。

业务上怎样算完成：

对每个进入监控的人员项目，推荐月份不晚于规则截止日；最新日期类推荐月份满足安全提前量；推荐月度人天总量相对当前方案可被图形和明细核对，且当前已排记录不会在无理由时被标为新增。

## 2. 当前事实

已确认代码事实：

- `qualification-pressure.ts` 已按人员项目计算当前有效期、当前轮次截止日、有效排班覆盖和排班后有效期。
- `training-load.ts` 已按有效且未取消记录计算月度人天、班次和项目筛选。
- `rule-engine.ts` 已区分“基准月”“3个月窗口”“最新日期”，并提供窗口信息和计划覆盖判断。
- `app-training-capacity.ts`、`app-charts.ts`、`app-elements.ts`、`app-controls.ts` 是排班后统计视图的现有接线边界。
- 当前页面通过 `public/tool/app/training-workbench/index.html` 提供统计区域，入口为 `app.ts`。

已确认测试事实：

- 培训工作台已有资质压力、实际负载、规则、排班总览和忽略清单测试。
- 最近全量测试已通过 67 个测试文件、263 个单元测试及 9 个 Python 测试。

已确认文档/spec 事实：

- `training-rule-spec.md` 规定应急训练为基准月前后 1 个月，危险品/英语/汉语为到期前 3 个月窗口，航空安保/TSA/疲劳管理/飞行作风为最新日期重算。
- 不飞人员、安保教员及 CRM 教员按现有特判清单处理；CRM 不进入资质压力。
- `spec.md` 规定实际培训负载包含已录入、未录入、未取消和模拟排班记录。

已确认配置或入口事实：

- 页面已加载 ECharts，但业务模块没有用于排程的第三方依赖。
- TypeScript 仍使用兼容既有浏览器模块的配置。

已确认命令输出：

- 当前分支与 `origin/master` 已同步，工作区在本任务开始时干净。

当前已有能力：

- 导入 Excel 后生成当前资质压力和实际培训负载图。
- 资质压力支持当前有效期/排班后预测切换和项目筛选。
- 模拟排班可以作为临时记录进入压力和负载统计。

当前缺口：

- 没有按合法月份自动选择推荐月份的计算模块。
- 没有当前方案与推荐方案的月度人天对比图。
- 没有“已排/新增/建议调整/无法满足”差异明细。
- 没有最新日期类安全提前量的页面设置。

仍不明确：

- 最新日期类现有管理提前期为 2 个月；第一版沿用该事实作为默认值，并允许页面选择 1、2、3 个月。
- 推荐人天按项目表中有效记录的最常见培训持续天数估算；缺少可用历史日期的项目按 1 人天估算。航空安保和 TSA 的推荐侧不推测未来合班，分别估算。

## 3. 失败测试

自动测试：

- 给定窗口类人员项目时，推荐月份落在窗口内且不晚于截止日，并标记为保锚。
- 给定最新日期类人员项目时，推荐月份不晚于“截止月 - 安全提前量”，并返回重算说明。
- 已有安排覆盖当前轮次时，当前安排被标为已排，不重复生成新增项。
- 当前排班和推荐排班按月汇总后，推荐方案的人天峰值不高于未优化的候选放置结果。
- 已排、待排、建议调整和无法安排的差异状态可稳定生成。

类型检查：

`npm.cmd run typecheck` 必须通过。

构建命令：

`npm.cmd run build` 必须通过，培训工作台入口和脚本资源正常生成。

静态检查：

- 新增模块不复制规则判断，不新增全局命名空间。
- 页面专属样式只使用主题 token，不使用彩色侧边强调。

真实用户路径演练：

导入 `南货航2026年培训表.xlsx` 的本地副本后，能看到两组月度人天、能切换安全提前量，并能展开差异明细；原始文件不被写回。

失败判定：

任何推荐项晚于规则截止日、无合法候选月份却静默丢失、当前已排记录被错误标成新增、图表与明细月度总量不一致，均视为失败。

## 4. 目标

用户路径完成到：

导入总培训表后自动显示智能排班对比区域；选择安全提前量后立即重算；查看月份图和差异明细。

代码主链路接到：

扫描分析 -> 现有排班覆盖/负载口径 -> 推荐月份计算 -> 月度汇总与差异 -> 页面渲染。

状态和记录落到：

新增结果放入培训工作台运行时状态，不改 Excel，不与模拟排班状态混用。

输出呈现到：

- 当前/推荐双系列人天柱状图。
- 推荐月份、状态、项目、姓名、员工号和原安排月份明细。
- 无法安排或需要人工确认的原因。

测试和文档同步到：

新增推荐算法单元测试，更新培训工作台 spec 和用户手册中当前页面事实。

完成判定：

相关单元测试、全量测试、类型检查和构建全部通过；本地真实 Excel 回放不报错；Git 提交并推送。

## 5. 不做范围

不做：

- 具体日期级排班优化。
- 教员、场地、班级人数、合班和人员同时占用约束。
- 自动修改或导出推荐排班 Excel。
- 用 OR-Tools、线性规划服务或网络服务增加运行依赖。

不保留：

- 不为旧的峰值摘要、月均摘要或已删除的 plan 口径恢复兼容入口。

不兼容：

- 推荐模块不改变现有排班总览、资质压力和实际负载的既有统计口径。

不处理：

- 缺少有效期或无法识别项目规则的记录；此类记录进入人工确认提示，不参与静默推荐。

## 6. 设计

主链路：

输入 -> 判断 -> 状态 -> 执行 -> 输出 -> 记录

导入培训表 -> 为每个人员项目计算当前轮次截止日和当前安排 -> 生成合法候选月份 -> 以月度人天成本排序选择月份 -> 汇总当前/推荐负载 -> 生成差异状态 -> 图表和明细展示。

模块边界：

- `smart-schedule.ts`：纯计算推荐候选、月份选择、月度汇总和差异，不读 DOM。
- `app-training-capacity.ts`：读取控件、调用计算模块和更新状态。
- `app-charts.ts`：新增对比图渲染，不承载业务规则。
- `app-elements.ts`、`app-controls.ts`、`app.ts`：接入控件和生命周期。
- `models.ts`：承载稳定的推荐结果类型。

文件职责：

推荐模块只处理人员项目级月度资源分配；规则窗口继续由 `rule-engine.ts` 提供；月度图只消费推荐结果。

状态归属：

`TrainingToolAppState.smartSchedule` 保存当前导入文件、提前量和筛选条件对应的推荐结果；重新导入文件或模拟排班变化时重建。

数据或事件流：

导入完成、模拟排班变化、安全提前量改变、项目筛选改变时重建推荐结果；点击月份只筛选明细，不改变推荐数据。

错误、恢复、中断、重试边界：

单个人员项目无合法月份时保留为“无法安排”，不阻断其他记录；空表显示空状态；重新导入清空旧推荐状态。

与现有架构的关系：

沿用 ESM、运行时接口和 ECharts；不引入第三方库，不注册 window 全局模块。

测试和文档影响：

新增 `smart-schedule.test.ts`；同步 `spec/dev/training-workbench/spec.md` 和用户手册的页面功能说明。

## 7. 实施任务

- [x] T001 建立推荐结果模型和纯计算模块；文件/模块：`models.ts`、`smart-schedule.ts`；依赖：现有规则、分析和排班结果；验收：两类项目候选月份与差异状态测试通过。
- [x] T002 接入工作台状态、提前量控件和导入/模拟排班刷新；文件/模块：`app-state.ts`、`app-elements.ts`、`app-controls.ts`、`app.ts`、`app-training-capacity.ts`；依赖：T001；验收：导入表后自动生成结果，控件变化可重算。
- [x] T003 增加当前/推荐月度人天图和差异明细；文件/模块：`app-charts.ts`、`index.html`、培训工作台专属 CSS；依赖：T001、T002；验收：图表和明细使用同一结果，月份/项目筛选可用。
- [x] T004 同步测试、spec 和用户手册；文件/模块：`tests/tool/training-workbench/smart-schedule.test.ts`、`spec/dev/training-workbench/spec.md`、`spec/user/training-workbench/manual.md`；依赖：T001-T003；验收：文档只描述当前实现事实。
- [x] T005 运行局部测试、全量测试、类型检查和构建，检查工作区并提交推送；依赖：T004；验收：全部命令通过，提交只含任务相关文件。

## 8. 验证计划

相关局部测试：

`npx.cmd vitest run tests/tool/training-workbench/smart-schedule.test.ts`

完整验证命令：

`npm.cmd run typecheck`、`npm.cmd run build`、`npm.cmd test`、`npm.cmd run verify`

构建或安装检查：

确认培训工作台入口及新增脚本进入构建产物，不新增 npm 依赖。

页面/工具入口检查：

确认 `public/tool/app/training-workbench/index.html` 导入后显示推荐区域、图表和差异表。

恢复或中断演练：

导入新文件后旧推荐清空；清空模拟排班后推荐恢复为原始总表口径。

文档同步检查：

检查 spec、用户手册不再声称只有当前压力/实际负载而遗漏智能推荐功能。

未验证内容：

页面视觉细节由 owner 人工检查；不做真实浏览器自动化验收。真实 Excel 已做只读回放，不作为仓库测试依赖。

剩余风险：

人天按人员项目计数是月级推荐的近似口径；若后续需要按课程时长或真实班次容量优化，需要另行确认业务字段。

## 9. 收口

目标是否完成：已完成第一版月级智能排班对比；输入为培训总表，输出当前实际月负载与推荐月负载及按月展开的差异。

失败测试是否变绿：已完成。窗口保锚、最新日期提前范围、提前量不足兜底、跨年边界、项目时长估算和固定负载成本测试均通过。

改了哪些文件：新增 `smart-schedule.ts` 及测试；接入培训工作台状态、控件、图表和按月差异明细；同步培训 spec、用户手册和模块 skill；保留根目录 `plan.md` 作为本任务执行合同。

跑了哪些验证：培训工作台 14 个测试文件 32 项通过；全量 68 个测试文件 269 项通过；9 个 Python 测试通过；`npm.cmd run typecheck`、`npm.cmd run build`、`npm.cmd run verify` 和 173 文件确定性构建均通过；真实培训表只读回放识别 8 个资质项目和 1344 个人员项目。

哪些内容没有验证：页面视觉细节和真实浏览器交互由 owner 人工检查。

剩余风险：推荐按月而非具体日期排程；航空安保/TSA 推荐侧不推测未来合班；项目人天使用历史最常见持续天数估算；已经过去的月份只保留事实，不能反推历史最优方案。

是否 commit / push：待执行。
