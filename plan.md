# 资质压力布局与模拟排班下线 Plan

## 1. 需求文档

培训工作台的未来资质压力需要直接比较两种事实口径：当前有效期和排班后预测。主图只使用两组柱状图，默认固定显示连续 12 个月，观察范围仍可选择 24、36 或 48 个月并通过滑块或滚轮查看后续月份。

点击任一柱子后，第二行始终展示所点击口径和月份的资质构成；第三行始终展示该口径和月份的人员明细。人员明细必须固定高度，并支持横向、纵向滚动。模拟排班当前没有使用价值，页面、状态、规则注入、测试和文档中的模拟排班逻辑全部删除。

## 2. 当前事实

- 压力主图已同时生成当前有效期和排班后预测两组柱状系列，并保留连续 12 个月滚动窗口。
- 资质构成与人员明细已改为独占整行；人员明细使用与项目风险矩阵相同的高优先级滚动壳，覆盖通用表格的纵向隐藏规则。
- 模拟排班页面、状态、事件、临时行注入、负载统计、测试和文档接线已删除。
- 当前压力模型使用同一批 `currentByProject`、`forecastByProject` 和人员明细；真实表回放已确认 411 人、2026-10 当前压力 71 人项、TSA 46 人项，且每月总量等于项目构成之和。

## 3. 失败测试

- 主图应同时生成 `当前有效期` 和 `排班后预测` 两组柱状系列，并保持固定 12 个月窗口。
- 未点击月份时，构成区和明细区不能消失，应显示明确空状态；点击任一系列后，构成和明细必须使用对应口径。
- 模拟排班相关测试和 `extraProjectRows` 接口必须移除，其他排班评估、漏项检查和负载统计仍需保持原有 Excel 事实。
- 压力月总量必须始终等于项目构成之和。

## 4. 目标

- 第一行：固定窗口的当前有效期柱状图与排班后预测柱状图。
- 第二行：固定保留的月份资质构成饼图和空状态。
- 第三行：固定高度、横纵双向滚动、表头固定的人员明细。
- 删除模拟排班所有生产接线和临时数据路径，不影响正式 Excel 排班记录的评估、压力预测和负载统计。

## 5. 不做范围

- 不新增智能排班、均衡算法或推荐月份。
- 不修改有效期规则、覆盖判断、忽略清单或正式 Excel 解析口径。
- 不估算具体培训日、教员、场地、班级容量、合班人数或人员冲突。

## 6. 设计

### 主图

- `currentTotal` 和 `forecastTotal` 各生成一组 `bar` 系列，使用相同人项坐标轴。
- `dataZoom` 使用 `startValue/endValue` 锁定首个连续 12 个月；观察范围更长时用滑块或图内滚轮横向移动。
- 点击柱子时把 `seriesName` 映射为 `current` 或 `forecast`，写入选中口径并刷新下方两个区域。

### 下钻区域

- 页面结构改为三行全宽布局：主图、构成饼图、人员明细，不再把饼图和表格放在同一网格中。
- 构成饼图始终占位；未选月份显示“点击主图柱子查看构成”，选中后从对应 `currentByProject` 或 `forecastByProject` 生成。
- 人员明细始终占位；未选月份显示提示，选中后按选中口径筛选。容器固定 `height`，`overflow-x/overflow-y: auto`，表头 `position: sticky`。

### 模拟排班下线

- 删除模拟排班 HTML、CSS、页面控制器和存储模块。
- 删除状态字段、元素字段、事件监听、导入清空逻辑、渲染刷新逻辑和所有 `extraProjectRows` 参数。
- 删除人员明细中的选择、全选、反选和取消选择控件；保留查看、筛选和导出人员明细。
- 删除模拟排班 spec，并同步开发 spec、用户手册和 skill。

## 7. 实施任务

- [x] T001 重写本计划，确认双柱状图、三行布局和模拟排班下线范围。
- [x] T002 主图同时渲染当前有效期与排班后预测两组柱状系列，保留固定 12 个月滚动窗口。
- [x] T003 将构成和人员明细改为始终占位的全宽第二、三行，并修复双向滚动。
- [x] T004 删除模拟排班页面、状态、接线、临时行注入、人员选择控件和相关文件。
- [x] T005 同步测试、开发 spec、用户手册和 training-workbench skill。
- [x] T006 运行专项测试、全量测试、构建、类型检查、真实表回放和静态残留检查。
- [x] T007 使用简体中文提交并推送。

## 8. 验证计划

- `npx.cmd vitest run tests/tool/training-workbench/qualification-pressure.test.ts tests/tool/training-workbench/training-load.test.ts tests/tool/training-workbench/schedule-assessment.test.ts tests/tool/training-workbench/schedule-gap-check.test.ts`
- `npm.cmd run build`
- `npm.cmd run typecheck`
- `npm.cmd test`
- `npm.cmd run verify`
- `npm.cmd audit --omit=dev`
- `git diff --check`
- 搜索模拟排班、`extraProjectRows` 和删除后的元素残留；检查 UTF-8 无 BOM。
- 使用真实培训表回放压力基线和总量/构成一致性。

## 9. 收口

目标是否完成：已完成。双柱状图、三行布局、双向滚动和模拟排班下线均已实现。

失败测试是否变绿：专项测试 4 个文件、9 项断言通过；全量单元测试 67 个文件、262 项断言通过；Python 测试 9 项通过。

改了哪些文件：压力图表与下钻页面、滚动样式、培训工作台接线和模型、模拟排班相关文件与测试删除、培训 spec/手册、training-workbench 与 ui-clarity skill。

跑了哪些验证：`npm.cmd run build`、`npm.cmd run typecheck`、`npm.cmd test`、`npm.cmd run verify`、`npm.cmd audit --omit=dev`、`git diff --check`、残留扫描、UTF-8 BOM 扫描和真实培训表回放。

哪些内容没有验证：页面最终视觉和滚动体验需要 owner 浏览器人工确认。

剩余风险：ECharts 滚轮在不同浏览器的灵敏度需要人工确认。

是否 commit / push：已完成。
