# 支持页面统一与全局黄底清理 Plan

## 1. 需求文档

工作技能、开发者和用户手册页面需要与工具首页形成统一的视觉系统，解决现有页面顶栏、配色、按钮、间距和滚动反馈彼此割裂的问题。使用者仍按原有方式搜索业务 Skill、展开开发者 Skill、查看提交记录和下载用户手册，不改变内容口径和数据入口。

当前范围包含三个页面的共享双主题壳层、默认白天模式、日夜切换、可见滚动条、响应式顶栏和各自内容区的断裂式样式重构；同时清理全站共享警告、培训签到提醒、工作技能搜索高亮和换季学习选中态中的大面积黄色背景，并优化培训皇帝的人员资质查询和宽屏内容利用率。完成标准是三页在桌面和窄屏下保持统一视觉语言，长页面和内部长内容都有明确滚动反馈，原有搜索、hash 导航、折叠、复制、提交记录和下载功能可用，警告语义以中性表面配合小面积琥珀文字呈现；培训查询和年度图表在宽屏上铺满可用内容区且不改变统计口径。

## 2. 当前事实

- public/jobskill/index.html、public/jobskill/site.css 和 src/jobskill/site.ts 负责工作技能页面；桌面为左侧 Skill 导航和右侧 Markdown 正文，移动端折叠导航。
- public/tool/developer.html 与 public/tool/manuals.html 共用 public/tool/developer.css，分别由 src/tool/developer.ts 和 src/tool/manuals.ts 渲染列表及展开内容。
- public/jobskill/site.css 和 public/tool/developer.css 都在全局设置 scrollbar-width: none，并隐藏 WebKit 滚动条，这是截图中没有滚动条的直接原因。
- 工具首页已使用新拟物双主题、共享 public/theme.js 和默认白天模式；当前三页尚未统一接入。
- 全站共享 public/theme.css 将警告背景定义为浅黄色；培训皇帝另用 --st-warning-soft: #fff8c5 渲染签到提醒和警告状态。
- 工作技能搜索命中使用 #ffe58f；换季学习使用浅黄色作为人员选中、名单变更和部分副驾驶类别底色。
- Jobskill 的业务正文、索引和搜索规则无需修改；开发者 Skill、提交记录和用户手册数据源无需修改。
- 当前自动测试覆盖 Jobskill 内容与资产、开发者/用户手册脚本语法和部分 HTML 本地资产，不覆盖视觉像素。
- 培训皇帝页面当前由 `.page { max-width: 1180px; }` 限制宽屏画布；人员查询表单又限制为最大 480px，年度图表和 CRM 双图虽具备 `width: 100%`，但受父容器宽度及不等比分栏约束而显得没有铺满。

## 3. 失败测试

- 自动测试：共享壳层或脚本引用缺失时，HTML 资产检查应失败；Jobskill 内容索引、搜索和 Markdown 图片测试必须保持通过。
- 类型检查：新增共享页面交互存在不明确参数或浏览器全局错误时，npm.cmd run typecheck 应失败。
- 构建命令：任一本地资源路径、TypeScript 脚本或页面入口缺失时，npm.cmd run build 或 smoke test 应失败。
- 静态检查：页面应提供窄而可见的中性滚动条，不再隐藏页面或 Markdown 内部滚动条。
- 配色检查：大面积提醒、选中和警告表面应为冷中性色或薄荷色，琥珀只保留在小型状态文字和图标。
- 真实用户路径演练：工作技能可搜索和切换；开发者可切换标签、展开和复制；用户手册可展开和下载；三页可切换日夜模式。
- 失败判定：出现横向页面溢出、文字遮挡、内容无法滚动、主题切换失效、原功能入口丢失或三页视觉壳层明显不一致。

## 4. 目标

- 三页统一到工具首页的冷银白天、石墨暗夜、终端绿和琥珀状态色。
- 三页共享顶栏、品牌符号、返回入口、主题按钮、GitHub 入口和新拟物交互样式。
- 页面滚动条和 Markdown/表格内部滚动条始终可发现，不占用过多视觉空间。
- 全站警告表面、培训签到提醒、工作技能搜索高亮和换季学习选中态不再出现大面积浅黄色。
- 工作技能保留导航加正文布局；开发者和用户手册保留紧凑列表展开布局。
- 培训皇帝人员查询使用全宽搜索区、清晰人员摘要和易扫描的资质结果；年度压力、已排分布、年度人次与 CRM 图表充分使用宽屏画布。
- 代码、Jobskill SPEC、相关 smoke test 和根计划同步当前事实。

## 5. 不做范围

- 不修改任何业务 Skill、Agent Skill、培训规则或用户手册正文。
- 不修改搜索、Markdown 解析、提交记录读取、复制和下载的数据规则。
- 不统一或重构培训皇帝、换季学习等成熟工具的其他内部布局和组件，工具内部仅调整本任务明确要求的黄色背景、人员资质查询和内容铺满问题。
- 不修改培训数据解析、搜索匹配、有效期状态、年度统计和图表数据口径。
- 不引入新框架、远程字体、远程图标库或运行时网络依赖。
- 不把三个页面的内容区机械改成同一种布局。

## 6. 设计

主链路：共享主题与页面壳层 -> 各页顶栏与内容容器 -> 原有数据脚本和交互 -> 可见滚动与响应式输出。

模块边界：public/tool/support-shell.css 维护三页共用的主题 Token、顶栏、按钮、滚动条和基础排版；src/tool/support-shell.ts 维护通用主题切换；Jobskill 和开发者 CSS 只保留各自内容布局；页面 HTML 负责语义结构与资源接线。

状态归属：主题继续由 public/theme.js 和 ohmyflight.theme 本地设置管理；各页首次访问默认白天。Jobskill 当前 Skill 与搜索状态、开发者折叠状态、手册展开状态仍归原脚本管理。

错误与恢复：共享主题不可用时页面仍以白天 Token 显示；正文加载或版本读取失败沿用原错误提示；滚动条只改变表现，不改变滚动容器和数据。

警告配色：public/theme.css 提供冷中性警告表面；培训皇帝继续使用独立警告文字色但改用中性背景；换季学习的选中态改用薄荷色，类别色改为冷灰紫；Jobskill 搜索命中使用主题强调色的浅混合。

测试和文档影响：更新 HTML 资产覆盖范围和 Jobskill SPEC；配色仅改变呈现，不修改业务规则；不为具体像素、旧类名或旧 CSS 缺失编写负向测试。

## 7. 实施任务

- [x] T001 建立共享双主题壳层、主题切换与可见滚动条；文件：public/tool/support-shell.css、src/tool/support-shell.ts；验收：三页可复用且默认白天。
- [x] T002 重构工作技能页面结构与专属布局；文件：public/jobskill/index.html、public/jobskill/site.css；验收：搜索、导航和正文保持可用，桌面/窄屏无溢出。
- [x] T003 重构开发者与用户手册页面结构和共享列表样式；文件：public/tool/developer.html、public/tool/manuals.html、public/tool/developer.css；验收：标签、展开、复制、提交记录和下载入口保持可用。
- [x] T004 同步 Jobskill SPEC 与相关 smoke test；验收：文档描述当前统一壳层和可见滚动。
- [x] T005 清理共享主题、培训皇帝、工作技能和换季学习的大面积黄色背景；验收：警告和选中语义保留但不再整块发黄。
- [x] T006 优化培训皇帝人员资质查询的搜索、候选人与结果展示；验收：全宽搜索、身份摘要和资质状态清晰，查询逻辑不变。
- [x] T007 放宽培训皇帝宽屏画布并让年度压力、已排分布、年度人次和 CRM 图表铺满父容器；验收：宽屏空间有效利用，窄屏保持单列且无横向页面溢出。
- [x] T008 完成构建、类型检查、全量验证和静态结构检查；验收：所有命令通过，目标页面无已知遮挡和隐藏滚动问题。

## 8. 验证计划

- 局部测试：npx.cmd vitest run tests/smoke/jobskill.test.ts tests/smoke/html-assets-all.test.ts tests/smoke/javascript-syntax.test.ts tests/smoke/theme-assets.test.ts。
- 完整验证：npm.cmd run verify，并补跑 npm.cmd run typecheck 与 npm.cmd test 以满足仓库收尾要求。
- 页面检查：静态核对构建后的工作技能、开发者和用户手册资源接线、响应式规则、滚动条规则与文字边界约束；检查培训日历提醒、换季选中态和共享警告表面不再出现大面积浅黄。最终视觉由 owner 实机验收。
- 文档同步检查：Jobskill SPEC、页面资源与实际实现一致。
- 未验证内容：最终生产浏览器审美由 owner 人工确认。
- 剩余风险：Jobskill Markdown 通过 fetch 加载，直接使用 file:// 截图时浏览器可能阻止正文请求；此限制不等同于构建或线上静态站点故障。

## 9. 收口

目标是否完成：已完成。三类支持页面已统一为新拟物双主题壳层并恢复可见滚动条；培训皇帝人员资质查询和宽屏图表已优化；全站大面积黄底已替换为中性或冷色表面。

失败测试是否变绿：是。专项人员资质查询和重点人员导出测试通过，全量 61 个测试文件、249 个前端测试和 7 个 Python 测试通过。

改了哪些文件：共享支持页壳层，Jobskill、开发者与用户手册页面，首页品牌标记，共享主题警告色，培训查询和图表布局，以及仍含硬编码黄底的相关工具页面与配色源。

跑了哪些验证：npm.cmd run build、npm.cmd run typecheck、npm.cmd test、npm.cmd run verify、专项 Vitest、黄底和隐藏滚动条静态扫描、git diff --check。

哪些内容没有验证：未启动本地服务；最终视觉审美和真实业务文件交互由 owner 按要求人工验收。

剩余风险：不同显示器对新拟物阴影和暗夜对比度的观感存在差异，自动测试不覆盖像素级视觉判断。

是否 commit / push：owner 已批准提交并推送，提交状态以 Git 记录为准。
