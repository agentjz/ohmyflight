# 培训排班漏项检查 Plan

## 1. 需求文档

培训皇帝需要在人员资质查询上方新增独立的“排班漏项检查”，用于排班完成后的二次复核。用户导入最新培训总表后，以可调整的观察基准日检查未来 30、60 或 90 天内即将到期的培训项目；已经存在有效且能够覆盖本轮到期安排的记录不再展示，只保留已过期未妥善安排、未排和已排但不能覆盖的项目，避免把排错日期当成已处理。

完成标准是：观察基准日默认当天，30/60/90 天使用分段选择并即时刷新；检查结果按人和项目逐项展示，能够区分已过期、30 天内、31 至 60 天、61 至 90 天以及已排未覆盖；该区域位于人员资质查询之前，不改变人员查询、排班总览、模拟排班或有效期更新原有行为。

## 2. 当前事实

- 人员信息表从“熟练检查”到“体检合格证”包含 13 项查询资质，当前人员资质查询只按当天区分有效、已过期、原表文字和空值。
- 现有排班规则只覆盖应急训练、危险品、航空安保、TSA、疲劳管理、飞行作风、英语能力和汉语能力 8 个项目；只有这些项目能够可靠判断项目 sheet 中的安排是否覆盖当前到期。
- `schedule-assessment.ts` 已统一实现人员匹配、有效记录识别、模拟排班注入和 `RuleEngine.evaluatePlanCoverage` 覆盖判断，并通过公开的 `buildResult` 产生已过期、已过期已排补训、必须排、推荐排、已排未覆盖、正常和异常状态；新视图无需修改核心评估模块。
- 已找到可覆盖本轮到期的有效安排时，现有排班评估状态为正常；存在记录但不能覆盖时为已排未覆盖或已过期已排补训。
- 用户明确要求采用保险口径：只有真正覆盖本轮到期的有效安排才排除，已排未覆盖仍进入漏项检查。
- 新区域只检查上述 8 个可排班项目；其余 5 项继续由人员资质查询展示，不能在缺少排班规则时猜测是否已经安排。

## 3. 失败测试

- 逻辑测试：观察基准日前已过期且无覆盖安排、未来 0 至 30 天、31 至 60 天、61 至 90 天的未覆盖记录必须进入对应区间。
- 逻辑测试：有效且能覆盖本轮到期的安排必须从漏项结果中排除。
- 逻辑测试：存在有效记录但培训日期不能覆盖本轮到期时必须保留，并明确为已排未覆盖。
- 逻辑测试：选择 30 天时不得混入 31 天后的项目；60 天和 90 天按最大观察范围逐级包含。
- 逻辑测试：原表文字、空值、无对应排班规则的资质和自动忽略清单命中的项目不得伪造成漏项。
- 页面接线复核：导入前控件禁用；导入后默认基准日为当天、默认 30 天，并在修改日期或范围时刷新结果。
- 失败判定：已正确覆盖人员仍出现、已排未覆盖人员消失、同一人同一项目重复、日期区间错位、结果与排班总览覆盖规则不一致，或新增区域破坏人员资质查询。

## 4. 目标

- 提供可测试的排班漏项展示模型，直接消费现有排班评估结果，不修改或复制核心项目判断。
- 页面在人员资质查询上方展示观察基准日、30/60/90 天分段控件、范围摘要和紧凑漏项表格。
- 一行代表一个人和一个项目，展示状态、人员、项目、当前有效期、最晚完成日期、已排日期和检查说明。
- 已过期使用红色小型状态标识，后续区间使用逐级减弱的冷色标识，不使用黄色整行底色或单边强调条。
- 代码、测试、开发规格和用户手册同步新口径。

## 5. 不做范围

- 不给熟练检查、机长年度、120-100、型别更新和体检合格证发明排班 sheet 或覆盖规则。
- 不修改项目有效期、窗口期、取消备注、自动忽略清单、人员匹配或模拟排班口径。
- 不把漏项检查结果写回 Excel，不新增导出，不发送系统通知。
- 不合并人员资质查询与排班总览，不重构无关培训统计模块。

## 6. 设计

主链路：培训总表分析 -> 以观察基准日到基准日加 30/60/90 天运行现有排班评估 -> 只保留到期日不晚于观察截止日且未被有效安排覆盖的 8 项项目记录 -> 将结果映射为已过期、30 天内、31 至 60 天、61 至 90 天或已排未覆盖 -> 排序、统计并渲染。

保险口径：`正常`代表已有覆盖安排，必须排除；`已过期已排补训`和`已排未覆盖`均说明安排不能覆盖本轮到期，必须保留；`已过期`、`必须排`和检查范围内的`推荐排`保留；`异常`继续由排班总览处理，不伪造成日期漏项。

模块边界：新增独立 `schedule-gap-check.ts` 调用现有 `Workbench.buildWorkbench`，只负责基准日、观察天数、结果筛选、展示区间映射、去重、排序和摘要；不修改 `rule-engine.ts`、`schedule-assessment.ts`、人员匹配或覆盖算法。`app-schedule-gap-check.ts` 只维护页面控件、状态和渲染；HTML/CSS 负责语义结构和响应式表现。

状态归属：观察基准日和观察天数属于当前页面内状态；导入新工作簿后基于新分析立即重算。默认观察基准日为当天，默认窗口为 30 天。日期范围使用本地业务日期比较，基准日与截止日均包含，截止日为基准日加所选天数。

展示：摘要按涉及人员数和漏项条数分别统计；表格每行对应一个人和一个项目。状态颜色仅用于小型标签和关键日期，不给整行铺设重色。表格限制合理最大高度并保留可见滚动条。

错误与恢复：没有分析结果时展示导入提示并禁用控件；基准日无效时不计算并显示明确提示；项目缺少有效期日期时不进入漏项结果，原有健康检查和排班异常继续负责数据质量提示。

测试和文档影响：新增独立规则测试，覆盖边界日期、覆盖排除和已排未覆盖；更新培训开发规格与用户手册。UI 只测试稳定接线和可观察行为，不编写像素或旧实现缺失测试。

## 7. 实施任务

- [x] T001 基于现有公开排班评估接口实现独立漏项展示模型；验收：核心排班模块零改动，保险口径、日期区间、去重排序和摘要均有测试。
- [x] T002 在人员资质查询上方接入观察基准日、30/60/90 天分段控件及漏项结果；验收：导入前后状态和即时刷新正确。
- [x] T003 完成紧凑表格、逐级状态色、全宽与窄屏布局；验收：不使用黄色整行背景或单边强调条，内容可滚动且无页面横向溢出。
- [x] T004 同步培训开发规格和用户手册；验收：8 项范围、保险口径、日期边界和操作方法与代码一致。
- [x] T005 运行专项测试、构建、类型检查和全量测试并完成静态复核；验收：全部命令通过且无业务数据或无关文件进入变更。

## 8. 验证计划

- 局部测试：新增漏项检查测试，并回归 `person-validity-query.test.ts`、`schedule-assessment.test.ts` 和排班规则相关测试。
- 完整验证：`npm.cmd run build`、`npm.cmd run typecheck`、`npm.cmd test`、`npm.cmd run verify`。
- 静态检查：核对新脚本资源接线、元素 ID、响应式表格、可见滚动条、颜色变量和单边强调样式。
- 文档同步检查：`spec/dev/training-workbench/spec.md` 与 `spec/user/training-workbench/manual.md` 描述当前实现。
- 未验证内容：按 owner 要求不启动本地服务，最终视觉和真实业务表交互由 owner 人工验收。
- 剩余风险：真实工作簿中项目 sheet 的安排质量仍依赖现有解析、人员匹配和覆盖规则；本功能只复用并显示其判断结果。

## 9. 收口

目标是否完成：已完成。培训皇帝已新增独立排班漏项检查，复用现有排班评估结果，按观察基准日和 30/60/90 天范围显示未正确覆盖项目，不改变核心排班规则。

失败测试是否变绿：是。新增 4 项漏项检查测试覆盖日期边界、正确覆盖排除、已排未覆盖保留、去重摘要和公开工作台接口接线；相关培训回归与全量测试均通过。

改了哪些文件：新增漏项检查模型与页面模块，接入培训皇帝页面、导入和模拟排班生命周期，补充紧凑表格及响应式样式，并同步培训开发规格、用户手册和专项测试。

跑了哪些验证：`npm.cmd run build`、`npm.cmd run typecheck`、5 个培训相关测试文件、`npm.cmd test`、`npm.cmd run verify`、`git diff --check` 和静态资源接线检查均通过；全量结果为 62 个前端测试文件、253 项前端测试和 7 项 Python 测试通过。真实文件 `南货航2026年培训表.xlsx` 成功识别 411 人和 8 个排班项目，健康检查为 0 个严重、8 个警告，30/60/90 天漏项检查分别得到 3/7/32 项。

哪些内容没有验证：按 owner 要求未启动本地服务；最终视觉由 owner 人工验收。

剩余风险：结果准确性仍依赖当前 8 项已确认排班规则、源 Excel 的有效期与人员信息质量，以及项目 sheet 的日期和取消备注填写正确；没有现行覆盖规则的 5 项资质不会进入漏项检查。

是否 commit / push：未提交，未推送。
