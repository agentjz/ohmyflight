# 智能排班方案对比 Plan

## 1. 需求文档

资质管理员需要同时回答两个问题：智能算法建议怎样安排，当前人工排班安排得是否合理。智能方案继续独立读取全部有效期并进行全局优化，不能被未来人工排班月份带偏；人工排班只作为“我的方案”投影，用同一批当前轮次任务、同一项目人天和同一 CRM 固定底座与智能方案公平比较。

对比不以“某个人的实际月份是否等于智能月份”判断对错。只要人工安排合法、没有过期且整体月负载均衡，就不应标记为建议调整。页面只明确展示客观状态：已排班、未排班、已排未覆盖、晚于截止日，并同时展示实际培训日期、我的方案月份、智能建议月份和最晚完成日期。

人工方案投影口径：正确覆盖当前轮次的未来安排放在实际培训月份；未排班、已排未覆盖和晚于截止日的当前轮次任务暂放在最晚完成月份，用于完整表达尚未解决的到期压力，避免因为尚未排完而虚假显示低负载。智能方案仍放在独立算法推荐月份。三套投影必须使用相同任务总量：原始到期压力、我的方案、均衡方案。

## 2. 当前事实

已确认业务事实：

- 智能方案的硬约束仍是资质不能过期、每人每项当前轮次只安排一次；目标是在合法方案中尽量均衡月度培训人天。
- 未来人工排班必须可见，用户需要借此评价自己的方案是否已提前消化峰值。
- 人工月份和智能月份不需要逐人一致；评价重点是合法性和整体月负载形状。
- 已发生且正确覆盖上一轮的培训只用于生成下一轮有效期，不能误标成下一轮“已排班”。
- CRM 继续作为全部项目视图的固定负载底座，单项目视图不叠加 CRM。

已确认代码事实：

- `qualification-pressure.ts` 已提供当前轮次的 `scheduledDate` 和 `coverageStatus`，但 `smart-schedule.ts` 当前未保留这些字段。
- 当前智能任务只有到期月和推荐月；月度视图只有原始到期压力、均衡方案和月均参考。
- 当前明细只按智能推荐月份筛选，只展示推荐月份，无法解释人工方案。
- 明细区域已有 420px 固定高度和纵向滚动壳，继续复用，不改变为页面整体无限增长。
- 项目筛选只投影同一套全局方案，不重新求解。

仍需通过测试和真实表确认：

- `coverageStatus` 各状态在过去培训、未来正确覆盖、窗口外安排和逾期安排下的实际输出。
- 正确覆盖安排落在观察范围之外时的投影边界和说明。

## 3. 失败测试

先补充以下可观察行为测试：

- 未来合法安排标记为“已排班”，保留实际培训日期，并把我的方案投影到实际培训月份。
- 没有安排标记为“未排班”，我的方案暂投影到最晚完成月份。
- 窗口外安排标记为“已排未覆盖”，不能算作已排班；晚于截止日的安排标记为“晚于截止日”。
- 已发生且覆盖当前轮次后生成的下一轮任务标记为“未排班”，不能沿用历史培训日期。
- 原始到期压力、我的方案和均衡方案使用同一批任务，三者资质任务总人天一致。
- 修改未来人工排班月份只改变我的方案月份，不改变独立智能推荐月份。
- CRM 仅在全部项目视图同时叠加到三套投影，单项目视图不叠加。
- 项目筛选只投影，不重算全局推荐。

真实表回放继续确认：

- 使用 `C:\Users\Administrator\Desktop\南货航2026年培训表.xlsx` 回放相同参数，三套投影的资质任务总人天一致。
- 抽查已排班、未排班、已排未覆盖和晚于截止日状态及对应日期；真实 Excel 和临时脚本不得提交。

## 4. 目标

用户导入培训表后，在同一张图上看到“原始到期压力”“我的方案”“均衡方案”和月均参考。图形直接说明当前人工排班是否削平了未来到期峰值，以及与独立智能方案相比还有多少整体差距。

点击任一月份，固定高度明细展示与该月原始到期、我的方案或智能方案相关的任务，逐项列出客观排班状态、实际培训日期、我的方案月份、智能建议月份、最晚完成日期和人天。

完成判定：专项测试、真实表回放、构建、类型检查、全量测试、确定性验证、依赖审计和静态检查全部通过；代码、开发 spec、用户手册和 training-workbench skill 口径一致。

## 5. 不做范围

- 不让未来人工排班参与或约束智能优化器。
- 不按个人月份差异生成“建议调整”或判定人工方案错误。
- 不新增具体培训日、教员、场地、班级、合班人数和人员冲突约束。
- 不为了均衡增加重复培训，不生成当前轮次之外的额外培训。
- 不改变现有效期规则、合法月份、安全提前量、避让月份和优化优先级。
- 不写回 Excel。

## 6. 设计

任务状态：

- `已排班`：当前轮次存在尚未发生且正确覆盖的培训安排；保留实际日期，我的方案月份取实际培训月。
- `未排班`：当前轮次没有有效未来安排；我的方案月份暂取最晚完成月。
- `已排未覆盖`：存在安排但不覆盖当前规则窗口；保留实际日期，我的方案月份暂取最晚完成月。
- `晚于截止日`：安排晚于当前轮次截止日；保留实际日期，我的方案月份暂取最晚完成月。
- 已发生并覆盖旧轮次的历史培训用于计算下一轮事实；新轮次没有未来安排时状态必须是“未排班”。

三套投影：

- 原始到期压力：同一批可安排任务投影到最晚完成月份。
- 我的方案：已排班任务投影到实际培训月份；其余任务投影到最晚完成月份。
- 均衡方案：同一批任务投影到独立算法推荐月份。
- 全部项目视图给三套投影叠加完全相同的 CRM 固定底座；单项目视图均不叠加。
- 若正确覆盖的实际月份不在观察范围，保留状态和实际日期，但为保证图表总量可比，将我的方案暂投影到最晚完成月份，并在说明中明确实际月份不在观察范围。

页面：

- 图表增加“我的方案”系列，不增加汇总数字卡片。
- 月份点击条件为到期月份、我的方案月份或智能月份任一匹配。
- 明细保持现有固定高度纵向滚动；列为项目、姓名、员工号、排班状态、已排日期、我的方案月份、智能月份、最晚完成、人天、说明。
- 使用现有状态 badge，已排班为正常，未排班为提醒，未覆盖和逾期为危险。

模块职责：

- `smart-schedule.ts` 负责保留覆盖事实、派生人工状态、生成三套同源投影。
- `smart-schedule-optimizer.ts` 保持独立求解，不读取人工排班状态。
- `app-training-capacity.ts` 负责图表接线、月份关联筛选和明细渲染。
- `app-charts.ts` 负责增加“我的方案”系列。
- `index.html` 与 `dashboard.css` 只调整表头和必要的列宽，不改变滚动结构。

## 7. 实施任务

- [x] T001 重写计划，固化独立求解、同源对比、状态口径和不按个人月份判错的边界。
- [x] T002 已补人工状态、三套投影、独立求解和 CRM 口径测试；旧实现运行 19 个场景时 6 个失败，失败点均为本次新增行为。
- [x] T003 已扩展智能任务模型，保留覆盖事实、模拟排班输入并生成“我的方案”投影，优化器接口不变。
- [x] T004 已重接图表、月份选择和固定高度对比明细。
- [x] T005 已同步开发 spec、模拟排班 spec、用户手册和 training-workbench skill。
- [x] T006 已使用真实培训表回放：411 人、1394 人项、1393 项可安排；人工状态为已排班 106、未排班 1264、已排未覆盖 23、晚于截止日 1；三套资质投影均为 1564 人天，人工峰值 261、均衡峰值 131，临时脚本已删除。
- [x] T007 已运行专项测试、构建、类型检查、全量测试、确定性验证和依赖审计；静态检查在提交前完成。
- [ ] T008 确认暂存范围，使用简体中文提交并推送。

## 8. 验证计划

局部验证：

- `npx.cmd vitest run tests/tool/training-workbench/smart-schedule.test.ts`
- 按影响联跑 `qualification-pressure.test.ts`、`training-load.test.ts` 和培训工作台构建测试。

完整验证：

- `npm.cmd run build`
- `npm.cmd run typecheck`
- `npm.cmd test`
- `npm.cmd run verify`
- `npm.cmd audit --omit=dev`

静态检查：

- `git diff --check`
- 检查修改文件 UTF-8 无 BOM。
- 搜索旧图表口径、旧明细标题和临时文件。
- 确认真实 Excel、回放脚本和构建临时文件未进入提交。

页面检查：

- 自动验证页面元素、事件接线和构建资源完整；最终视觉效果按仓库约定由 owner 人工检查。
- 明细固定高度、纵向滚动，常见宽度下表头和内容不重叠。

## 9. 收口

目标是否完成：代码、测试、文档和自动化验收已完成，等待提交推送。

失败测试是否变绿：智能排班专项测试共 19 项，全部通过。

改了哪些文件：已扩展智能排班任务与三套投影，重接图表和对比明细，并同步测试、开发 spec、模拟排班 spec、用户手册、skill 与本计划。

跑了哪些验证：相关测试 23 项通过；真实表回放通过；`npm.cmd run build`、`npm.cmd run typecheck`、`npm.cmd test`、`npm.cmd run verify` 和 `npm.cmd audit --omit=dev` 全部通过。全量为 68 个测试文件、282 个 TypeScript/JavaScript 测试、9 个 Python 测试，确定性构建核对 173 个文件，生产依赖 0 个漏洞。

哪些内容没有验证：页面最终视觉效果未自动验证，按仓库约定由 owner 人工确认。

剩余风险：页面最终视觉仍由 owner 人工确认。

是否 commit / push：尚未提交或推送。
