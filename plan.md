# ohmyflight 原生 ESM、类型契约、性能与长期交付重构 Plan

## 1. 需求文档

用户要解决什么实际问题：

- 将当前依靠 HTML 脚本顺序和 `window.*` 命名空间装配的浏览器工具源码，断裂式重构为标准、显式、可静态检查的模块体系。
- 在不引入运行时应用框架和后端服务的前提下，提高未来 10 至 30 年的可维护性、扩展性、构建恢复能力和运行性能。
- 消除为了兼容旧内部结构而保留的双轨装配、全局别名和空壳包装；新实现应真正接管依赖、状态、构建、测试和页面启动。
- 性能优化必须以固定输入、浏览器指标和构建产物数据为依据，既优化首屏装载，也关注大型 Excel、PDF、Word 处理过程中的主线程耗时、内存增长和重复计算。
- 核心业务逻辑是本次重构的绝对不可变边界：所有解析口径、字段映射、规范化规则、匹配算法、判断分支、日期计算、过滤清单、统计口径、排序去重、状态定义、错误语义、导出数据与文档处理结果均不得改变。
- 断裂式重构只允许断开旧的模块装配、全局命名空间、构建和测试加载协议；不允许借机“改良”、重写、合并或替换任何已经工作的核心业务算法。

谁会使用：

- 项目 owner 是当前主要开发者和使用者，并使用 AI 参与长期维护。
- 最终用户继续通过浏览器本地页面或 GitHub Pages 使用各个独立工具。

用户完成任务时应该看到什么体验：

- 工具入口、业务功能、输入输出和操作路径保持当前已确认事实，不要求用户理解模块系统变化。
- 页面启动无脚本顺序错误、缺失全局对象错误和控制台异常。
- 重型工具在相同输入下保持结果一致，并在已建立的固定基准上不发生可确认的性能回退。
- 每个浏览器工具仍可单独打开、单独维护、单独排错，不被改造成全仓 SPA。

当前范围包含：

- `src/` 下浏览器 TypeScript、浏览器全局类型和站点支持页面逻辑。
- `public/` 下 HTML 脚本入口、静态第三方库接线和构建需要的静态资源。
- `scripts/build.mts`、TypeScript 配置、npm 脚本、测试辅助设施和 CI 验证流程。
- 浏览器工具的单入口 ESM 源码、按页面生产构建、类型契约、单元测试、浏览器启动测试和性能基准。
- 第三方浏览器库版本、来源、许可证和文件覆盖记录。
- 与架构、构建、运行和维护方式有关的 `spec/dev/`、`spec/user/`、README 或贡献说明。
- 培训皇帝、审计之王、校对之王、换季学习及全部轻型浏览器工具；Python 下载型工具只处理页面接线和发布边界。

当前范围不包含：

- 新增业务功能、修改业务口径或重做页面视觉设计。
- 把本地工具迁移为 Java、Node 或其他中心化后端服务。
- 重写独立分发的 Python 自动化业务逻辑。

业务上怎样算完成：

- 当前 spec、用户手册和测试定义的业务结果、导出结构、错误语义和用户路径全部保留。
- 同一输入在重构前后必须得到深度一致的结构化结果、排序、状态、提示、异常类型和导出内容；涉及二进制文件时比较规范化后的工作簿、文档模型、页数、字段、单元格和其他业务可观察内容。
- 浏览器内部模块不再依赖 `window.TrainingTool`、`window.AuditKing`、`window.ManualProof` 等全局注册表传递内部依赖。
- 所有浏览器实现页面都由明确的 ESM 入口启动，生产构建不再依赖手写的应用脚本加载顺序。
- 类型、单元、Python、静态资源、浏览器启动、确定性构建和性能验证全部达到本计划验收条件。

## 2. 基线事实与完成证据

已确认代码事实（重构前基线）：

- 重构前仓库登记 20 个工具，`src/tool/app/` 下有 18 个工具源码目录；owner 已明确下线原 `crew-extract-id` 工具，当前登记 19 个工具。
- `src/` 当前有 196 个 TypeScript 文件，约 28562 行；其中 `src/tool/app/` 有 178 个 TypeScript 文件。
- 重型工具源码规模为：`training-workbench` 44 个 TypeScript 文件约 7880 行，`audit-king` 29 个约 4229 行，`proof-king` 21 个约 3349 行，`seasonal-learning` 11 个约 2211 行。
- `scripts/build.mts` 当前使用 `ts.transpileModule` 和 `ts.ModuleKind.None`，逐文件输出普通脚本，不生成标准模块依赖图，也不打包应用源码。
- `src/` 当前没有使用 `import` 或 `export` 的 TypeScript 源文件。
- `src/tool/app/` 中有 133 个 TypeScript 文件引用 `window.*`，64 个文件直接引用 `document.*`。
- 当前主要内部全局命名空间包括 `window.TrainingTool`、`window.TrainingToolApp`、`window.AuditKing`、`window.ManualProof`、`window.SeasonalLearning*` 等。
- 培训皇帝主页当前显式加载 44 个脚本，审计之王加载 37 个脚本，校对之王加载 24 个脚本；这些标签同时承担依赖顺序。
- 现有大型工具已经区分规则、解析、状态、导出、视图和页面接线，业务模块边界应保留，不从零重新发明业务架构。
- `tsconfig.json` 已启用 `strict: true`，但为当前动态浏览器模块保留 `noImplicitAny: false`。
- `src/tool/app/` 有 43 个 TypeScript 文件包含 `any`、`Record<string, any>` 或 `as any`；动态第三方边界和稳定内部业务契约尚未完全区分。
- 浏览器依赖本地托管；Bootstrap 5.3.3 已作为 UI 基础使用，项目并非完全没有 UI 框架。
- `start_index.py` 通过本地 HTTP 服务运行 `dist/`，GitHub Actions 通过 GitHub Pages 部署；当前运行方式不要求直接通过 `file://` 双击页面。
- `dist/` 是未跟踪构建产物，正式部署由 CI 构建产生。
- `requirements.txt` 已精确固定 Python 包版本；`package-lock.json` 已锁定 npm 依赖。

已确认测试事实（重构前基线）：

- 2026-08-01 只读评估中，`npm.cmd run typecheck` 通过。
- 同次评估中，`npx.cmd vitest run` 的 62 个测试文件、253 项测试全部通过。
- 同次评估中，Python `unittest` 的 7 项测试全部通过。
- 测试已经覆盖培训、审计、校对、换季、Excel 解析、业务规则、导出、静态资源和仓库结构等关键区域。
- Vitest 当前使用 Node 环境；现有 smoke 测试检查脚本语法、静态资源存在性和 Worker 文件，但不真实启动所有浏览器页面并检查控制台、DOM 初始化和关键交互。
- 部分单元测试通过 `loadBrowserScripts` 将构建后的普通脚本载入 VM 全局上下文，不是直接导入源码模块。

已确认文档/spec 事实（重构前基线）：

- 重构前 20 个工具均有开发 spec 和用户手册，仓库测试会检查其存在性；下线 `crew-extract-id` 后，当前 19 个工具的 spec 和用户手册保持完整。
- 培训皇帝当前五个可见入口、业务规则、模拟排班边界、Excel 结构和导出列已由 `spec/dev/training-workbench/` 与用户手册明确记录。
- 培训皇帝的 `rule-engine.ts`、`schedule-assessment.ts`、`workbench.ts`、独立统计模块和 `app-*.ts` 已有明确职责；不能因为迁移模块系统而合并不同业务口径或把规则塞回页面层。

已确认配置或入口事实（重构前基线）：

- npm 统一入口包括 `build`、`typecheck`、`test` 和 `verify`。
- CI 当前使用 Node 22、Python 3.12，先执行全量验证，再运行 `npm audit --audit-level=high` 并部署 Pages。
- `public/libs/versions.json` 已记录浏览器第三方库文件、版本和来源，测试会验证清单覆盖所有托管文件。
- `pica.min.js` 的清单版本当前仍记为“本地文件未标注”。

已确认命令输出：

- 2026-08-01 的 `npm.cmd audit --json` 对 npm 依赖报告 0 个已知漏洞；该结果不覆盖 `public/libs/` 中手工托管的浏览器文件。
- 计划创建前 `git status --short` 无输出，工作区干净，根目录不存在 `plan.md`。
- 2026-08-02 实施开始时位于 `master` 分支、提交 `89705a6a685dba5d5a04590060215c56a2ca602e`；除本计划外没有未提交改动。
- 2026-08-02 当前稳定实现执行 `npm.cmd run build`、`npm.cmd run typecheck` 和 `npm.cmd test` 均通过；构建输出 181 个脚本，Vitest 为 62 个文件 253 项，Python unittest 为 7 项。
- 当前基线环境为 Windows 11 10.0.26200、Node 22.19.0、npm 10.9.3、Python 3.12.10、Playwright 1.58.0、Chromium 145.0.7632.6。
- owner 提供的本地培训回放文件 `C:\Users\Administrator\Desktop\南货航2026年培训表.xlsx` 已确认存在，大小 277348 字节；该真实业务文件只用于本地回放，不进入仓库。
- owner 提供的本地换季回放文件 `C:\Users\Administrator\Desktop\9.15-9.30换季学习.xlsx` 已确认存在，大小 60304 字节；该真实业务文件只用于本地回放，不进入仓库。
- 审计之王已由 37 个页面脚本迁为第三方资源加单一 ESM 应用入口；13 个审计测试文件及共享项目归档共 14 个文件、68 项测试通过，固定浏览器工作流结果 SHA-256 仍为 `123d95246be4bfb6ca84baf8090085de514468ea159485553f13ef684fdeacbd`，且无控制台、页面或 HTTP 错误。
- 迁移后固定浏览器最终正式采样中，培训、换季、审计、校对工作流中位数分别为 951.88、212.30、183.99、261.74 ms，均在回写预算内；四项结果哈希与迁移前金样完全一致。
- 重型页面预热一次并在每轮正式采样前回收可达堆后，连续五轮工作流的末次相对首次堆增长分别为 1141060、969198、233228、229704 字节，均在 35% 或 10 MiB 的允许上限内，无持续增长证据；未因此修改任何核心或生产逻辑。

重构前已有能力：

- 静态、本地优先、按工具独立部署。
- 核心业务逻辑和 UI 接线整体分离。
- TypeScript 类型检查、Vitest、Python 测试、构建和 CI 闭环。
- 第三方资源本地托管，不依赖生产时 CDN。
- Worker 和 Blob URL 等大对象生命周期已有部分主动清理实现。

重构前缺口：

- 内部依赖由 HTML 顺序和 `window.*` 隐式表达，编译器不能完整验证装配关系。
- 稳定内部模块的部分 API 退化为 `any`，成员拼写、返回结构和调用约束存在类型盲区。
- 缺少真实浏览器启动和控制台错误闭环。
- 缺少固定性能基线，当前不能基于事实声称某项重构更快或达到“极致性能”。
- 当前应用源码未按页面打包，重型页面存在大量应用自有脚本请求和重复解析开销。
- 构建生成的 `version.json` 包含当前构建时间和分支信息，同一提交重复构建不能保证内容哈希一致。
- 浏览器第三方库清单尚未包含完整性哈希和统一许可证字段，npm 漏洞审计不覆盖这些文件。

仍不明确：

- owner 最终要求支持的最低 Edge/Chrome 版本和 Windows 版本。
- 可公开用于性能回放的脱敏 Excel、PDF、Word 样例规模；真实业务文件不得提交。
- 当前各重型页面在固定机器上的冷启动、处理耗时、峰值内存和重复操作后内存基线。
- `public/libs/` 各文件是否全部能追溯到确切上游发布包及其许可证文本。

## 3. 失败测试

自动测试：

- 在任何核心模块改动前增加特征测试和金样：用当前稳定实现运行边界样例、回归样例与结构等价合成夹具，固化输入、结构化输出、排序、状态、错误和导出模型；迁移后必须逐项完全一致。
- 核心测试的期望值不得为了让新实现通过而修改。只允许修改导入路径、测试装载方式和经 owner 明确确认原测试事实错误的内容；后者必须先更新 spec 并单独说明。
- 为纯业务模块编写直接 `import` 源码的测试；当前无导出模块，测试应先失败，再随 ESM 迁移变绿。
- 为所有浏览器实现页面增加真实浏览器启动测试，要求页面完成初始化且无 `pageerror`、资源 404 和非白名单控制台错误；现有测试设施无法给出该保证。
- 为每个重型工具增加至少一条代表性本地工作流浏览器测试，验证上传后的核心结果区域、导出准备状态或后台 Worker 完成状态；没有可提交样例时使用结构等价的合成夹具。
- 为生产构建增加两次独立构建内容哈希对比；当前 `version.json` 的构建时间和分支字段会导致失败。
- 为第三方资源清单增加精确版本、上游来源、许可证和文件覆盖校验；当前不完整条目应先失败。

类型检查：

- 在不批量补 `any` 的前提下，将稳定浏览器源码纳入 `noImplicitAny: true`；当前配置和部分源码应先失败。
- 删除内部全局命名空间类型后，所有业务、应用、视图和入口依赖必须通过显式导入获得类型；缺失导出、错误成员名和错误返回结构必须导致类型检查失败。
- 动态 Excel、图表、PDF、Word 和第三方库边界可保留经过说明的 `unknown`、窄化函数或必要 `any`，但不能把稳定业务模型整体声明为 `Record<string, any>`。

构建命令：

- 新构建必须从明确入口生成每页应用包和构建清单；入口缺失、循环初始化导致的未定义值、重复输出、未解析内部依赖或静态资源缺失必须失败。
- 构建不得从 HTML 标签顺序推断内部业务依赖。

静态检查：

- 生产 HTML 的应用入口必须来自构建清单，第三方资源与应用包职责明确；不为旧全局装配保留重复脚本链。
- 静态检查验证每个工具目录的页面、应用入口或 Python 资产完整，但不以“旧字符串不存在”代替可观察的运行测试。

真实用户路径演练：

- 首页进入各工具、页面初始化、上传文件、生成结果、筛选、导出、清空或重新导入等当前路径应保持可用。
- 培训皇帝必须回放五个可见入口，并核对排班漏项、资质查询、月历、排班总览和有效期更新边界。
- 审计之王、校对之王的文档读取、Worker、项目导入导出和报告路径不得因模块化或延迟加载失效。

性能失败判定：

- 固定环境为 Windows 11 10.0.26200、32 逻辑处理器、Playwright 1.58.0、Chromium 145.0.7632.6；每项预热 1 次，以全新浏览器 context 正式采样 3 次并记录中位数、P95、最小值和最大值。
- 冷启动 wall time 基线/上限分别为：培训 233.98/333.98 ms、换季 112.37/212.37 ms、审计 186.01/286.01 ms、校对 169.60/269.60 ms。上限使用基线中位数加 25% 与 100 ms 两者较大值。
- 固定工作流中位数/上限分别为：培训真实总表 939.20/1174.00 ms、换季真实表 203.35/303.35 ms、审计生成 DOCX 173.77/273.77 ms、校对生成 DOCX 对 241.07/341.07 ms。
- 固定工作流结果 SHA-256 必须与 `spec/dev/esm-delivery/legacy-performance.json` 完全一致；任何 console error、pageerror 或 HTTP 失败均判定失败。
- 同页连续 5 次固定工作流后，末次 JS 堆不得超过首次的 135% 或增加 10 MiB 两者中的较宽上限，并且不得存在持续增长的 Worker、Blob URL 或事件监听器证据。迁移前堆增长为：培训 0.62 MiB、换季 1.51 MiB、审计 7.60 MiB、校对 0.68 MiB。
- 浏览器实现页面的应用自有生产脚本目标为每页单入口单包；第三方共享库、Worker 和必要动态资源不计入应用包数量。
- 同一固定夹具的核心计算结果必须一致；中位耗时不得出现超过约定噪声区间的回退。
- 连续执行至少 5 次“导入或运行 -> 清空或替换输入”后，不得出现可复现的无界 Worker、Blob URL、事件监听器或 JS 堆增长。
- 优化不能通过删除结果、减少校验、改变业务口径、降低导出完整性或跳过错误处理换取速度。
- 性能优化不得修改核心函数的算法、条件、常量、计算顺序和输出；允许优化构建、网络装载、模块初始化、无效重复调用、DOM 批量渲染、Worker 调度和资源释放，但调用核心逻辑时必须使用相同输入并取得相同结果。

失败判定：

- 任一当前业务测试、spec 事实、用户手册路径、导出结构或浏览器入口发生未确认变化。
- 任一核心逻辑的判断条件、业务常量、字段读取、默认值、日期边界、过滤项、排序去重、错误分支或输出内容在没有 owner 明确新业务事实的情况下发生变化。
- 迁移后结果与迁移前金样不一致，或者通过修改、删除、放宽旧断言让不一致结果通过。
- 仍通过兼容别名、全局代理或包装层让旧 `window.*` 内部模块继续承担真实实现。
- 只把旧脚本机械拼成一个文件，但源码仍无显式依赖、类型盲区和状态边界没有改善。
- 只为提高覆盖率修改测试或伪造成功状态，没有真实运行证据。
- 全量验证、确定性构建、浏览器启动或性能验收任一未完成却声称任务完成。

## 4. 目标

用户路径完成到：

- 所有当前工具入口、页面行为、文件处理、结果展示和导出路径保持可用。
- 页面不暴露模块迁移细节，不要求用户安装前端运行时或连接后端。

代码主链路接到：

- 源码统一使用标准 ESM `import/export` 表达内部依赖。
- 每个浏览器实现页面有一个明确应用入口；生产输出按页面打包应用源码，第三方本地库作为受控外部边界共享。
- 纯规则和数据模块不访问 DOM；应用层组织用例；适配层负责第三方库、文件、Worker 和浏览器 API；视图层负责 DOM；入口只负责创建依赖并启动。
- 内部模块不再通过 `window.*` 注册和查找实现。`window` 只用于真实 Web API、受控第三方全局和明确必须公开的跨页面宿主契约。

状态和记录落到：

- 页面可变状态归属于每个应用实例的明确类型，不挂在全局命名空间。
- Worker、Blob URL、图表实例、事件监听器和大文件引用有明确创建、替换和释放路径。
- 构建输出生成可机读清单，记录页面入口、输出文件、哈希、依赖版本和构建源码提交。

输出呈现到：

- 生产页面只加载所需共享资源和一个应用入口包；重型功能按真实性能证据决定是否延迟初始化或拆为独立 Worker，不盲目切块。
- 生产包可压缩并生成 source map；第三方大型库不得重复打入每个工具包。
- 同一提交和固定工具链重复构建得到内容一致的核心产物。

测试和文档同步到：

- 纯逻辑测试直接导入源码模块。
- 新增全页面浏览器启动测试、重型关键路径测试、确定性构建检查、第三方资源清单检查和独立性能基准命令。
- 更新开发规格、用户手册、README、贡献指南和构建说明中受影响的当前事实，不写迁移历史。

完成判定：

- 本计划 T001 至 T020 全部完成并逐项更新状态。
- `npm.cmd run verify`、浏览器测试、确定性构建检查、第三方资源清单检查和性能验收全部通过。
- 旧内部全局装配和多脚本应用链已被真实删除，没有兼容壳继续承载实现。
- 核心业务特征测试与金样逐项一致，执行者已按工具审查核心文件 diff，并确认除模块导入导出、类型标注和不改变运行语义的机械调整外没有业务逻辑变化。
- 工作区只包含本任务相关改动，未提交真实业务数据、临时性能文件或浏览器输出。

## 5. 不做范围

不做：

- 不引入 React、Vue、Angular、Svelte、Lit 或自研虚拟 DOM/组件框架。
- 不新增 Java、Node 常驻服务、数据库、认证中心或后端 API。
- 不把独立工具合并成 SPA，不增加全仓路由器和全局状态仓库。
- 不借架构重构新增业务需求、调整业务规则或重新设计 UI。
- 不重写、简化、合并、抽象替换或“顺手修正”核心业务逻辑；即使发现看似可优化或可疑的规则，也只记录事实并停止该项，未经 owner 确认不得修改。
- 不为了展示工程化增加无实际维护收益的分层、代码生成器、装饰器、依赖注入框架或复杂配置。
- 不在没有性能剖析证据时做微优化，不把代码体积或文件行数当作唯一性能指标。

不保留：

- 不保留 `window.TrainingTool`、`window.AuditKing`、`window.ManualProof` 等内部模块注册表的兼容别名。
- 不保留“新 ESM 外壳调用旧全局实现”的双轨结构。
- 不保留仅为过渡存在、任务收口后没有调用者的桥接文件、重复入口和旧脚本标签。

不兼容：

- 不保证旧的内部全局 API、脚本加载顺序、测试 VM 装载协议和源码文件路径兼容。
- 不保证未写入 spec、用户手册、测试或当前页面的隐含开发接口兼容。
- 必须兼容当前已确认的用户行为、输入输出、业务规则、错误语义和独立部署方式。
- “不兼容旧内部协议”不等于允许改变业务结果；内部可以全部断开重接，核心逻辑和一切业务可观察结果必须保持完全一致。

不处理：

- 不拆分或重构 Python 自动化脚本的业务实现。
- 不提交真实 Excel、PDF、Word、浏览器配置、性能原始临时文件或业务数据。
- 不创建新分支；按 owner 要求直接在当前分支工作。
- 未经 owner 另行要求，不 commit、不 push、不发布生产站点。

## 6. 设计

主链路：

源码入口 -> 显式 ESM 依赖图 -> TypeScript 全量检查 -> 按页面生产打包 -> 静态资源复制与生成 -> 构建清单和完整性记录 -> Node 单元测试 -> Python 测试 -> 真实浏览器启动与关键路径测试 -> 性能与内存基准 -> 静态部署产物

输入 -> 判断 -> 状态 -> 执行 -> 输出 -> 记录：

- 输入：页面事件、用户文件、URL/页面配置和本地第三方库。
- 判断：纯业务模块解析、规范化、计算和校验，不读取 DOM。
- 状态：每个工具由 typed app context 或 app state 持有，明确区分业务结果、UI 选择、异步任务和待导出内容。
- 执行：应用控制器调用业务模块和适配器，负责中断、错误、Worker 与资源生命周期。
- 输出：视图模块渲染 DOM，导出适配器生成文件，入口反馈状态。
- 记录：测试断言、构建清单、第三方资源清单和性能摘要形成可复核证据。

模块边界：

- 核心业务模块是受保护层。本次只允许增加或调整 `import`、`export`、类型声明和依赖传入方式；不得改变其可执行判断、常量、数据转换、排序、错误和返回内容。确需改变运行语义时必须停止执行并向 owner 单独确认，不得继续自动完成。
- `domain` 或现有规则/逻辑模块：稳定业务模型、解析后的结构、规则计算、筛选、统计和导出数据模型；不得依赖 DOM。
- `application` 或现有 actions/controller：组织用户用例和状态变化，不直接实现业务规则。
- `adapters`：XLSX、ECharts、PDF、Word、Worker、文件下载、浏览器存储和其他外部边界；以窄接口隔离第三方全局。
- `view`：DOM 查询、渲染、事件绑定和可访问状态；不计算业务规则。
- `main`：页面唯一组合根，导入依赖、创建上下文、绑定页面并启动。
- 不要求为每个工具机械创建同名目录；优先保留已成熟的文件职责，只在依赖方向或状态边界确实混乱时调整。

文件职责：

- 现有纯逻辑文件改为显式导出其函数、常量和稳定类型。
- 现有 `runtime.d.ts`、`app-models.d.ts` 中稳定模型迁入可导入类型模块；仅外部全局声明保留在环境 `.d.ts`。
- 现有 IIFE 和 `window.Namespace.Member = ...` 改为模块导出；页面入口直接导入所需 API。
- `scripts/build.mts` 负责静态文件复制、入口发现/校验、应用源码打包、生成数据、版本和构建清单，不承载业务规则。
- 生产打包采用轻量、可替换的构建期工具，优先选择固定版本的 esbuild；源码始终保持标准 ESM，不使用构建器私有模块语法。
- 每个页面应用源码生成一个生产入口包；第三方本地库保持外部共享，避免在多个工具中重复打包。
- Word 模板填充器生成的独立 HTML 必须继续自包含；其生成器源码可 ESM 化，生成物内部运行时按独立文件事实设计，不强迫依赖站点模块。

状态归属：

- 每个工具拥有独立 `AppState` 和 `AppContext` 类型；状态由入口创建并显式传给控制器、视图和适配器。
- 不引入全仓 store，也不把所有状态塞入一个通用抽象。
- 可变状态只放生命周期确实一致的数据；规则配置、静态清单和不可变结果保持只读。
- 培训皇帝的排班总览、CRM、年度统计、月历、模拟排班、资质查询和有效期更新继续保持不同口径，不因共享页面状态而合并。

数据或事件流：

- DOM 事件进入应用控制器，控制器读取当前输入并调用纯模块。
- 纯模块返回结构化结果或明确错误，不直接修改页面。
- 控制器原子更新应用状态，再调用视图渲染和按钮状态刷新。
- 文件和 Worker 适配器通过 Promise、AbortSignal 或明确请求 ID 返回；过期任务结果不得覆盖新输入状态。
- 导出从已确认的当前结果模型生成，不重新从 DOM 反向解析业务数据。

错误、恢复、中断、重试边界：

- 页面入口缺少必需 DOM 或第三方库时立即给出带工具名和依赖名的错误，并在浏览器测试中失败。
- 新文件导入或新任务开始时终止旧 Worker、撤销旧 Blob URL、释放图表/预览对象并使旧异步响应失效。
- 大文件读取和重计算优先使用已有 Worker；新增 Worker 必须由剖析证明主线程存在可观察阻塞。
- 用户可恢复错误显示在页面状态区；程序错误保留 console 证据并由浏览器测试捕获。
- 不隐藏异常、不吞掉失败、不用默认空结果伪装成功。

性能设计：

- 先测量再优化：记录页面脚本请求数、应用包原始/压缩体积、冷启动时间、关键任务中位耗时、长任务数量、峰值堆和重复操作后堆趋势。
- 构建层按页面打包、压缩并输出 source map，直接消除重型页面几十个应用脚本的请求和解析调度。
- 第三方大库继续按工具按需加载，不把 XLSX、ECharts、PDF、Word 等重复嵌入每个应用包。
- 对重复调用、重复模块初始化、重复第三方对象创建、重复 DOM 全量重绘和未释放资源进行浏览器剖析；只优化核心业务函数外部被证实的热点。
- 核心业务算法保持原样；性能提升优先来自按页打包、减少应用请求、避免对相同不可变输入重复调用核心函数、复用核心函数已经产生的结果、减少应用层数据复制、DOM 批量更新、Worker 调度和资源释放。
- 延迟加载必须以首屏收益和交互代价为证据，不能为了“代码拆分”重新制造脚本顺序和运行时装配问题。
- 性能基准独立于普通单元测试运行，避免把设备噪声引入业务测试；结构性预算和明显回退可以进入 CI。

可复现交付设计：

- 固定 Node、Python、npm 和构建依赖版本，增加仓库可机读运行时版本声明。
- 同一提交、相同工具链的两次构建应生成内容哈希一致的核心产物；版本信息使用提交事实或 `SOURCE_DATE_EPOCH`，不写入不可复现的本地分支和当前时间。
- 构建清单记录每个页面入口、输出文件、内容哈希和外部第三方资源。
- 第三方资源清单补充精确版本、来源发布地址和许可证，并确保清单恰好覆盖本地受管文件；无法确认版本的文件必须确认、替换或明确阻断收口。
- 发布产物包含可直接运行的 `dist`、source map、版本清单和恢复说明；源码、锁文件和测试继续由仓库保存。

与现有架构的关系：

- 保留静态、本地优先、按工具独立、业务逻辑与页面分离、代码/测试/spec 同步等现有优点。
- 断裂式替换内部装配、类型契约、构建输出和测试加载方式，不维护旧内部协议。
- Bootstrap 和已确认必要的浏览器库继续本地托管；是否升级第三方版本必须单独验证，不作为模块化重构的默认动作。

测试和文档影响：

- 业务测试从 VM 全局脚本装载迁到源码模块导入，但断言业务事实不变。
- 重构前先扩充特征测试和金样，重构后只替换装载机制；不得以架构变化为由修改业务期望值。
- 构建和 smoke 测试改为验证构建清单、真实页面启动和可观察行为，不依赖旧目录字符串。
- 新增浏览器和性能测试说明；用户手册只在运行入口或可见行为确实变化时更新。
- 每进入培训、Excel、手册比对等专项工具前，执行者必须读取 `AGENTS.md` 要求的相应 skill、spec 和用户手册。

## 7. 实施任务

- [x] T001 建立不可变业务与性能基线；文件/模块：根目录计划、全工具核心模块清单、构建统计脚本、测试夹具和性能记录；依赖：当前稳定代码；验收：逐工具列出受保护的解析、规则、匹配、统计、状态、导出和文档处理 API，生成重构前特征金样，同时记录页面入口、应用脚本数、产物体积、固定浏览器/设备、重型工具固定夹具、冷启动/关键任务/内存基线，并将正式性能预算回写本计划。
- [x] T002 先补失败测试；文件/模块：`tests/helpers/`、`tests/smoke/`、各工具核心测试和重型工具金样；依赖：T001；验收：当前实现的边界、回归、错误、排序和导出模型被固化；源码直接导入、确定性构建、浏览器启动、第三方完整性中当前缺口可复现失败；所有原业务断言保持绿色且未经放宽。
- [x] T003 建立 ESM 构建主链路；文件/模块：`scripts/build.mts`、构建配置、`package.json`、锁文件；依赖：T002；验收：固定版本 esbuild 按明确页面入口构建标准 ESM 源码，每页输出单一应用包、source map 和构建清单，静态资源及 Python 资产继续正确复制。
- [x] T004 收紧 TypeScript 契约；文件/模块：`tsconfig.json`、环境 `.d.ts`、共享类型；依赖：T003；验收：稳定浏览器源码启用 `noImplicitAny`，动态边界显式标注并可解释，不用批量 `any` 消除错误。
- [x] T005 迁移站点公共入口；文件/模块：`src/tool/`、`src/jobskill/`、`src/site-visibility.ts`、对应 HTML 和生成数据；依赖：T003-T004；验收：首页、工具目录、手册、开发页和 Jobskill 使用明确 ESM 入口，不再依赖内部全局数据装配。
- [x] T006 迁移中型样板工具；文件/模块：`crew-flight-stats`、`hotel-bill-check`；依赖：T003-T004；验收：逻辑、上下文、视图、动作和入口以显式导入连接，业务测试直接导入源码，页面浏览器测试通过且无兼容壳。
- [x] T007 迁移其余轻型 Excel/文本工具并按 owner 决定下线 `crew-extract-id`；文件/模块：`focus-crew`、`crew-match-name-id`、`text-joiner`、`session-bill-check`、`personnel-structure-stats`，以及 `crew-extract-id` 的入口、源码和当前文档；依赖：T006；验收：保留工具按原业务和导出行为迁移为每页单入口，稳定模型有明确类型；`crew-extract-id` 无入口和残留当前文档。
- [x] T008 迁移图片、PDF 和 Word 工具；文件/模块：`image-tool`、`pdf-tool`、`pdf-stamp`、`word-template-filler`；依赖：T006；验收：第三方适配器、Blob URL、Worker/预览和独立生成 HTML 生命周期明确，当前功能与导出测试通过。
- [x] T009 迁移 Python 下载型页面接线；文件/模块：`lock-entry-helper`、`flight-stats-helper`、`oa-read-helper`、`qualification-query-helper` 页面及 shell；依赖：T005；验收：不重构 Python APP，下载、复制命令和入口页面正常，不强造无业务价值的应用模块。
- [x] T010 迁移换季学习；文件/模块：`seasonal-learning` 数据、健康检查、均衡规则、分配、导出、上下文、视图和入口；依赖：T006；验收：各模块显式依赖，均衡口径、移动记录、导出和现有测试完全保留。
- [x] T011 迁移校对之王；文件/模块：`proof-king` 模型、读取、比对、Worker、导航、决策、项目、报告、视图和入口；依赖：T008；验收：Worker 请求隔离与终止有效，DOCX/PDF 比对、复核、项目和报告结果保持一致，浏览器关键路径通过。
- [x] T012 迁移审计之王；文件/模块：`audit-king` 状态、文档读取、索引、搜索、定位、项目、导出、视图、动作和入口；依赖：T008；验收：不合并不同证据和定位口径，索引复用、状态更新和大文档生命周期明确，现有 13 个测试文件及浏览器关键路径通过。
- [x] T013 迁移培训皇帝；文件/模块：`training-workbench` 全部规则、扫描、统计、导出、状态、渲染、控制器和入口；依赖：T006、T010-T012；验收：遵守培训专项 skill 和全部 spec，五个可见入口、独立统计口径、模拟排班和导出行为不变，44 个应用脚本装配被真实替换为单入口包。
- [x] T014 清理旧装配；文件/模块：HTML、浏览器全局 `.d.ts`、IIFE、旧 VM helper 和无调用桥接；依赖：T005-T013；验收：内部业务实现不再注册到全局命名空间，没有新壳调用旧实现，没有重复入口，第三方全局例外有窄适配器和类型说明。
- [x] T015 完成真实浏览器测试；文件/模块：固定 Playwright、临时测试服务器、生产页面清单和脱敏合成夹具；依赖：T005-T014；验收：26 个构建 HTML 启动无 pageerror、404 或 console error，四个重型工具关键路径可重复通过；CI 安装固定 Chromium 并运行 `test:browser`。
- [x] T016 完成核心逻辑外部的性能剖析和优化；文件/模块：构建统计、应用脚本请求、模块初始化和资源存活检查；依赖：T001、T010-T015；验收：未修改核心算法，正式三次采样均在预算内，四页应用脚本请求均为单入口，相同输入的核心结果与金样完全一致，预热后五轮可达堆无持续增长证据。
- [x] T017 完成确定性构建；文件/模块：版本生成、构建清单、哈希检查和运行时版本声明；依赖：T003、T014；验收：同一提交、同一工具链连续两次构建 194 个文件内容一致，构建记录不依赖本地分支和实时时钟。
- [x] T018 完成第三方治理；文件/模块：`public/libs/versions.json`、许可证和资源清单测试；依赖：T002；验收：每个受管文件具备精确版本或经证据确认的标识、上游发布来源和许可证，清单恰好覆盖本地受管文件，未知 `pica` 问题闭合，npm 与浏览器依赖审计边界写清。
- [x] T019 同步当前文档；文件/模块：受影响 `spec/dev/`、`spec/user/`、README、CONTRIBUTING、项目 skills、运行和恢复说明；依赖：T003-T018；验收：只描述最终架构和当前操作事实，不保留迁移过程、旧内部协议或未经验证的性能宣传。
- [x] T020 全量验收与收口；文件/模块：全仓；依赖：T001-T019；验收：完成第 8 节全部命令和检查，更新本计划收口，工作区仅含本任务文件，明确 owner 仍需人工检查的视觉内容，未经要求不 commit、不 push。

## 8. 验证计划

相关局部测试：

- 每迁移一个工具，先运行其现有 `tests/tool/<name>/` 测试，再运行新增源码导入测试和浏览器启动测试。
- 培训皇帝按专项模块分别运行规则、有效期、排班总览、漏项、月历、模拟排班、CRM、年度统计和导出测试。
- 审计、校对、PDF、Word 和图片工具分别验证 Worker、Blob URL、项目文件、报告和第三方库适配器。

完整验证命令：

```powershell
npm.cmd run build
npm.cmd run typecheck
npm.cmd test
npm.cmd run verify
npm.cmd run test:browser
npm.cmd run verify:deterministic
npm.cmd run test:performance -- --training-workbook <培训表> --seasonal-workbook <换季表>
```

- `verify` 已包含类型、全量测试、真实浏览器和确定性构建门禁；第三方版本、来源、许可证和文件覆盖由全量 Vitest 中的资源清单测试验证。
- 真实浏览器命令长期保留脱敏合成夹具；性能命令使用 owner 本地真实输入并自动读取迁移前基线，真实输入和原始输出不提交。
- 不通过重复执行相同子命令掩盖失败；记录每类验证的实际结果。

构建或安装检查：

- 在清洁依赖环境使用 `npm.cmd ci` 和固定 Python 依赖完成从零构建。
- 构建后验证所有 HTML、本地库、Worker、Python 文件、模板和生成数据路径存在。
- 连续两次独立构建并比较核心产物内容哈希。
- 检查生产页面应用包数量、原始/压缩体积、source map、构建清单和版本记录。

页面/工具入口检查：

- 通过本地 HTTP 服务打开首页、工具首页、支持页、Jobskill 和全部 19 个工具入口。
- 自动捕获请求失败、控制台错误、页面异常和必要初始化标志。
- `test:browser` 自动验证可观察交互和运行状态；页面视觉效果由 owner 人工检查。

性能检查：

- 使用 T001 固定的浏览器、机器、夹具、预热次数、正式重复次数和统计口径。
- 比较迁移前后请求数、应用包体积、冷启动中位数、关键任务中位数/高分位、长任务、峰值堆和重复操作后堆趋势。
- 对培训、审计、校对、换季分别保留结果正确性校验，确保性能变化没有省略计算或输出。
- 对全部受保护核心 API 比较重构前金样和重构后结果；任何差异都先判定为失败，不允许以性能收益抵消正确性差异。
- 性能原始文件放临时目录或 CI artifact，不提交真实业务数据和机器相关噪声文件；仓库只保留可复现脚本、合成夹具、预算和摘要。

恢复或中断演练：

- 在没有现有 `dist/` 和缓存的环境按锁定版本重新生成站点。
- 中断 Worker 或快速替换输入，确认旧任务不会覆盖新状态，页面可以继续操作。
- 对大文件工具重复导入、清空、重新运行，确认资源释放和错误恢复。
- 使用正式构建产物启动本地服务，不依赖开发服务器私有能力。

文档同步检查：

- 检查工具 spec、用户手册、README、贡献指南、构建和恢复说明与最终代码一致。
- 用户手册不写内部 ESM、打包器和类型细节，除非它们改变用户运行前提。
- 开发文档明确模块边界、入口约定、第三方适配器、性能基准和正式发布方法。

未验证内容：

- owner 尚未提供最低企业浏览器/Windows 版本前，只能按当前 CI 与本机可用 Chromium 建立自动验证基线。
- owner 未提供可公开脱敏的真实大文件前，CI 性能与浏览器测试只能使用合成夹具；真实业务样例仅能本地回放且不得提交。
- UI 视觉一致性由 owner 最终人工检查。

剩余风险：

- 全仓一次性 ESM 迁移会改变大量内部依赖和测试入口，主要风险是未被现有测试覆盖的页面初始化与第三方加载边界。
- 模块语法迁移仍会触碰核心文件，因此必须依靠迁移前金样、现有测试、机械化 diff 审查和逐工具验证共同证明运行语义未变；不能只凭类型检查或解释认定等价。
- 浏览器定时和内存指标受设备与运行环境影响，必须使用固定方法和允许区间，不能把单次数字当作事实。
- 第三方 UMD 库继续作为受控全局边界；它们的上游停更、安全公告和文件格式变化仍需长期维护。
- 10 至 30 年的可运行性不能靠永久冻结保证，仍需要定期恢复演练、浏览器基线复核和依赖安全维护。

## 9. 收口

目标是否完成：T001 至 T020 已全部完成。浏览器源码已迁为标准 ESM 显式依赖，25 个页面入口按页面生成单一应用包，校对 Worker 独立构建；旧内部全局注册表、多脚本装配和 VM 全局测试加载协议已删除，`crew-extract-id` 已从入口、页面、源码、spec 和用户手册下线。核心业务算法、规则和可观察输出未作业务改写。

失败测试是否变绿：已变绿。稳定业务测试直接导入 `src/`，真实 Chromium 页面启动与四项重型工作流、确定性双构建、第三方完整性和严格类型门禁均已纳入正式命令；旧业务断言未删除、放宽或改写以迁就迁移。

改了哪些文件：浏览器工具和站点的 `src/` ESM 源码及稳定模型、对应 `public/` 单入口 HTML、`scripts/build.mts` 与验证脚本、TypeScript/npm/CI 配置、第三方资源清单、源码直导测试和合成夹具、ESM 交付 spec、README、CONTRIBUTING、项目 skills；删除 `crew-extract-id` 全部当前实现与文档。真实 Excel、临时浏览器输出和性能原始报告未进入改动文件。

跑了哪些验证：`npm.cmd run build` 通过，生成 25 个页面入口、1 个 Worker 和 1 个 Python 资产；`npm.cmd run typecheck` 通过；`npm.cmd test` 通过，Vitest 66 个文件 260 项、Python 9 项；`npm.cmd run verify` 通过，包含全量测试、浏览器和确定性构建门禁；`npm.cmd run test:browser` 在 Chromium 145.0.7632.6 中启动 26 个 HTML，零 pageerror、HTTP/请求失败和 console error，四个重型合成工作流重复结果稳定且每页只有 1 个应用脚本请求；`npm.cmd run verify:deterministic` 连续两次构建的 194 个文件完全一致；第三方资源清单测试 2 项通过；培训规则 12/7/5/6 条校验、培训 12 个测试文件 21 项、审计及项目归档 14 个测试文件 68 项均通过；`start_index.py` 默认使用浏览器允许访问的 4567 端口，专项启动测试 2 项通过且实际访问 `/index.html` 返回 200；`git diff --check` 通过。

真实输入性能验证：owner 本地培训表和换季表均完成回放且未提交。培训、换季、审计、校对工作流中位数分别为 951.88、212.30、183.99、261.74 ms，五轮堆增长分别为 1141060、969198、233228、229704 字节，均在预算内；结果 SHA-256 分别为 `36f72df2caa826ef9d27246f90503da605437ba0ff5dc94a2496703ef6a86434`、`926d9695af95e4b6216dc0599eb4430fac9f0163484919ffee3902746eacdda3`、`123d95246be4bfb6ca84baf8090085de514468ea159485553f13ef684fdeacbd`、`eee5043dceb616b950ce2feb312425e97be943ebe461e306175db910efbce4ad`，与迁移前金样完全一致。

哪些内容没有验证：最低企业 Edge/Chrome 和 Windows 版本仍未由 owner 指定；未提交可公开脱敏的真实大文件夹具，因此 CI 使用结构等价合成夹具；页面视觉一致性按仓库规约仍由 owner 人工检查。

剩余风险：浏览器性能数字受机器和版本噪声影响，应继续按现有固定方法和预算复核；本地 UMD 第三方库已具备版本、来源、许可证和文件覆盖门禁，其上游停更与安全公告仍需长期维护；10 至 30 年运行能力仍依赖定期恢复演练和浏览器基线复核。

是否 commit / push：已 commit 并 push；未部署。
