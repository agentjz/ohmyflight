# watchdog 开发历史

更新时间：2026-08-19

已提交历史锚点：以 `master` 最新提交为准；当前工作区事实以 `AGENTS.md`、`spec/`、代码和测试为准。

当前项目地址：`https://luckymaomi.github.io/watchdog/`

用途：给后续窗口继续理解 watchdog。它不是 `spec/`，不是 README，也不是产品宣传。`spec/` 只写当前工具怎么用；这份文件写历史：业务怎么长出来，哪些路试过又删掉，人和 AI 怎么从反复误解走到现在的协作方式。

## 一句话

watchdog 从一开始就不是炫技项目。

它的价值一直是实用：把飞行部真实工作里烦、碎、容易错、重复做的事，变成能上传、能核对、能导出、能复查的工具。

技术栈只是承载方式。真正主线有两条：

```txt
业务史：真实工作 -> 工具探索 -> 删除无效工具 -> 培训皇帝 / 审计之王等核心工作台
协作史：口头纠偏 -> 反复误解 -> spec / test / skill / AGENTS 固化事实 -> 新窗口也能接上
```

## 项目边界

- watchdog 的前身是 777F / 777发。
- 项目所有者确认：777F 第一版来自 Void Editor，早期也有 Kiro 参与，后续主要由 Codex 维护演进。
- 当前可见证据能证明：Kiro 早期“飞行部工具箱”讨论、777F git 历史、watchdog git 历史、Codex 维护会话。
- 没有证据的内容不写成事实；项目所有者明确提供的背景作为 owner 事实记录。

## 读法

新窗口先看结论，再看阶段。

不要把历史里的旧方案直接恢复。历史是证据，不是当前实现。当前事实仍然以 `AGENTS.md`、`.agents/skills/`、`spec/`、代码和测试为准。

# 第一部分：业务史

## 阶段 00：种子期，先把实际活变成工具

时间：早期到 2026-03

owner 事实：

777F / 777发是 watchdog 的前身。第一版来自 Void Editor，早期也有 Kiro 参与。

当前可见 Kiro 证据：

- 项目被描述为“飞行部工具箱”。
- 目标是本地、离线、纯前端。
- 工具围绕 PDF、Word、Excel、Markdown、图片、OCR 和飞行业务表格展开。
- 页面形态偏传统：HTML、Bootstrap、iframe、本地 `libs/`。

当时真正要解决的问题不是“做一个网站”，而是把工作里重复的活变成工具：

- 表格太多，人工筛选慢。
- 姓名、员工号、日期、培训项目容易看错。
- PDF、Word、Excel 处理经常重复。
- 有些工作不需要服务器，只需要本地把口径算清楚。

走对的地方：

- 先解决真实工作，不先追技术先进。
- 数据尽量留在本地，不上传。
- 一个工具解决一个具体问题。
- 工具可以粗糙，但必须能产出结果。

埋下的问题：

- 工具越来越多后，入口会乱。
- 每个工具自己写上传、解析、导出，重复会越来越多。
- 业务规则只在代码里，后续容易忘。
- 没有测试时，规则改动很容易把旧场景改坏。

最后留下的判断：

watchdog 的项目气质从一开始就是“把活干完”。这个判断到现在都没有偏移。

## 阶段 01：777F 第一批真实工作流

时间：2026-03 下旬

关键 777F 提交：

- `2bdc6fc 2026-03-25 fix:飞行经历/左座经历起落数按天统计和新增锁班助手添加备注的功能`
- `9e41c92 2026-03-25 update:飞行经历查询循环`
- `845367c 2026-03-27 update:换季学习抽风查询助手`
- `291db4d 2026-04-09 更新危险品，英语汉语考试培训规则`

这一阶段的业务重心：

- 飞行经历 / 左座经历起落数统计。
- 锁班助手。
- 换季学习查询。
- 危险品、英语、汉语考试培训规则。

这些工具不是抽象需求。它们来自实际工作里会反复出现的“人肉劳动”。人工也能做，但慢、烦、容易错。

走对的地方：

- 先自动化最烦的重复劳动。
- Python 小助手用于浏览器或外部系统操作。
- 表格工具用于本地统计和导出。
- 业务规则先跑起来。

暴露的问题：

- 规则还没有足够文档化。
- 工具入口和代码结构还不够稳。
- 训练、锁班、飞行经历这些重要工具逐渐暴露：只“能跑”不够，必须能解释结果。

最后留下的东西：

777F 阶段证明了项目有真实价值。后面迁移、重构、测试、spec，都是为了让这些真实价值长期站住。

## 阶段 02：工具箱膨胀，什么都想放进去

时间：2026-04 上旬

当时出现或讨论过的工具方向：

- PDF 工具。
- Word 模板填充。
- Excel 批处理。
- Markdown / 文档查看。
- 图片处理。
- OCR。
- 文件批量工具。
- 账单 / 甘特 / 航班 / 名单类工具。

这一阶段有一半是对的，一半是危险的。

对的地方：

飞行部工作确实会遇到各种文件处理和表格处理。PDF、Word、Excel、图片、本地模板这些能力不是装饰，它们是办公工具箱的底座。

危险的地方：

不是每个能做的工具都值得长期维护。工具太多会让首页和维护成本失控。通用文件工具如果没有明确业务场景，很容易变成“做了但不常用”。

最后留下的判断：

watchdog 可以有通用办公工具，但必须服务真实工作。工具不是越多越强，留下来的必须有使用场景。

## 阶段 03：平台化试探，后来证明不是主线

时间：2026-04 中旬

当时试过的方向：

- 通用 workflow 平台。
- 类似 n8n / Coze 的节点编排。
- React 拖拽节点。
- 插件接口。
- 工作流工具集合。

为什么当时想做：

工具越来越多后，很自然会想到“能不能把所有工具变成节点，用工作流串起来”。它看起来更高级，也似乎能解决复用问题。

后来证明为什么不对：

- 用户真正要的是“上传这份表，得到这个结果”，不是理解节点系统。
- 通用平台会把简单工具复杂化。
- 很多业务问题不是流程编排问题，而是业务口径问题。
- 平台一旦做起来，会吞掉培训、锁班、账单这些真正重要的业务规则。
- React / 构建 / MIME / 本地服务问题当时也制造了很多和业务无关的返工。

最后留下的实践：

watchdog 不做大一统工作流平台。它选择“独立业务工具 + 统一入口 + 统一 spec + 统一测试”的路线。

这条探索路线很重要：

它说明项目不是没想过平台化，而是试过以后发现主线不在这里。watchdog 要解决的是具体工作，不是展示一个通用系统。

## 阶段 04：从 777F 迁移到 watchdog，拒绝假迁移

时间：2026-04-15 到 2026-04-16

关键提交：

- `369ff49 2026-04-15 create spec`
- `48b397d 2026-04-15 已迁移，停止维护`
- `67d5521 2026-04-16 typescript rewrite,tool,workflow`

当时的核心问题：

777F 能做事，但长期维护开始吃力。迁移到 watchdog 不是换名字，而是把项目从“工具堆”变成“可维护的业务工具仓库”。

当时明确退出的路线：

- 旧文件夹继续被新代码依赖。
- TypeScript 只是包一层旧 JS。
- 只让构建通过，不整理业务事实。
- 把旧工具全部无脑搬过来。

这里有一个重要协作经验：

AI 很容易把“迁移”理解成目录和构建迁移。项目所有者真正要的是“第一方事实只剩 TS，业务逻辑可维护，错误尽量在类型和测试阶段暴露”。所以当时才会反复追问：

```txt
这是不是挂羊头卖狗肉？
用 TS 重构的意义在哪里？
```

最后证明正确的路：

- 建立 `src/`、`public/`、`spec/`、`tests/`。
- 工具入口集中管理。
- spec 记录当前用户侧规则。
- 测试保护关键业务口径。
- 旧项目停止维护，新项目接住主线。

这阶段留下的核心原则：

迁移不能是假迁移。真正迁移必须让业务规则、文件结构、入口、测试、文档都进入新主干。

## 阶段 05：培训皇帝实践史，项目最硬的业务主线

时间：2026-04 下旬到 2026-06

关键提交：

- `660159a 2026-04-16 update:培训计划核对`
- `b8fea36 2026-04-17 更新培训计划核对,有效期内均判定为OK`
- `a4dca33 2026-05-09 升级为培训工作台`
- `b9d202d 2026-05-11 完善超级培训工作台和CRM年度核对`
- `165d90b 2026-05-12 增加超级培训Excel健康检查`
- `e6e7add 2026-05-12 新增TSA安保独立项目规则`
- `6b8aa13 2026-05-13 改进超级培训年度有效期到期压力图`
- `8b92f4f 2026-05-13 更新培训皇帝工具与仓库结构`
- `80759f4 2026-06-01 更新培训自动过滤清单口径`
- `0ca8c04 2026-06-05 调整培训有效期更新为机器看口径`
- `6048cc2 2026-06-12 CRM年度核对不再过滤是否运行=否的人员，覆盖全员`
- `7d7cb0d 2026-06-22 培训工作台：完善航班统计、教员管理、年度培训功能及工作簿健康检查，移除git推送脚本`

培训皇帝为什么是核心实践：

它不是 Excel 筛选器。它处理的是一整本培训总表，里面有人员信息、CRM、多个项目 sheet、有效期、排班记录、录入状态、取消备注、教员、窗口期、年度压力、补训、导出。

任何一个字段理解错，结果就会错。

### 早期路线：到期清单、预排、培训计划核对

最早的培训工具更像几个入口：

- 到期清单查询。
- 预排班设置。
- 培训计划核对。
- 有效期更新。

这些入口当时合理，因为问题还没有完全收束。后来它们没有作为当前可见入口保留。当前培训皇帝只保留：

- 排班总览。
- 有效期更新。

为什么收束是对的：

用户真正需要的是“当前谁要处理、为什么要处理、已有安排是否覆盖、能不能导出”，不是一堆分散入口。入口越多，越容易让同一套业务规则分裂。

### 第一轮硬口径：有效期内已安排就是 OK

早期容易犯的错：

如果某人本月没安排，就认为有问题。

后来证明不对：

如果已经提前安排，并且安排在有效窗口内，就应该 OK。培训不是看“本月有没有安排”，而是看“已有安排能不能覆盖当前有效期风险”。

这个修正把工具从简单筛选推进到真正业务判断。

### 第二轮硬口径：项目规则不一样

培训项目不是统一规则：

- 应急训练有基准月。
- 危险品是 3 个月窗口，但失效前一日截止。
- 英语能力、汉语能力是 3 个月窗口。
- 航空安保、TSA、疲劳管理、飞行作风按最新日期。
- TSA 和航空安保后来被明确拆成两个独立有效期项目。

走过的探索：

把项目当成同一种有效期规则。

最后证明正确的路：

每个项目必须有自己的规则。规则写进 `training-rule-spec.md`，测试覆盖典型样例。培训皇帝必须按规则判断，而不是按项目名大概猜。

### 第三轮硬口径：航空安保和 TSA 不能混

一开始容易把 TSA 和航空安保看成一组相关训练。

后来明确：

- `航空安保` sheet 只证明做了航空安保。
- `TSA` sheet 只证明做了 TSA。
- 同一天同时做了航空安保和 TSA，也应该两个项目 sheet 各记录一条。
- 航空安保有效期 24 日历月，TSA 12 日历月。

这说明培训皇帝不能靠“看起来类似”合并规则。业务上是两个项目，代码里也必须是两个项目。

### 第四轮硬口径：`培训信息是否录入` 不是覆盖判断

这个字段非常容易误用。

最后规则是：

- 排班总览只判断是否存在有效安排。
- 只要项目 sheet 中已排且备注没有取消，就参与覆盖判断。
- `培训信息是否录入` 是人工录入状态字段，不参与排班覆盖判断。
- `机器看 = Y` 只用于有效期更新。

如果把 `培训信息是否录入` 当成覆盖判断，未录入但已安排的计划会被错误忽略。业务上，排班总览要解决的是“有没有安排覆盖风险”，不是“后台是否录入完成”。

### 第五轮硬口径：取消备注

备注包含 `取消` 的记录必须特殊处理。

最后规则是：

- 未录入记录备注取消：这条计划无效，不参与覆盖判断。
- 已录入或 `机器看 = Y` 但备注取消：数据矛盾，应异常提示或跳过。

这个规则看起来小，但很容易造成误判。培训工具不能只看日期，还要看备注语义。

### 第六轮硬口径：CRM 是年度核对，不是有效期项目

CRM 曾经容易被混进培训有效期逻辑。

最后规则是：

- CRM 是独立年度核对模块。
- 只读取精确名为 `CRM` 的 sheet。
- `CRM2025(不参与统计)` 这类历史 sheet 不参与。
- 指定年份内有培训开始日期或结束日期，且备注不包含取消，判定已参加。
- `培训信息是否录入` 不参与 CRM 参加判断。
- 人员信息表里 `是否运行 = 否` 的人员也进入应参加名单。
- CRM 教员不进入未参加名单。

CRM 不是“另一个培训项目”。它是年度参加核对。它只分已参加和未参加，不产生推荐排、必须排、窗口期、覆盖判断或有效期更新。

### 第七轮硬口径：健康检查不是第二套规则

培训总表很复杂，用户导入后必须知道程序怎么理解这本 Excel。

因此增加 Excel 健康检查：

- 说明识别了哪些 sheet。
- 暴露严重、警告、提示。
- 复用现有解析器、取消备注策略和 CRM 年度核对逻辑。
- 不维护第二套业务规则。

健康检查不是装饰。它是防止“工具默默算错”的保险。

### 第八轮硬口径：工具呈现事实，不替人乱判断

培训皇帝不是自动替人判断一切。

它围绕培训规则、窗口期、到期压力、已排覆盖、取消备注这些明确口径呈现事实。无窗口项目提前 2 个月推荐排、到期区间必须排，这属于必要业务判断。超出规则的“模型脑补判断”必须避免。

这条线后来变成协作原则：

```txt
机器和代码做确定事实。
人确认业务口径。
AI 不凭感觉替业务下判断。
```

### 培训皇帝最后变成了什么

当前培训皇帝面向南货航新总表结构，直接导入一整本培训总表。核心判断链是：

```txt
人员信息表当前有效期 -> 项目 sheet 有效排班记录 -> 培训规则 -> 备注取消特判
```

它保留两个可见入口：

- 排班总览。
- 有效期更新。

它不再保留独立的：

- 到期清单查询。
- 预排班设置。
- 培训计划核对入口。

这个收束是实践结果，不是功能缩水。分散入口曾经存在，但最后证明同一套规则应该在一个主工作台里闭环。

## 阶段 06：删除和收束无效工具，成熟不是越堆越多

时间：2026-05 到 2026-06

曾经出现后被删除或收束的方向：

- `training-tools-suite`
- `super-training-test`
- `bill-gantt`
- `file-batch-tool`
- `flight-filter`
- `health-poster`
- `seasonal-learning-check-test`
- `tool-mode-toggle`
- `workflows-data`

这些不等于当时做错。

很多工具在当时有探索价值。问题是：一个项目长期维护时，不能把每个探索都留在首页和主干里。

### `training-tools-suite` 为什么退出

它曾经承载规则参考、预排、冲突检查、有效期更新等多个入口。

问题是：

- 入口太分散。
- 同一套培训规则容易在多个页面重复。
- 用户要的是统一判断链，不是到处找入口。

最后它被培训工作台 / 培训皇帝吸收。

### `super-training-test` 为什么改名和收束

`super-training-test` 是培训皇帝的前身之一。

问题是：

- 名字像测试，不像正式工具。
- 逻辑一度过度合并。
- 到期清单、计划核对、预排、有效期更新这些能力需要重新梳理。

最后变成 `training-workbench`，用户侧叫培训皇帝。

### `bill-gantt`、`file-batch-tool`、`health-poster` 等为什么退出

它们更像通用能力、阶段性工具或边缘工具。

问题是：

- 和当前核心业务的绑定不够强。
- 长期维护优先级不如培训、账单核对、名单、锁班、审计这些硬场景。
- 留太多会让首页越来越像杂货铺。

最后它们退出主线。

这一阶段最后留下的判断：

删除不是退步。删除是项目变成熟的一部分。watchdog 不追求“我做过多少工具”，而追求“留下来的工具是不是能长期解决真实工作”。

## 阶段 07：锁班、人员结构、OA，外部系统自动化必须能核验

时间：2026-05 下旬

关键提交：

- `1ca836b 2026-05-26 改进通用锁班助手并发录入和结果校验`
- `890e7fb 2026-05-27 调整并发锁班助手备注填写逻辑`
- `5be8793 2026-05-27 新增人员结构统计工具并完善项目开发和UI规约`
- `3bcea0c 2026-05-27 新增人员结构报告表格填充脚本并完善闭合统计测试和核对规程`
- `d5992f9 2026-05-30 恢复锁班原始助手并补充并发助手入口`
- `98fcf74 2026-05-30 为原始锁班助手增加实时结果Excel`
- `0913e58 2026-06-10 修复锁班结果匹配误判`

这一阶段的业务问题：

有些工作不是浏览器本地算一张表就够了。锁班、OA 已阅、飞行经历查询这些任务要操作外部系统，所以 Python 小助手是必要的。

走过的探索：

- 过度相信自动化执行。
- 并发助手试图替代原始助手。
- 结果匹配不够硬，出现误判。

最后证明正确的路：

- 原始稳定入口要保留。
- 并发入口可以补充，但不能吞掉稳定路径。
- 自动化必须输出结果 Excel 或清晰结果，给人复查。
- 异常时要能看出哪条异常，而不是只说执行完。

人员结构统计的意义：

它不是技术工具，而是报告口径工具。核心是总人数、地面人员、占比、闭合统计这些业务定义要对。

最后留下的实践：

外部系统自动化必须可核验。它不是“点了按钮就算成功”，而是要留下结果、异常和复查路径。

## 阶段 08：账单、珠海、重点人员、航线，工具从计算走向核对

时间：2026-06

相关当前工具：

- 珠海皇帝。
- 酒店皇帝。
- 重点人员标注。
- 航线班次统计。
- 姓名匹配员工号。
- 提取员工号。

这一阶段的业务变化：

工具不再只是“把输入变成输出”，而是越来越多地承担核对：

- 名字是否匹配。
- 员工号是否识别正确。
- 场次表和账单表人次是否一致。
- 酒店账单和入住登记表是否能按姓名和日期容差匹配。
- 审班表里重点人员是否标清。
- 航线班次是否按人统计清楚。

酒店皇帝这一类工具说明了一个核心点：

有些工具不是为了完全替代人工，而是把人工核对前的材料整理好。比如导出表格后，人仍然要核对入住证明。工具价值在于把混乱输入变成可核对结果，而不是假装自动完成所有判断。

这些工具的共同原则：

- 输入可以乱，但输出要清楚。
- 异常要暴露。
- 人名、日期、员工号不能靠猜。
- 导出要便于复查。

最后证明正确的路：

业务核对工具必须让人看到差异。它的价值不是替人做最终决定，而是把人工最容易漏看的地方亮出来。

## 阶段 09：Word 模板填充器，效率来自真实流程变化

时间：2026-06 下旬

关键提交：

- `cab6a5f 2026-06-24 硬化Word模板填充器批量导入与项目验证流程`

这个工具一开始是“生成一个独立填写应用”。后面真正有价值的变化是批量导入：

- 先用配置表定义字段。
- 生成独立应用。
- 再通过 Excel 批量导入信息。
- 每行可以带文件标题。
- 批量导出多份 Word。

这不是简单功能升级，而是业务流程升级。

原来是让每个人填写。后来的思路是：信息集中收集后，自己导入 Excel，一次批量生成。效率变化很大。

走过的探索：

- 示例文件过多会干扰用户。
- “列表/循环字段”这种复杂能力第一版不支持，要明确边界。
- 批量 Excel 的列必须和配置字段稳定对应。
- 生成脚本太大时，后续维护会累，需要拆成生成模块。

最后留下的实践：

工具不是为了功能多，而是为了让真实流程变短。批量导入就是比“一个个填写”更贴近工作现实。

## 阶段 10：维护性收束，技术原则服务业务稳定

时间：2026-06 下旬到 2026-07-05

关键提交：

- `9247d2c 2026-06-23 重构多工具，抽离逻辑层，提高可维护性`
- `9890c9e 2026-07-05 稳定 Pages 部署并明确 Python 单文件边界`
- `4f6e674 2026-07-05 拆分重点工具入口职责并补强测试`

这一阶段技术开始变重要，但它仍然服务业务。

为什么要重构：

不是为了“代码优雅”，而是因为业务规则越来越多。如果页面、状态、解析、导出、规则、DOM 全塞在一起，每次修一个规则都可能撞坏另一个工具。

最后形成的技术原则：

- 业务逻辑和页面渲染分开。
- 解析、统计、规则判断放可测试模块。
- 页面只负责上传、展示、导出、状态提示。
- spec、代码、测试必须讲同一个当前事实。
- 超过 300 行触发职责审查，但不为了拆而拆。
- Python 工具和浏览器工具边界清楚。
- GitHub Pages 部署保持简单稳定。

这部分是技术史，但不是主线。

技术重构的目的，是让培训、审计、锁班、账单这些业务工具不被未来改坏。

## 阶段 11：审计之王，从表格工具进入证据工具

时间：2026-07 初

关键提交：

- `5240977 2026-07-03 新增审计之王手册依据检索工具`
- `d69e85e 2026-07-03 重写审计篮子并改用 Python 启动脚本`
- `906ff04 2026-07-03 完善审计之王关键词来源定位闭环`
- `010e906 2026-07-03 完善审计之王关键词排序和手册调教入口文案`
- `1cc6c13 2026-07-03 优化审计之王关键词和审计篮子交互`
- `3390fbb 2026-07-03 支持审计篮子按条款排序`
- `eb15633 2026-07-04 完善审计之王手册证据绑定和上下文查看`
- `25dc386 2026-07-05 优化审计之王全文上下文控制`
- `42582f3 2026-07-05 完善审计之王手册证据采集闭环`
- `948fd07 2026-07-05 拆分审计之王入口动作模块`

审计之王的业务意义：

它不是普通搜索。审计工作要的是依据链：

- 关键词从哪里来。
- 命中了哪条手册依据。
- 依据是否能绑定到审计篮子。
- 条款顺序是否清楚。
- 全文上下文是否可控。
- 用户能不能复查来源。

走错的路线：

- 只做一个搜索框。
- 把全文都塞给用户。
- 搜索结果没有证据绑定。
- 审计篮子只是临时列表。

最后证明正确的路：

- 搜索是入口，证据链才是核心。
- 关键词要有来源定位。
- 审计篮子要能编辑、排序、绑定依据。
- 全文上下文要控制，不能淹没用户。
- 入口动作要拆开，不能继续堆大文件。

审计之王说明项目开始进入第二类核心业务：

```txt
培训皇帝：规则判断和排班风险。
审计之王：手册依据和证据组织。
```

这两个方向都不是通用工具展示，而是具体业务工作台。

## 阶段 12：审计之王 PDF 证据工作区，把审计篮子落到 PDF 页面

时间：2026-07-06

这一阶段的业务问题：

审计篮子里已经有人为整理过的条款和依据原文，但最终迎审材料往往还要回到 PDF 手册页面。只让用户重新在 PDF 里人工搜索、截图、整理文件夹，会把已经做过的审计篮子工作再做一遍。

真正要解决的是：

- 审计篮子原文能不能帮助定位 PDF 手册页。
- 定位结果能不能让人快速核对，而不是假装全自动正确。
- 用户能不能按 1.1 到 1.61 这类序号组织证据。
- 每个序号能不能选择 PDF、页码范围、标题并导出。
- 做到一半能不能保存工作区，避免重复返工。

走过但没有继续走的路线：

- 用 Python 命令行直接自动高亮导出 PDF。
- 试图把命中区域做成完全自动的 PDF 高亮证据。
- 把算法识别结果当作最终事实。

这些路线退出的原因：

- PDF 文本层和页面显示之间不总是稳定对应。
- 自动高亮一旦偏移，用户很难信任结果。
- 真实迎审材料不能只靠算法判断，必须保留人工核对和调整入口。

最后收束出的当前路线：

- 仍然保持纯前端工作台，不引入 AI，不做 OCR。
- PDF 证据工作区放在审计之王底部，作为审计篮子之后的证据整理环节。
- 支持上传多份文字型 PDF。
- 支持从审计篮子生成证据槽，也支持按 `1.1-1.61` 生成空槽。
- 每个槽位可以编辑编号、标题、PDF、起始页、结束页和是否导出。
- 自动识别只给建议：根据审计篮子原文切片，在 PDF 连续页面中找集中命中。
- 结果同时显示状态和量化分数，例如可信度、覆盖率、片段命中数。
- 槽位点击后直接刷新右侧页面预览，不再用多余的预览按钮。
- 页面预览和左侧槽位列表保持固定高度，内容超出时滚动。
- 预览使用高清 canvas 渲染，便于人工核对页面。
- 自动识别填入页码时，可用复选框选择是否前后各加 1 页。
- 勾选前后各加 1 页时，核心命中页保持不变，只把用于预览和导出的槽位页码向前后扩展，并且不越界。
- 不勾选时，槽位页码只使用核心命中页。
- 支持导出单条、勾选项或全部 PDF 证据，多条导出时按编号分文件夹。
- 支持导出和导入 PDF 工作区 JSON，保存槽位、页码、标题、识别结果、选中槽位和扩页设置。
- 工作区 JSON 不保存 PDF 本体，下次导入后按当前已上传 PDF 文件名重新绑定。

当前识别算法的形态：

它不是语义检索，也不是 AI 判断。它是一个偏保守的文本证据定位器，目标是把“审计篮子原文”稳定地映射到“PDF 连续页面候选范围”。

核心链路是：

```txt
审计篮子依据原文 -> 文本归一化 -> 切片 -> PDF 页面连续窗口 -> 逐窗口评分 -> 选最高候选 -> 写入槽位供人核对
```

归一化规则：

- 对文本做 NFKC 归一化，统一全角半角。
- 转小写。
- 去掉普通空白、全角空格、不间断空格。
- 去掉常见标点和符号，只保留中文、英文、数字。
- 去掉 PDF 页眉页脚类干扰，例如公司名、版本号、修订号、孤立页码、日期和短书名行。
- 去掉结构编号前缀，例如 `7.5.1`、`（1）` 这类编号。
- 保留数字和英文，因为 `1000小时`、`4500小时`、`ICAO`、`121.467` 这类内容本身就是关键证据。

审计篮子原文切片规则：

- 先按行处理审计篮子里的依据原文。
- 每行归一化后，太短或太弱的片段丢弃。
- `进入条件`、`飞行经历`、`执照`、`课程安排`、`训练资料`、`教学方法`、`体检合格证` 这类很短的通用标题，如果单独出现，不作为强证据片段。
- 长句不整段硬匹配，而是切成滑动窗口：约 26 个字符一片，步长约 20 个字符。
- 每个有效片段带权重，权重按片段长度计算，最低权重为 8。

PDF 页面候选窗口规则：

- PDF 读取后按页保存文本。
- 每本 PDF 都生成 1 页、2 页、3 页的连续页面窗口。
- 非连续页不会被合成一个候选窗口。
- 每个页面窗口也做同样归一化，再与审计篮子切片逐个匹配。

评分规则：

- 覆盖率：命中的片段权重 / 全部片段权重。
- 顺序一致性：命中的片段在 PDF 窗口里是否基本按原文顺序出现。
- 总分：覆盖率权重大，顺序一致性权重小；当前是覆盖率约占 78%，顺序约占 22%。
- 3 页窗口会有轻微密度惩罚，避免太宽的候选页范围轻易赢过更集中的命中。
- 候选结果按总分、覆盖率、页数更短、起始页更早排序。

状态分类规则：

- 没有候选、覆盖率低于约 45%，或命中片段少于 2 个，标记为未命中。
- 覆盖率高、顺序一致性高，并且领先第二候选，标记为可信命中。
- 其余有候选但不够确定的情况，标记为需确认。
- 依据为空或明确不适用时，标记为跳过。

为什么这样设计：

- 不能只按整段原文匹配，因为 PDF 抽取文本可能有换行、空格、编号和标点差异。
- 不能只按单个关键词匹配，因为容易误命中通用词。
- 不能自动认定最终证据，因为 PDF 文本层、页码和手册排版都可能变化。
- 所以算法只寻找“多个有效片段集中出现在连续页面”的候选范围，再交给用户核对。

扩页规则和核心命中页分开：

- `result.startPage/endPage` 保存算法识别出的核心命中页。
- 槽位里的 `startPage/endPage` 是用户预览和导出的页码。
- 如果勾选“前后各加1页”，槽位页码在核心命中页基础上前后扩展，但不会越过 PDF 边界。
- 如果取消勾选，槽位页码只使用核心命中页。
- 这样既保留算法判断的核心范围，也满足迎审证据通常需要前后页面上下文的实际使用。

这阶段最重要的判断：

PDF 证据工作区不是取代审计篮子，也不是取代人工判断。它是把“已经整理好的审计依据”快速落到“可导出的 PDF 页面证据”。算法只负责把可能页面推到用户眼前，最终页码和标题仍由人确认。

这里形成的新边界：

```txt
审计篮子：人工维护最终条款和依据文本。
PDF 证据工作区：用审计篮子原文辅助定位 PDF 页，并整理导出页面证据。
自动识别结果：建议，不是最终结论。
工作区 JSON：恢复工作状态，不保存 PDF 文件本体。
```

这个阶段也强化了一个旧原则：

复杂业务里，自动化的价值不是替人装作确定，而是把人需要核对的位置快速、清楚、可恢复地摆出来。

## 阶段 13：校对之王，手册差异不是把搜索做两遍

时间：2026-07

校对之王来自一个很具体的痛点：手册修订人员要知道参考手册新增了什么、删除了什么、哪些原有要求发生了实质修改，并且必须能立刻回到两侧原稿复核。

最初看起来，它很像审计之王：

```txt
把两本手册按标点切片 -> 用搜索引擎逐片检索 -> 找到相似句 -> 计算匹配率
```

这条路线只解决了“某句话能不能在另一本手册找到”，没有解决“这句话在整本手册修订序列中的位置和作用”。真实手册回放证明，差异比对不能等同于两次独立搜索。

### 走错的路线

第一条错误路线，是把每个标点都当作同等强度的切点，再做全量片段互搜。

问题是：

- 目录、表格、列表和连续短行会被切得过碎。
- 同一句通用表达可能在手册多处出现，单片段搜索不知道应该对应哪一处。
- 一段内容可能在另一侧拆成多段，或多段合并成一段；只支持 1:1 会同时误报新增和删除。
- 搜到相似句不代表它位于同一修订区间，也不能据此判断新增、删除或修改。

第二条错误路线，是把匹配率和大量“内容修改”作为主结果。

匹配率适合当对应证据，不适合当修订人员的工作入口。真实业务最关心的是整段新增和整段删除，其次才是同一事项里的文字变化。把所有不完全一致都报成修改，会制造大量噪声，用户仍然要从头读两本手册。

第三条错误路线，是让语义或 AI 猜测两个主体、职责、等级或数字是否业务等价。

这会把工具从“展示可复核差异”推向“代替业务人员下结论”。组织主体变化、训练时间变化、等级变化可能正是修订重点，不能因为语义相近就吞掉。通用工具也不能写死部门名、机型、等级、标题或示例句。

第四条错误路线，是把完整 Word 版式、全部差异事件和全部 PDF 页面同时压在浏览器主线程渲染。

顶配电脑仍会卡死，不是算力不够，而是主线程同时承担了 Word DOM、差异计算、大量事件节点和 PDF canvas。继续打补丁只会保留更多旧状态、旧别名和中间适配层。

第五条错误路线，是强行给 PDF 做逐字页内高亮。

PDF 文字层坐标和视觉排版并不总能稳定对应。高亮一旦偏移，用户反而无法信任。PDF 在这个业务里需要的是原始页面和正确页码，不需要复制 Word 的交互方式。

第六条错误路线，是只显示孤立摘录、表格化结果或彼此独立的左右预览。

没有上下文时，用户不知道新增内容应该插在我的手册哪里；左右原稿如果分别按不可靠的段落号或页码跳转，就会出现两侧根本不是同一事项。结果必须带共同前后锚点，并让两侧原稿同时定位。

### 最后选择的正确路径

校对之王最终彻底重建，不保留旧别名、兼容转发、历史适配层或旧语义包装。当前算法链路是：

```txt
原始 Word/PDF 结构单元
-> 保留原文，生成仅用于匹配的归一化副本
-> 按强标点重建句子，超长句再按弱标点拆分
-> 建立两侧唯一且完全一致的长句锚点
-> 用最长递增链保留顺序一致的全局锚点
-> 在相邻锚点之间用二元 gram 召回候选窗口
-> 支持 1:1、1:N、N:1 和受限 N:M 对应
-> 用最大权重有序链选择不重叠、不逆序的对应关系
-> 聚合连续未对应内容和对应文字差异
-> 输出参考新增、参考删除、内容修改、待确认
```

这里最关键的判断是：

- 标点切片仍然有用，但它只是句子重建规则，不是最终比对算法。
- 搜索索引仍然有用，但它只负责召回候选，不负责决定修订关系。
- 相似度仍然有用，但它只给对应候选打分，不作为业务结论。
- 顺序锚点决定两侧内容属于哪个共同修订区间。
- 没有可靠对应时宁可进入待确认，也不强行定性为修改。
- 连续新增和删除必须聚合成完整修订事件，不能把用户淹没在碎片里。

### 正确的浏览器实现

- 差异计算放入 Web Worker。Worker 是浏览器本地后台线程，不是在线服务，也不上传文件。
- 差异导航使用虚拟滚动，只渲染可视区附近事件。
- Word 使用轻量全文阅读视图，保留完整文字、纵向滚动、定位和高亮，不强求原始分页版式。
- PDF 显示原始完整页面，按页码跳转，只懒渲染视口附近 canvas，并释放远离视口的页面。
- PDF 不做页内文字高亮；Word 承担精确文字高亮。
- 新增或删除的一侧没有对应原文时，使用同一顺序区间的共同前后锚点定位插入位置。
- 两侧原稿同时定位，不让一侧等待另一侧渲染完成。
- 结果优先显示参考新增和参考删除，再显示内容修改和待确认。

真实第三章 Word/PDF 回放最终确认：差异切片、结构单元、Word 原文和 PDF 页码映射闭合，关键责任主体、数字、时间、条件和表格变化可以进入正确复核区域；复杂表格不再同时误报为新增和删除。

### 校对之王和审计之王的根本区别

两者共享的技术底座：

- 都在浏览器本地读取 Word/PDF。
- 都保留原文，并用归一化副本参与算法。
- 都用二元 gram 或本地索引缩小候选范围。
- 都要求算法结果回到原文坐标或 PDF 页码复核。
- 都不让 AI 语义判断替代业务人员。
- 都拒绝内置公司、部门、机型、等级、章节或关键词特判；特殊规则只能通过 hook 注入。

但两者解决的是不同问题。

审计之王是“一个检查项去多本手册找候选依据”：

```txt
检查项关键词 -> 多本手册独立检索 -> 多条候选命中 -> 人工保存证据 -> 人工采纳依据
```

- 查询和手册是 1:N。
- 每条命中相互独立，结果之间不要求保持全文顺序。
- 精确或宽松包含即可成为候选。
- 算法不判断某条内容是新增、删除还是修改。
- 最终产物是检查项下的证据链和审计依据。

校对之王是“两本有顺序的全文做全局对齐”：

```txt
我的手册序列 <-> 参考手册序列 -> 全局锚点 -> 区间有序对应 -> 修订事件
```

- 两侧是有主次的 1:1 版本关系。
- 同一句能搜到还不够，必须在全局顺序中找到可靠对应。
- 新增和删除只能根据锚点之间的对应缺口判断。
- 一处错误对应会影响后续区间，因此必须使用不重叠、不逆序的全局链。
- 最终产物是可复核的版本修订事件，不是审计依据篮子。

因此，校对之王不是更强的审计之王，审计之王也不是简化的校对之王。它们共享“搜索只做候选、原文才是证据”的纪律，但一个解决证据检索，一个解决版本对齐。

### 说人话版

最关键的判断其实只有一句：

```txt
“能搜到一句相似的话”和“这两处就是同一次修订的前后版本”，完全不是一回事。
```

审计之王像拿着一个检查问题，去很多本手册里翻答案。翻到一处像答案的内容，就先放到候选区，最后由人决定要不要采用。这里不在乎第一个答案和第二个答案在整本书里的先后关系，因为它们本来就可能来自不同手册、不同章节。

校对之王像两个人各拿一本旧版和新版手册，从第一页开始一起往后走。先找两边都完全一样、而且只出现一次的“共同路标”，再看两个路标之间：新版是不是多了一段，旧版是不是少了一段，原来同一段是不是改了数字、主体或条件。

所以：

- 搜索只能回答“这句话像不像、有没有出现过”。
- 顺序对齐才能回答“它是不是这里原来那句话的新版本”。
- 只有先确认两边处在同一个位置，才能判断新增、删除和修改。
- 找不准位置时就交给人确认，不能为了让结果好看硬配一对。

浏览器实现也遵循同一个朴素原则：

- Word 适合精确定位和高亮文字，就让 Word 做这件事。
- PDF 适合保留原始页面和页码，就让 PDF 做这件事，不强行画不可靠的高亮。
- 用户最关心新增和删除，就把它们放前面；相似度和匹配率只是辅助信息，不抢主位置。
- 电脑再强，也不应该一次渲染整本 Word、整本 PDF 和几百条差异；后台计算、按需渲染才是正常做法。

这也是今天最终选中正确路线的原因：不是算法名字更高级，而是它真正回答了手册修订人员的问题。

### 一次真实手册回放留下的教训

时间：2026-07

校对之王稳定后，曾尝试先切碎段落、再用结构键限制对应范围。真实手册回放证明，这套结构规则会和已有全文顺序对齐互相覆盖：原本能在共同前后锚点之间识别为修改的内容，被结构硬边界拆成新增和删除，四类结果随输入和规则组合大幅波动。

最终处理不是继续叠加权重，而是恢复并保留稳定的全文顺序算法，让新能力停留在读取、视图和导出边界：

- 结构信息可以帮助用户筛选和阅读，不能成为覆盖全文顺序证据的硬边界。
- 不用事件数量变少证明算法更好；先保证不漏，再按新增、删除、修改和待确认分类交给人复核。
- 章节筛选、关键词命中侧、Word 导出和上下文说明只消费既有事件，不反向改变对齐结果。
- 任何读取规则变化都必须重新用真实手册记录两侧单元数和四类事件数，不能只跑合成测试。

同一次回放还发现，参考 PDF 的 401 页各有一个人员编号姓名水印。最初在视觉行形成后按整行过滤，仍有 8 处水印先和正文合并，随后进入事件和导出结果。由此钉死了明确噪音的处理边界：

- 具体噪音只放在独立 `special-rules.ts`，不散落进通用读取器和对齐算法。
- 在 PDF 原始文本项阶段先按规范化后的完全相等过滤，视觉行阶段保留同口径兜底。
- 禁止用包含匹配删除噪音，包含相同姓名或编号的正常长句必须保留。
- 每次新增噪音词条都要核对原文单元、事件列表、页面搜索、Excel 和 Word，不能只看页面上某一处消失。

最终验收固定使用正确方向的两本真实 PDF 和人工对照 Word：人工标注 22/22 行可达，新增、删除和修改代表事件的双侧 PDF 都能跳到目标页周边，canvas 非空，Excel 和 Word 可以反向读取。这个过程再次证明，手册比对不能只验算法输出，还必须同时验输入清洗、人工样例覆盖、原页定位和导出结果。

## 阶段 14：审计之王重建，检查项成为唯一主对象

时间：2026-07

校对之王完成后，审计之王也暴露出用户心智问题：关键词池和审计篮子分别维护标题，同一个检查条款存在两份编号、名称、增删和排序状态。手册证据虽然属于关键词，却不能自然追加为同一条款的审计依据。

最终业务模型收束为：

```txt
检查项
├─ 检查编号
├─ 检查项名称
├─ 一个关键词
├─ 检查单来源
├─ 多条候选手册证据
└─ 多条审计依据
```

编号、名称和关键词都由人维护，缺失时保持空值，不互相猜测。保存命中只增加候选手册证据；“采纳为审计依据”只追加一条依据，不删除候选，不覆盖人工依据，同一候选不重复采纳。检查项列表和审计篮子只是同一对象的不同视图，不再保存两份条款事实。

旧关键词工作簿和旧审计篮子不再作为当前导入格式。新的检查项工作簿使用 `检查项`、`手册证据`、`审计依据` 三个 sheet 显式保存关系和证据 ID。公司手册输入统一支持标准 Word 和文字型 PDF。

这次还重新审查了审计之王搜索和 PDF 证据定位：

- 关键词宽松检索统一忽略 Unicode 空白、标点和符号，但命中仍映射回原文坐标。
- PDF 证据定位删除了公司名、业务标题和跳过词硬编码，外部特例只能通过空默认 hook 注入。
- 三本真实 PDF 共 1193 页、61 条审计依据的分类和页码结果保持完全一致。
- PDF 连续页窗口从“每条依据重复构建”改为“每轮识别只构建一次”，真实耗时从约 18.99 秒降到约 1.02 秒。

这里形成的新边界：

```txt
审计之王负责把检查项组织成证据链。
PDF 证据工作区负责把已经确认的审计依据落到候选页面。
搜索和定位都只提供证据，不代替人工采纳和确认。
```

说人话就是：以前页面上有“关键词这一摞”和“审计篮子那一摞”，用户要自己记住哪一条对应哪一条。现在只有一张检查项卡片，编号、名称、关键词、找到的手册原文和最后采用的依据都放在它下面。点“采纳”只是往这张卡片里再加一条依据，不会把别的内容冲掉，也不会凭空创建另一张同名卡片。

## 阶段 15：知识、入口和外部自动化继续分化，不再靠一个总工具承载

时间：2026-07-12 到 2026-07-30

这一段在旧 history 里没有独立记录，但提交和当前代码能够确认几条并行主线：

- 7 月 12 日把开发规格迁入 `spec/dev/`，新增 `spec/user/` 用户手册和手册入口；手册阅读方法、开发规则和业务事实开始分开维护。
- 7 月中旬补齐赞助页面、站点可见性和首页入口状态。入口是否展示、是否启用、是否冷却不再散落在页面里，而是集中由工具清单和站点配置表达。
- 7 月 20 日继续补强培训皇帝 TSA 与航空安保人员一致性检查，说明培训数据健康检查仍然只围绕明确业务口径扩展。
- 7 月 27 日锁班助手增加疗养、公休额度路由和冲突记录精准解锁重提。自动化不再只会“提交”，而是先按员工号、状态、日期重叠和唯一候选核对旧记录，再决定是否解锁并重提。
- 7 月 28 日人员结构统计统一闭合口径和 Word 报告写回，继续坚持统计结果、报告模板和测试三者一致。
- 7 月 29 至 30 日换季学习从旧检查工具断裂式重建为独立重型工具，围绕总名单、实际安排、岗位与美线带队身份、动态分组、人员移动和 Excel 导出形成闭环；旧交换组和固定三类算法在验证后退出。

这一阶段形成的共同边界是：知识内容、用户手册、入口状态、浏览器自动化和重型业务工作台各有自己的变化原因。统一入口不等于统一业务模型；能独立说清职责的工具应独立维护，只有共享主题、构建和事实规约。

## 阶段 16：首页视觉、浏览器模块和项目名称完成全仓收束

时间：2026-07-31 到 2026-08-11

7 月 31 日到 8 月 11 日主要解决的是全仓接线，而不是新增一套业务规则：

- 首页先后收束工具状态、锁屏、搜索、分类、主题和工作技能/用户手册入口，最后统一为当前公共主题；培训皇帝同时补上排班漏项检查和全宽资质压力视图。
- 8 月 2 日全部浏览器工具和站点入口迁移为标准 ESM 显式依赖和按页面单入口构建，移除内部 `window.*` 装配；构建、类型检查、核心测试和确定性隔离构建成为长期验证主线。
- 真实 Chromium 和性能基线在迁移期用于验收，确认稳定后退出全局验证；第三方库继续保留版本、来源和许可证记录，不用跨平台不稳定的文件哈希门禁制造假失败。
- 8 月 10 日全站统一公共主题，菜鸟教程、用户手册和维护入口继续收束；页面专属 CSS 只保留布局和领域特判。
- 8 月 11 日经历一次 `cargodog` 的断裂式更名后，最终又全量收口为 `watchdog`，统一仓库链接、包名、主题事件、存储键、构建变量、下载文件名和维护 Skills。

这段历史留下两条重要经验。第一，ESM、主题和项目名称这种全仓改动必须一次性改完整，不能留下两套入口、两套存储键或两套品牌。第二，临时验收设施完成任务后应退出；长期门禁只保留稳定、可复现并真正保护业务或交付结果的部分。

## 阶段 17：菜鸟教程独立成站，审计界面收敛，浏览器性能验证退出全局

时间：2026-08-10 到 2026-08-11

三本手册最初作为用户手册内容接入后，业务边界继续收紧。菜鸟教程最终只保留《飞行人员训练大纲》和《飞行技术管理手册》，并从单一大 JSON 拆成来源索引、主题模块和自由文章。副驾驶转机型、机长转机型、技术等级、资质和重新获得资格训练按业务关系组织；共享恢复规则只维护一份，但可在 A1、A2、B、C、D 等页面就地完整展开。页面不再用横向大表格表达没有比较意义的生命周期信息，条件、保持、失效和恢复改为纵向阅读，特殊类别不强套统一结构。

审计之王的审计篮子同步收敛了视觉和定位行为：检查项标题、关键词、依据正文和操作按钮使用更稳定的字号层级；长标题不会把关键词挤成逐字竖排；选择检查项或命中项后，检查单参考和全部详情分别把当前标记滚动到各自面板顶部。数据模型、搜索、采纳和导出规则没有因此改变。

这一阶段也确认，真实 Chromium 启动和 Playwright 性能采样只是 ESM 迁移期的临时验收手段，不继续作为仓库全局验证入口。仓库删除浏览器验证脚本、性能基线和相关命令，`verify` 收束为类型检查、TypeScript/Python 核心测试和确定性构建。飞行门户、锁班、OA 等生产 Python 自动化仍然使用 Playwright；退出的是仓库级页面测试，不是业务自动化能力。

这里形成的新边界：

```txt
页面视觉 -> owner 人工检查
业务规则 -> TypeScript/Python 核心测试
交付稳定 -> 构建与确定性构建
外部系统自动化 -> 各独立 Python APP 按自身流程使用 Playwright
```

## 阶段 18：更名和构建收口后的断层补记，飞行经历查询恢复旧工作流

时间：2026-08-12 到 2026-08-18

现有历史在 8 月 11 日停止更新，但仓库在随后几天仍有可确认的工作：

- 8 月 12 日继续补齐培训工作台的资质、培训提交和月初告警口径。
- 8 月 13 至 14 日连续重构培训工作台的资质压力、实际负载和智能排班视图，随后移除未经确认的模拟排班，并把真实表回放、测试和计划收口记录对齐。
- 8 月 14 日为工具清单增加独立应用 ZIP 导出，构建时按工具清单生成离线包；同时修复资源路径、构建完整性测试和 GitHub Pages 使用说明。
- 8 月 17 日修复测试期间共享构建目录竞态，并补充启动脚本的技术栈说明，删除不再可靠的独立 ZIP 数量校验。

这几天留下的共同判断是：构建、导出和页面入口都必须围绕工具清单的当前事实生成；测试应验证可观察的业务和交付结果，不应把临时目录数量或旧文件形状当成产品契约。

8 月 18 日的飞行经历查询工作把一段长期断开的旧逻辑重新钉回当前实现。旧工具的关键习惯不是“启动后跑完一批就退出”，而是：人工扫码登录，进入查询页后按天查询，逐人查询、单条失败继续，结果即时保存，浏览器保持打开，用户点停止才退出。为恢复这条工作流，单文件 APP 被拆成职责清楚的 `start.py`、`flight_stats/` 和 `web/`，但没有引入新的业务判断或强制拦截。

当前已确认并写入实现的规则：

- 工作台保留 `/api/start` 全链路 agent 入口；人工页面则通过“进入查询页面”“数据健康检查”“开始查询”“终止”分步控制，每一步停下来等用户操作。
- 查询范围必须直接写清输出口径：`飞行时间+起落数`、`飞行经历+起落数`、`左座经历+起落数`、`全部数据`。前三项可组合，全部数据按页面当前可见表头输出；页面没有的字段不补造。
- 进入飞行经历页面就沿用旧流程选择“按天查询”，不能等到真正查询时才尝试，也不能把门户单选框的点击异常误报成业务失败。
- 数据健康检查只是既有输入有效性提示，不是人工确认闸门；页面健康检查没有业务价值且容易报错，因此不再增加。
- 每次查询同时保留原版和去分钟版；去分钟版只处理飞行时间、飞行经历和左座经历的分钟部分。结果区逐人追加，用户向上滚动后不被强制拉回底部。
- 批次完成后复用同一浏览器、context、page 和 worker，重新选择 Excel 或粘贴数据即可继续；只有终止或服务关闭才销毁浏览器和进程树。
- 测试按输入、门户、导出、运行和 HTTP/会话模块拆开；运行结果目录加入忽略规则，源码、测试、日志和历史不写真实姓名、员工号或业务文件名。

真实门户扫码、真实账号权限、真实 Excel 批量查询和门户改版仍未在仓库环境验证；这部分只能在内网由 owner 按实际页面确认。当前本地已通过模块化 Python 测试、前端语法检查和仓库验证命令，不能把本地假 worker 通过等同于真实门户成功。

# 第二部分：协作史

## 协作阶段 00：早期靠口头纠偏，浪费很多时间

早期人和 AI 的协作最大问题不是“模型不会写代码”，而是没有共享事实。

用户说的是业务现象：

- 这个人这个项目在该月有任意一条记录就算。
- 提前安排且在允许范围内就算 OK。
- 导出只是给我看，不是二次导入。
- 插名字、标红、带有效期就够。
- 工具只是呈现事实，不要替我判断一切。

AI 很容易听成技术任务：

- 顺手建索引。
- 顺手抽象公共模块。
- 做一个更通用的流程。
- 做更多入口。
- 解释一个看起来“工程上更干净”的方案。

这就造成反复纠偏。

典型浪费点：

- AI 没先看仓库就开始判断，所以用户会说“你是不是先 research 一下我的仓库再说话呢？”
- AI 把迁移理解成目录结构变化，用户要的是可维护的业务事实迁移。
- AI 把业务规则理解成技术优化，用户要的是先把口径钉死。
- AI 想顺手做性能或结构优化，用户要的是如无必要勿增实体。
- AI 试图用通用平台、通用抽象、通用工具覆盖具体业务，最后又被拉回真实工作流。

最后学到的东西：

业务项目里，先听懂“这个活是怎么干的”，再讨论代码怎么写。

## 协作阶段 01：强约束提示词出现，因为普通对话接不住复杂任务

当项目变大后，普通一句“帮我改一下”已经不够。

用户开始写很强的任务提示：

- 一次性完成。
- 先 research。
- 不要半成品。
- 不要做一点停一点。
- 先写文档和验收标准。
- TDD。
- spec、代码、测试同步。
- 不要向后兼容旧垃圾。
- 不要挂羊头卖狗肉。

这些话不是情绪，它们来自反复校准。

强约束解决的问题：

- AI 容易只做局部补丁。
- AI 容易不验证。
- AI 容易用解释代替完成。
- AI 容易把历史旧目录当成兼容对象。
- AI 容易把用户话术直接写进代码，而不是理解业务事实。

强约束的副作用：

它能逼任务完成，但如果没有仓库规则承接，每个新窗口都要重新骂一遍、重新解释一遍、重新纠偏一遍。

所以后面必须把这些经验写进项目，而不是永远靠口头。

## 协作阶段 02：spec 开始承接业务事实

`spec/` 的作用不是写漂亮文档。

它是为了防止 AI 和人对业务事实各说各话。

在培训皇帝里，spec 解决了这些问题：

- 什么项目有什么有效期规则。
- 什么是基准月。
- 什么是 3 个月窗口。
- 危险品为什么是失效前一日截止。
- 航空安保和 TSA 为什么分开。
- CRM 为什么是年度核对，不是有效期项目。
- `培训信息是否录入` 不参与排班覆盖判断。
- `机器看 = Y` 只用于有效期更新。

在其它工具里，spec 解决了这些问题：

- 工具做什么，不做什么。
- 输入是什么。
- 输出是什么。
- 用户应该怎么核对。
- 哪些异常要暴露。

spec 的正确边界：

- spec 写当前用户侧功能事实。
- spec 不写历史。
- spec 不写未来计划。
- spec 不写实现过程。

history 的作用：

- history 写历史。
- history 解释为什么某些规则存在。
- history 记录哪些路线验证后退出。
- history 帮新窗口理解项目演进。

这两个分开以后，协作清楚很多。

## 协作阶段 03：测试从“证明能跑”变成“保护业务口径”

早期没有测试时，AI 很容易改坏规则还不知道。

后面逐渐明确：

- 修改业务规则，必须补测试。
- 修改日期规则，必须补测试。
- 修改导出结构，必须补测试。
- 修改解析规则，必须补测试。
- 曾经错过的场景，必须变成回归测试。

测试的目的不是保护目录形状，也不是证明 UI 文案没变。

测试要保护真实产品行为：

- 培训有效期规则。
- CRM 参加判断。
- TSA / 航空安保拆分。
- 酒店账单匹配。
- Word 批量导入导出。
- PDF 加水印位置规则。
- 人员结构闭合统计。
- 锁班结果匹配。

这里也有过协作经验：

AI 有时会写“某段旧代码不得出现”这类负向测试。这种测试看起来硬，实际容易保护口号。后来规则明确：除非对应真实业务可观察回归，否则不写这种负向测试。

最后留下的实践：

测试不是给 AI 刷绿的。测试是给业务规则守门的。

## 协作阶段 04：AGENTS.md 把沟通规矩写成仓库入口

后来 `AGENTS.md` 成为仓库最高约束。

它解决的是新窗口最容易犯的错：

- 不读 UTF-8，误判中文文件乱码。
- PowerShell 里直接用 `npm` / `npx`，踩执行策略问题。
- here-string 里的中文被编码污染，还以为是代码错。
- 不区分事实、推测、计划、完成。
- 没有原始输入就猜业务规则。
- 不看中间值，只看最终现象。
- 修改业务不更新 spec 和测试。
- 遇到脏工作区误删用户改动。

AGENTS.md 的核心不是“约束多”，而是把过去反复浪费时间的环节提前固化。

最重要的几条：

- 事实高于一切。
- 事实不清楚先排查。
- 不能猜业务规则。
- 用户给了原始输入，优先用原始输入复现。
- 表格、文本、日期、姓名问题，要看中间值。
- 代码、测试、文档必须同步。
- 文档只写当前事实，不写历史过程。

这些规则让协作从“每次靠用户提醒”，变成“模型接手就知道边界”。

## 协作阶段 05：skills 把不同工作流拆开

随着项目变复杂，一个总规约不够了。

于是出现项目级 skills：

- `watchdog-dev`：新增或修改工具的一般流程。
- `training-workbench`：培训皇帝专属规则。
- `ui-clarity`：页面 UI 清晰度。
- `playwright-probe`：内网页面探测和临时自动化。
- `docx-report`：Word 文档读取、填写、生成。
- `interview-lock-list`：面试名单转锁班导入模板。
- `plan`：中大型任务的单文件执行合同。

skills 解决的问题：

不同任务的成功标准不一样。

培训任务要先看培训规则。UI 任务要看页面清晰度。Playwright 任务要区分输入解析、DOM、提交值。docx 任务要看 Word 表格结构。大重构要有 plan。

如果都靠一个大提示词，模型会混。

skills 的价值：

它把“这类任务应该怎么做”从对话里移到仓库里。新窗口不需要重新问一遍，也不需要靠记忆猜。

## 协作阶段 06：plan.md 用来管大任务，不让任务边做边飘

当任务变成跨模块重构、工具下线、测试整理、生产级验收时，口头计划不够。

plan.md 的作用：

- 写清需求。
- 写清当前事实。
- 写清验证用例。
- 写清目标。
- 写清不做范围。
- 写清设计。
- 写清实施任务。
- 写清验证计划。
- 写清收口记录。

这不是形式主义。

它解决的是 AI 最常见的问题：

- 做着做着忘了最初目标。
- 修了局部，没收全局。
- 代码改了，spec 没改。
- 测试过了，但不是用户真实路径。
- 任务做到一半停下来让用户补洞。

plan.md 后来变成大任务的合同。它不是为了拖慢开发，而是为了防止大任务失控。

## 协作阶段 07：history.md 承接长期上下文

现在写 history，是因为对话已经太长，新窗口不可能天然知道所有历史。

但 history 不能写成干净架构图。

这个项目的历史本来就是：

- 做了。
- 发现不对。
- 删了。
- 又做。
- 又收束。
- 规则被一次次钉死。
- 协作方式也被一次次改造。

history 的正确作用：

- 让新窗口知道项目从哪里来。
- 让新窗口知道哪些路线试过后退出。
- 让新窗口知道哪些规则来自长期实践。
- 让新窗口知道用户为什么讨厌半成品、假迁移、无意义抽象。
- 让新窗口知道 spec 是当前事实，history 是历史证据。

history 不应该做的事：

- 不替代 spec。
- 不替代 README。
- 不写成宣传稿。
- 不把旧功能当当前能力。
- 不把没有证据的内容写成事实。

## 协作阶段 08：飞行经历查询对话暴露的“不要自作主张”边界

8 月 18 日飞行经历查询重构再次证明，代码质量不只取决于拆了多少模块，更取决于有没有守住旧工作流的边界。对话中反复出现的纠偏，最后固化成以下历史教训：

- 先研究旧脚本、页面路径和用户操作阶段，再动代码。用户说“之前怎么做”，指的是可观察的操作顺序和退出方式，不是让 AI 自己发明一套更自动的流程。
- 人工按钮和 agent 接口是两条并存的入口。前端分步操作不能替代 `/api/start` 全链路入口，也不能把“进入页面”和“开始查询”杂糅成一次启动。
- 健康检查只能复用已有数据有效性口径并提示结果，不能因为“看起来更安全”擅自加确认、拦截或页面健康检查。没有业务价值的保护会制造新的失败点。
- 页面控制必须让用户看得见状态：复选框要有选中反馈，范围名称要说明是否包含起落数，按天模式要在进入页面时确认，日志要说明打开浏览器、登录、进入统计应用和飞行经历页面的阶段。
- 输入提示只写规范字段格式，不写“到今日”这类会改变口径的文案，也不在 placeholder、示例或结果提示里放具体姓名。
- 结果是工作流的一部分，不是最后才出现的文件。逐人结果、原版和去分钟版、失败原因、进度和日志都要同步可见；自动置底只能在用户停留底部时发生。
- “浏览器保持打开，点停止才退出”是已有业务习惯，不应被单批次自动关闭、后台强制清理或新的恢复机制替换。下一批输入应复用登录会话。
- 生产数据隐私优先于示例方便。界面、文档、测试和历史不写真实姓名、员工号、业务文件名；运行结果目录必须忽略，不能让生产数据进入版本库。
- 测试按变化原因拆模块，保护输入、动态表头、导出、失败继续和 HTTP/会话状态机；不要为了目录好看写脆弱的旧文件存在性测试。

这段对话也补上了 history 本身的维护规则：发现日期或阶段断层时，要先用 Git、代码、spec 和测试核对事实，再一次性补齐缺失阶段；不能只在文件末尾加一条“今天改了什么”，也不能把未验证的内网结果写成完成。

## 协作阶段 09：锁班皇帝从模块拆分走向前后端工作台

同日对锁班皇帝的改造分成了两个明确阶段。第一阶段先删除并发助手，把原始串行和智能串行的输入、门户、路由、导出与运行职责拆清，并以两个入口形成可回退基线；第二阶段在该基线上补齐前后端分离工作台。这里形成了新的交付认识：

- “模块化”与“前后端分离”不是同一件事。把 Python 大脚本拆成模块，只解决代码职责；用户要求的工作台还必须具备本机 HTTP 后端、可见前端状态、人工阶段按钮、逐条结果和下载闭环。
- 明确要求“先推送，再改”时，要先把已验收阶段提交并推送，再进入下一阶段，不能让两种目标混在一个无法回退的提交中。
- 前端工作流继续沿用飞行经历查询形成的习惯：进入页面、数据健康检查、开始录入、终止分别控制；agent 另有全链路接口，两者不能互相替代。
- 新编排层只复用原始串行和智能串行现有业务函数，不重新发明锁班类型、额度路由、结果匹配、冲突解锁或重提规则。前后端改造不授权顺手改变业务经验。
- 一批结束后保留浏览器和登录会话，下一批复用同一 worker；只有用户终止或服务退出才关闭。原始和智能模式由两个启动入口固定，运行中不混用。
- 生产自动化的前端必须逐人或逐片段展示输入类型、实际类型、日期、门户结果与失败原因。结果 Excel 和运行目录同时属于生产数据，必须即时落盘、可下载并排除在版本库之外。
- 测试按输入、门户规则、导出、worker 和 HTTP 状态分层。真实门户小样本用于验证生产路径，仓库自动测试使用假 worker，不把真实姓名、员工号和结果带入代码或提交。

## 协作阶段 10：HTTP 锁班皇帝验证“替换执行层，不改业务层”

锁班皇帝的下一次演进没有继续优化 Playwright，而是新增了与旧工具并列的 `http-lock-entry-helper`。用户自己维护 IEB 登录态，工作台从 Copy as cURL 或 Cookie Header 中只提取必要 Cookie，通过纯 HTTP 复用门户录入、查询和状态接口。

这一阶段留下的事实和边界：

- 纯 HTTP 可以显著减少浏览器渲染和 DOM 操作开销，但不授权改变锁班类型、日期模式、备注、额度互换、冲突归属或重提规则。业务经验仍来自旧工具和真实探测。
- 门户返回 HTTP 200 不代表业务成功。员工、类型、日期时间和结果行必须结构化解析并精确归属，冲突解锁仍要求唯一旧记录、动作前落证据、动作后复核和最多一次重提。
- 登录由用户维护。工具不实现扫码、验证码或自动刷新，只验证当前会话；凭据只在进程内存，不能进入日志、状态、Excel、测试或 Git。
- 锁班属于写操作，历史上的并发曾造成记录对应错误。因此 HTTP 版仍保持全批严格串行，前端没有并发入口。技术变快不能覆盖已确认的生产风险。
- 人工工作台继续保留验证凭据、数据健康检查、开始录入和终止；agent 继续保留 `/api/start` 全链路。检查只提示，开始时重新读取当前输入。
- 结果逐条即时写入，保证中断或冲突动作后仍有审计证据；一批结束保留 Session，可直接换下一批。

这次形成了专用 `ieb-http-lock-entry` skill。它把动态类型、两种日期模式、额度路由、查询分页、唯一冲突解锁、Cookie 隐私和串行边界从一次性探针提升为后续维护入口。

## 协作阶段 11：飞行经历查询把纯 HTTP 用在只读查询

同样的 HTTP 思路随后用于飞行经历查询，但执行策略没有机械照搬锁班。飞行经历查询是只读操作，真实接口的一次人员请求同时返回页面全部可见字段，因此新增 `http-flight-stats-helper`，旧 Playwright 版继续保留。

这一阶段确认：

- 页面入口和查询接口都是普通 GET。当前有效会话未观察到 CSRF、`__VIEWSTATE`、动态签名或 Authorization Header；未登录会回到登录页。
- 一名员工一次查询同时返回飞行时间、飞行经历、左座经历、起落总数及其他可见字段。前端四个预设只决定输出列，不能为了多个预设重复请求。
- 早期探针确认只读查询可以使用 4 个独立 `requests.Session` 并发，结果按员工号和可选姓名精确归属，最终按原输入索引归位。
- owner 指定的真实模板回放共有 44 条有效数据，4-worker 全部查询成功、失败 0，当次总耗时约 14 秒；该数字是一次运行证据，不是永久性能承诺。
- 查询过程中逐人显示已返回结果，但 Excel 等全部任务结束后才一次性生成。原版与去分钟版保持 44 行和 19 个门户动态字段一致，三类经历分钟转换核对通过。
- 登录凭据仍只存在内存，真实输入、人员、响应和结果目录不进入 Git。结果目录同时从版本控制和独立下载包排除。

随后 owner 因并发风险和维护成本决定撤回并发。当前产品改为单个验证 Session、严格按输入顺序逐人查询，不保留并发数、并发开关或 worker 池；上述 44 条约 14 秒只保留为历史探测事实，不作为当前串行性能承诺。

这次新增 `ieb-http-flight-stats` skill，专门记录接口参数、动态表头、四预设、单个 Session、严格串行、登录失效停止后续查询、最终双文件和隐私边界。由此也明确：是否并发取决于业务副作用、维护风险和 owner 的当前决策，不由“HTTP 比浏览器快”这一条理由决定。

## 协作阶段 12：技术等级运行资格查询补齐本地工作台

`qualification-query-helper` 原来是终端驱动的单文件 Playwright 脚本：脚本负责扫码、菜单导航、Excel 读取、两张表解析、结果写回和退出。后续改造保留原有门户查询与输出口径，把它变成“技术等级运行资格查询助手（乞丐版）”本地前后端工作台。

这一阶段确认并固化：

- 前后端分离不改变执行技术。浏览器仍由 Playwright 驱动，只是用户在本地页面控制 Cookie、输入、查询阶段、结果和停止动作。
- 登录由用户维护。Cookie 只注入当前可见 Chromium 的 browser context，不保存、不回显，也不进入日志、Excel、测试或 Git。
- “进入查询页面”和“开始查询”必须是两个动作。前一个动作依次验证登录态并进入资质管理、飞行训练、技术资料和资料管理；页面就绪后停下，等待人工开始。
- 数据健康检查只复用员工号、重复行和输入有效性规则，不检查门户页面，也不成为强制确认。Excel 和粘贴输入都以六位员工号为查询键，姓名只做结果核对。
- 原有资料管理事实保持不变：精确员工号链接、技术等级 `#qualList`、运行资格 `#showSingleEmpOperQualList`、跨 frame 和合并单元格展开、表头校验、失败后关闭个人弹窗。
- 查询严格串行，每人完成后立即原子保存三张结果表，同时把页面姓名、技术等级条数、运行资格条数和失败原因推送到工作台。
- 一批完成后浏览器保持打开并可准备下一批；只有停止接口或服务退出才关闭整个 Playwright 进程树。人工分步入口与 agent `/api/start` 全链路入口并存。
- 一次授权的单人只读回放验证了完整链路，页面身份归属一致，返回 4 条技术等级和 10 条运行资格；实测文件随后删除，仓库没有保留真实人员、Cookie 或结果。

这次拆分以变化原因为边界：Cookie、输入、门户 DOM、导出、批次编排、状态、进程管理、HTTP 接线和前端分别维护。拆分的价值是让门户页面、Excel、UI 或进程生命周期单独变化时不必修改同一个大脚本，而不是为了文件数量本身。

# 第三部分：当前形态

## 当前正式工具

- 菜鸟教程。
- 培训皇帝。
- 换季学习。
- 审计之王。
- 校对之王。
- 姓名匹配员工号。
- 锁班乞丐。
- 锁班皇帝（新）。
- 飞行经历查询（乞丐版：飞行时间+起落数、飞行经历+起落数、左座经历+起落数、全部数据）。
- 飞行经历查询（皇帝版）。
- 技术等级运行资格查询助手（乞丐版）。
- 珠海皇帝。
- 酒店皇帝。
- 重点人员标注。
- 航线班次统计。
- 自动点 OA 助手。
- Word 模板填充器。
- PDF 工具。
- PDF 加水印。
- 图片工具。
- 文本拼接助手。
- 人员结构统计。

## 当前项目不是

- 不是通用工作流平台。
- 不是所有小工具都往里塞的杂货铺。
- 不是只看技术栈的前端项目。
- 不是只跑得通就算完成的 AI 生成项目。
- 不是炫技项目。

## 当前项目是

- 飞行业务工具集合。
- 浏览器本地处理为主。
- Python 小助手补足外部系统自动化。
- 培训和审计是最重的业务工作台。
- PDF、Word、图片、名单、账单、航线等工具围绕真实办公场景服务。
- spec 记录当前事实。
- history 记录历史。
- tests 保护核心业务规则。
- skills 记录不同任务怎么协作。
- AGENTS.md 记录仓库最高工作规约。

## 最硬的业务主线

```txt
培训皇帝：培训总表 -> 规则判断 -> 排班风险 -> 有效期更新 -> 可核对导出
审计之王：检查项 -> 关键词检索 -> 候选手册证据 -> 审计依据 -> PDF 证据闭环
校对之王：两本手册 -> 全局顺序对齐 -> 完整修订事件 -> 双侧原稿复核 -> Excel / Word 差异报告
```

其它工具围绕真实工作补足：

```txt
锁班 / OA / 飞行经历 -> 外部系统自动化
账单 / 珠海 / 航线 / 人员结构 -> 表格核对和报告
PDF / Word / 图片 / 员工号 -> 日常办公文件处理
```

## 最重要的业务实践经验

- 工具不是越多越好。
- 培训规则不能靠直觉。
- 字段名相似不代表业务意义相同。
- 已安排、已录入、机器看，是三件事。
- CRM 不是有效期项目。
- TSA 和航空安保不能混。
- 取消备注不是普通备注。
- 自动化必须能复查。
- 删除无效工具是成熟，不是倒退。
- 技术重构必须服务业务稳定。

## 最重要的协作实践经验

- AI 先 research，再说话。
- 先用非技术语言对齐业务。
- 用户说的是业务现象，不要直接翻译成技术方案。
- 不要顺手优化，先判断业务必要性。
- 如无必要勿增实体。
- 不要用通用平台吞掉具体业务。
- spec 写当前事实。
- history 写历史证据。
- tests 保护业务口径。
- skills 固化不同任务的工作方式。
- AGENTS.md 是新窗口入口。
- plan.md 管大任务，防止任务漂移。

## 后续新窗口接手顺序

1. 读 `AGENTS.md`。
2. 按任务读 `.agents/skills/.../SKILL.md`。
3. 读相关 `spec/dev/<tool>/` 和 `spec/user/<tool>/manual.md`。
4. 看同类成熟工具源码。
5. 看相关测试。
6. 需要理解历史原因时，读本文件对应阶段。

最后记住一句：

```txt
真实业务问题 -> 清楚业务口径 -> 可复查输出 -> 再谈技术实现
```
